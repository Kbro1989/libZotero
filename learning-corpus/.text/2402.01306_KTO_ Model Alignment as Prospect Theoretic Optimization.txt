                                        KTO: Model Alignment as Prospect Theoretic Optimization

                                        Kawin Ethayarajh 1 Winnie Xu 2 Niklas Muennighoff 2 Dan Jurafsky 1 Douwe Kiela 1 2

arXiv:2402.01306v4 [cs.LG] 19 Nov 2024                          Abstract                                    Figure 1. The utility that a human gets from the outcome of a
                                                                                                            random variable, as implied by different human-aware losses (HA-
                                              Kahneman & Tversky's prospect theory tells us                 LOs). Notice that the implied value functions share properties
                                              that humans perceive random variables in a biased             such as loss aversion with the canonical human value function in
                                              but well-defined manner (1992); for example, hu-              prospect theory (Tversky & Kahneman, 1992).
                                              mans are famously loss-averse. We show that ob-
                                              jectives for aligning LLMs with human feedback                x). This is because preferences, despite being a kind of data
                                              implicitly incorporate many of these biases--the              that is relatively scarce and expensive to collect in practice
                                              success of these objectives (e.g., DPO) over cross-           (Casper et al., 2023), are required by the alignment methods
                                              entropy minimization can partly be ascribed to                shown to work best--RLHF (Christiano et al., 2017) and
                                              them belonging to a family of loss functions that             DPO (Rafailov et al., 2023).
                                              we call human-aware losses (HALOs). However,
                                              the utility functions these methods attribute to hu-          To understand why these methods work so well, and whether
                                              mans still differ from those in the prospect theory           feedback needs to be in preference form, we frame align-
                                              literature. Using a Kahneman-Tversky model of                 ment through the lens of prospect theory (Kahneman &
                                              human utility, we propose a HALO that directly                Tversky, 1979; Tversky & Kahneman, 1992). Prospect the-
                                              maximizes the utility of generations instead of               ory explains why humans make decisions about uncertain
                                              maximizing the log-likelihood of preferences, as              events that do not maximize their expected value. It formal-
                                              current methods do. We call this approach KTO,                izes how humans perceive random variables in a biased but
                                              and it matches or exceeds the performance of                  well-defined manner; for example, relative to some refer-
                                              preference-based methods at scales from 1B to                 ence point, humans are more sensitive to losses than gains,
                                              30B, despite only learning from a binary signal               a property called loss aversion. We show that popular align-
                                              of whether an output is desirable. More broadly,              ment methods such as DPO and PPO-Clip (Schulman et al.,
                                              our work suggests that there is no one HALO that              2017) implicitly model some of these biases, helping ex-
                                              is universally superior; the best loss depends on             plain their success independently of the data used (§3.2).
                                              the inductive biases most appropriate for a given             We then propose a more general class of such loss functions
                                              setting, an oft-overlooked consideration.                     called human-aware losses (HALOs).1

                                        1. Introduction                                                         1We use the term human-aware to draw an analogy with how
                                                                                                            hardware-aware methods benefit from being designed around hard-
                                        Aligning generative models with human feedback has been             ware limitations (Dao et al., 2022), not to claim that a simple loss
                                        successfully used to make generations more helpful, factual,        function is fully aware of human behavior.
                                        and ethical, among other desiderata (Ouyang et al., 2022;
                                        Tian et al., 2023). For LLMs, alignment methods such as
                                        RLHF and DPO have consistently proven to be more benefi-
                                        cial than doing supervised finetuning (SFT) alone. However,
                                        human feedback is often discussed only in the context of
                                        preferences (e.g., output yw  yl for input x), even though
                                        it can take many forms (e.g., approval/disapproval of y given

                                            1Stanford University (first author was an intern at Contex-
                                        tual AI) 2Contextual AI. Correspondence to: Kawin Ethayarajh
                                        <kawin@stanford.edu>.

                                        Proceedings of the 41 st International Conference on Machine
                                        Learning, Vienna, Austria. PMLR 235, 2024. Copyright 2024 by
                                        the author(s).

                                                                                                         1
Model Alignment as Prospect Theoretic Optimization

Although it is impossible to say that HALOs are categori-            2. Background
cally better than non-HALOs, we find that among existing
methods, those that meet the definition of a HALO work               For a detailed survey, please see Appendix A. In brief, LLMs
better than those that do not (§3.3). We find that DPO per-          are traditionally trained in three stages (Ouyang et al., 2022):
formance can even be matched at most scales by running an
offline PPO variant on dummy +1/-1 rewards, suggesting               Pretraining Given a large corpus, train the model to max-
that preference data might not be needed if the inductive            imize the log-likelihood of the next token conditioned on
bias in the loss function is good enough. However, despite           the preceding text. Let 0 denote the pretrained model.
the surprising success of this simple baseline, it significantly
lags behind DPO at the 30B LLM scale and suffers from                Supervised Finetuning (SFT) Finetune the model to pre-
hyperparameter sensitivity, making it difficult to use.              dict the next token on data that is more relevant to the down-
                                                                     stream task. Often, such data will comprise instructions and
Taking a more principled approach, we derive a HALO us-              an appropriate response (i.e., instruction finetuning). Let
ing the model of human utility that Kahneman & Tversky               ref denote the finetuned model.
proposed to describe how humans make decisions about un-
certain monetary outcomes (Tversky & Kahneman, 1992).                RLHF Given a dataset D of preferences (x, yw, yl)--
This approach, which we call Kahneman-Tversky Optimiza-              where x is an input, yw, yl are the preferred and dispreferred
tion (KTO), directly maximizes the utility of generations            outputs (i.e., yw  yl for x), and r is the "true" reward
instead of maximizing the log-likelihood of preferences, as          function underlying the preferences--it is first assumed that
most current methods do (§4.1). KTO only requires a binary           the probability that yw is preferred to yl can be captured with
signal of whether an output is desirable or undesirable for          a specific function class, typically a Bradley-Terry model
an input. This data is more abundant, cheaper, and faster to         (Bradley & Terry, 1952). Where  is the logistic function:
collect in the real world, making it easier to scale alignment
in production and rapidly iterate on models. We find that:                    p(yw  yl|x) = (r(x, yw) - r(x, yl)) (1)

   · KTO matches or exceeds DPO performance at scales                Since getting the true reward from a human would be in-
      from 1B to 30B parameters (§4.3).2 That is, breaking           tractably expensive, a reward model r learns to serve as a
      up n preferences meant for DPO into 2n examples for            proxy, done by minimizing the negative log-likelihood of
      KTO can yield better LLM generations, as determined            the human preference data:
      by closed-ended tasks such as mathematical reasoning
      and open-ended judgments from humans and GPT-4.                  LR(r) = Ex,yw,ylD[- log (r(x, yw) - r(x, yl))]

   · KTO can handle extreme data imbalances, matching                But solely maximizing the reward might come at the ex-
      DPO performance while using up to 90% fewer desir-             pense of desiderata such as generating grammatical text. To
      able examples (i.e., examples of good generations). Its        avoid this, a KL divergence penalty is introduced to restrict
      success cannot be ascribed to the alignment data being         how far the language model can drift from ref. Where  is
      sourced from a preference dataset.                             the model we are optimizing, the optimal model  is that
                                                                     which maximizes
   · When the pretrained model is sufficiently good, one
      can skip supervised finetuning (SFT) and go straight              ExD,y [r(x, y)] - DKL((y|x)ref(y|x)) (2)
      to KTO without a loss in generation quality, whereas
      SFT is always needed for best results with DPO.                where  > 0 is a hyperparameter. Since this objective is
                                                                     not differentiable, we need to use an RL algorithm like PPO
The intent behind KTO was that even if the model learns              (Schulman et al., 2017).
from a weaker signal, we could compensate with the higher
volume of data that could be accessed in practice; the fact          However, RLHF is often slow (largely because of having
that KTO can match and even outperform DPO on the same               to sample generations) and quite unstable in practice (espe-
data is thus surprising. We conclude by discussing some              cially in a distributed setting). For this reason, recent work
theoretical explanations for this phenomenon (§4.4). De-             has focused on designing closed-form losses that maximize
spite the success of KTO in our experiments, our work                the margin between the preferred and dispreferred genera-
ultimately suggests that there is no one HALO that is uni-           tions. In particular, Direct Preference Optimization (DPO)
versally superior; the best HALO depends on the inductive            (Rafailov et al., 2023) has emerged as a popular alterna-
biases appropriate for a given setting, and this choice should       tive as it allows the same optimal policy as in RLHF to be
be made deliberately instead of defaulting to any one loss.          recovered under certain conditions:

    2Our code is available on Github; models are on Huggingface.     LDPO(, ref) = Ex,yw,ylD

                                                                     - log   log (yw|x) -  log (yl|x)    (3)

                                                                     ref (yw |x)              ref(yl|x)

                                                                  2
                      Model Alignment as Prospect Theoretic Optimization

3. A Prospect Theoretic View of Alignment                            is used to get the relative gain or loss; concavity in relative
                                                                     gains (i.e., diminishing sensitivity away from z0); and loss
To understand why alignment methods work so well, we                 aversion (i.e., greater sensitivity to losses).
now frame them through the lens of prospect theory (Tver-
sky & Kahneman, 1992). Prospect theory explains why,                 3.2. HALOs
when faced with an uncertain event, humans make decisions
that do not maximize their expected value. For example,              Definition 3.4 (HALOs). Let  denote the trainable param-
because humans are loss-averse, given a gamble that returns
$100 with 80% probability and $60 with 20% probability, a            eters of the model  : X  P(Y) being aligned, ref the
person might accept $60 to avoid the gamble, despite their
certainty equivalent of $60 being less than the expected             reference model, l : Y  R+ a normalizing factor, and
value of $80.
                                                                     r(x, y) = l(y) log[(y|x)/ref(y|x)] the implied reward.
                                                                     Where Q(Y |x) is a reference point distribution over Y and

                                                                     v : R  R is non-decreasing everywhere and concave in

                                                                     (0, ), the human value of (x, y) is

3.1. Prospect Theory                                                             v(r(x, y) - EQ[r(x, y)])         (5)

In prospect theory, human utility depends on a value func-           A function f is a human-aware loss for v if  ax,y 
tion and a weighting function:3                                      {-1, +1} such that:

Definition 3.1. A value function v : Z  R maps an out-               f (, ref) =                                  (6)
come z, relative to some reference point z0, to its perceived          Ex,yD[ax,yv(r(x, y) - EQ[r(x, y)])] + CD
(or subjective) value. For example, these functions capture
the fact that humans tend to be more sensitive to relative           where D is the feedback data and CD  R is a data-specific
losses than relative gains of the same magnitude.                    constant.

Definition 3.2. A weighting function  is the derivative of           In a classic prospect theory experiment, r would be the dol-
a capacity function that maps cumulative probabilities to            lar amount assigned to each outcome; here, r is measured
perceived cumulative probabilities. These functions capture,         in nats, as the decrease in conditional surprisal when going
for example, the fact that humans tend to overestimate the           from ref to , normalized according to l. This follows
chance of rare events. Let z denote the weight placed on             naturally from the next-token prediction objective used to
outcome z.                                                           pretrain and finetune LLMs. As  is aligned, we would
                                                                     expect r to grow increasingly positive for desirable outputs
Definition 3.3. The utility of a random variable Z is a              and increasingly negative for undesirable outputs.
function of its outcomes: u(Z)  zZ zv(z - z0).

However, because humans do not see the full probability              Another perspective on the reward comes from the RLHF
distribution of an LLM, weighting functions are not salient          objective in (2). The policy that maximizes this objective
to this discussion; we will focus only on value functions. Us-       has a closed-form expression (Peng et al., 2019; Peters &
ing experiments that presented real humans with monetary             Schaal, 2007); where Z(x) is the partition function:
gambles and asked for their certainty equivalent, Tversky &
Kahneman (1992) proposed the following functional form               (y|x)       =    1                1 r(x, y)
for human value:                                                                    Z(x) ref(y|x) exp  

                      (z - z0)   if z  z0                            Letting l(·) = , we get
                      -(z0 - z)  if z < z0
v(z; , , z0) =                              (4)                      r (x, y) = r(x, y) -  log Z(x)

                                                                                                                  (7)

where the median value of hyperparameter  = 0.88 and                 Under , the HALO-defined reward is just the optimal
 = 2.25 across individuals.  controls the curvature of the
function, which reflects risk aversion;  controls its steep-         reward shifted by an input-specific term, meaning that r
ness, which reflects loss aversion. While the shape of the           is in the same equivalence class as r and would also induce
median Kahneman-Tversky value function is illustrated in             the optimal policy  (Lemma 1, Rafailov et al. (2023)).
Figure 1, it should be noted that it varies across individuals
(Tversky & Kahneman, 1992). There are also other func-               The reference point in a HALO is the expected reward from
tional forms for the value function that have been proposed          the human's perspective, where Q(Y |x) describes the ex-
in later work (Gurevich et al., 2009). The salient qualities of      amples that are used to construct a baseline. We require
a value function are: the existence of a reference point that        that the value function be concave in gains but not neces-
                                                                     sarily convex in losses--unlike the canonical Kahneman-
    3Cumulative prospect theory is the full name of the expanded     Tversky value function--because a minority of individuals
theory we dicuss here (Tversky & Kahneman, 1992).                    are risk-averse in both the gain and loss regime, resulting in
                                                                     concavity everywhere (Kahneman & Tversky, 1979).

                                                                  3
                         Model Alignment as Prospect Theoretic Optimization

Figure 2. HALOs (DPO, offline PPO variant) outperform non-HALOs (SLiC, CSFT), as measured by the GPT-4-0613-judged winrate
of the aligned model's generations against a hard-to-beat baseline: the outputs that would have been used as the targets for SFT. The y-axis
here plots the winrate above chance (i.e., the winrate ­ 50%). The difference between methods is only significant (p < 0.05) at 13B+
parameters, and only the HALO-aligned Llama-{13B, 30B} models are able to match the baseline and yield a winrate at or above chance.

Theorem 3.5. DPO and PPO-Clip are human-aware losses.               "clipping" how far  can drift from the version old at
                                                                    the previous step:
The proof is deferred to Appendix B. In Figure 1, we can
see this more intuitively by plotting the value function (i.e.,     LPPO (offline) = - Ex,y,tD[min(qA(x:y<t, yt),
the implied human utility).                                                         clip(q, 1 - , 1 + )A(x:y<t, yt))]

3.3. Does being a HALO matter?                                      where q  =   (yt|x:y<t)       and A(x:y<t, yt) is the per-
                                                                                old (yt |x:y<t )
It is difficult to answer whether being a HALO is useful,
since both HALOs and non-HALOs are diverse function                 token advantage (i.e., the surplus benefit from produc-
classes, but we attempt to do so by comparing popular meth-
ods that qualify as a HALO with those that do not:                  ing a given token in a given state).

1. CSFT: Conditional SFT is a simple alignment method               PPO is an online algorithm--generations are sampled
   where a control token is prepended to the output during          from the current model, judged by a reward model, and
   training; then, at inference, the control token corre-           then used to update the current version. However, for
   sponding to desirable generations is appended to the             a fair comparison with offline methods, we consider a
   input to induce good generations (Korbak et al., 2023).          variant of PPO that is only fed offline data. Because
   This is not a HALO, since that would demand that                 RLHF is also quite unstable in a distributed setting, we
   - log ref(y|x) always equal the reference point; how-            never update old and keep it as ref, instead clipping
   ever, since the reference point is fixed for a given x, this     less conservatively than we traditionally would. Ba-
   is not possible when ref is a non-uniform distribution.          heti et al. (2023) found that these changes, along with
                                                                    treating the entire output sequence as a single action,
2. SLiC: Sequence Likelihood Calibration (Zhao et al.,              greatly improves stability. However, since RLHF tradi-
   2023), which combines a max-margin loss for prefer-              tionally uses token-level advantages, we omit the third
   ences with a language modeling loss, is not a HALO               change and only preserve the first two. The PPO-Clip
   (for the same reasons given above for CSFT):                     loss is unchanged and remains a HALO (Theorem 3.5).

Lcal() = Ex,yw,ylD  max  0,  - log (yw|x)                           Calling this method PPO is somewhat imprecise, be-
                                       (yl |x)                      cause it is offline and takes only one step, but to avoid
                                                                    introducing too many new terms, we will call this PPO
Lreg(, ref) = ExD,yref(x)[- log (y|x)]                              (offline). Instead of using learned rewards, we simplify
LSLiC(, ref) = Lcal() + regLreg(, ref)                              even further and use dummy +1/-1 rewards4 for yw and
                                                                    yl instead. Further details on the implementation of
                                                                    this method can be found in Appendix C.

3. DPO: DPO is a HALO (Theorem 3.5).                                Experimental Setup We compare these baselines on two
                                                                    model families, Pythia-{1.4B, 2.8B, 6.9B, 12B} (Bider-
4. PPO (offline): The standard RLHF objective in (2) is
   typically optimized with PPO-Clip, which works by                    4Note that +1/-1 do not refer to the HALO-implied rewards, but
                                                                    rather the rewards used for the advantage A(x:y<t, yt) calculation.

                                                                 4
Model Alignment as Prospect Theoretic Optimization

man et al., 2023) and Llama-{7B, 13B, 30B} (Touvron                  binary signal of good/bad generations may be sufficient
et al., 2023). This permits us to see how LLM alignment              to reach DPO-level performance, even if the offline PPO
scales within a model family (Llama-2 lacks a 30B model,             approach itself was unable to do so past a certain scale
hence our use of Llama). Later experiments (§4.3) are done           (§3.3). Taking a more principled approach, we now derive a
on Mistral-7B derivatives (Jiang et al., 2023) and Llama-3           HALO using the Kahneman-Tversky model of human value,
(Dubey et al., 2024). The models are trained on a combina-           which allows us to directly optimize for utility instead of
tion of Anthropic-HH (Ganguli et al., 2022), OpenAssistant           maximizing the log-likelihood of preferences.
(Ko¨pf et al., 2023), and SHP (Ethayarajh et al., 2022).
                                                                     4.1. Derivation
All models are aligned under identical settings on the same
data, save for hyperparameters unique to them. Similar to            The canonical Kahneman-Tversky value function (4) suffers
Rafailov et al. (2023), the target sequences for SFT are a           from numerical instability during optimization due to the
subset of {yw}. We use GPT-4-0613 to judge whether                   exponent a, so we replace it with the logistic function ,
the aligned model's response is better than the SFT target           which is also concave in gains and convex in losses.
for a given test input with respect to helpfulness, harmless-
ness, and conciseness, a now standard practice (Zheng et al.,        To control the degree of risk aversion, we introduce a hy-
2023; Li et al., 2023).5 Note that while the SFT target is
considered a desirable output for x, it is by no means the           perparameter   R+ as part of the value function. The
best output, meaning that an aligned model can certainly
achieve a winrate above 50%, although this is difficult given        greater  is, the more quickly the value saturates, meaning
that many of the target sequences are human-written.
                                                                     the human is simultaneously more risk-averse in gains and
In Figure 2, we see the results of this analysis:
                                                                     more risk-seeking in losses. In practice, this has a similar
   · HALOs either match or outperform non-HALOs                      effect as  in the DPO loss, which controls how far  drifts
      at every scale, though the gap is only significant (p <        from ref, though we introduce it here explicitly to control
      0.05) at 13B+ model sizes after correcting for multiple        risk aversion; in DPO, it carries over from the KL constraint
      comparisons (Holm, 1979). In fact, only the HALO-
      aligned Llama-{13B, 30B} models match or exceed a              in the RLHF objective (2) and is part of the reward.
      win rate of 50% (i.e., are able to match or exceed the
      generation quality of the SFT targets in the test data).       We replace the loss aversion coefficient  in the origi-
                                                                     nal Kahneman-Tversky value function (4) with {D, U },
   · Up to a scale of 7B parameters, alignment provides              where D and U are hyperparameters for desirable and
      virtually no gains over SFT alone. However, it is              undesirable outputs respectively; more complex schemes
      worth noting that if the base models were more perfor-         could also be used for importance sampling.
      mant, or if the SFT data distribution were less similar to
      the preference data, then the gains from the alignment         Rather than having just one dispreferred generation serve as
      stage would ostensibly be greater.
                                                                     the reference point z0, as in DPO, we assume that humans
   · Despite only using dummy +1/-1 rewards, our of-                 judge the quality of y|x in relation to all possible outputs.
      fline PPO variant performs as well as DPO for all              This implies that Q(Y |x) is the policy and that the refer-
      models except Llama-30B. This challenges conven-               ence point is the KL divergence KL((y|x)ref(y|x)).
      tional wisdom, which places heavy emphasis on reward           However, as we discuss further below, we end up taking a
      learning (Casper et al., 2023), and suggests that even
      the simplest rewards can prove useful when used in a           biased estimate of this term in practice.
      loss function that has the right inductive bias. Despite
      its success, our offline PPO baseline still suffers from       Where y denotes D(U ) when y is desirable(undesirable)
      hyperparameter sensitivity and training instability, al-       respectively, the default KTO loss is:6
      beit not to the same extent as traditional RLHF.
                                                                                LKTO(, ref) = Ex,yD[y - v(x, y)]             (8)
4. Kahneman-Tversky Optimization
                                                                     where
The surprising success of offline PPO with dummy +1/-1
rewards suggests that--with the right inductive biases--a            r (x,  y)  =  log   (y|x)
                                                                                        ref(y|x)
    5We validate that GPT-4 judgments concur with human judg-
ments in Appendix D.                                                        z0 = KL((y|x)ref(y|x))

                                                                     v(x, y) = D((r(x, y) - z0)) if y  ydesirable|x
                                                                                    U ((z0 - r(x, y))) if y  yundesirable|x

                                                                         6An earlier draft separated out D, U into a function w. For
                                                                     an easier comparison with (4), they--along with --have been
                                                                     moved into the value function itself. Note that y exists solely

                                                                     to make the loss non-negative and can be removed. The use of
                                                                     multiple x to estimate z0 in practice was included in the loss

                                                                     definition originally, but has now been moved outside for clarity.

                                                                  5
                                   Model Alignment as Prospect Theoretic Optimization

Table 1. Recommended hyperparameter settings for different losses and models when aligned on UltraFeedback, evaluated on the

benchmarks discussed in §4.3. The hyperparameter sweeps were done with AdamW, an effective batch size of 32, and D = U = 1.
Depending on your task and ratio of desirable:undesirable examples, the optimal choice of D, U might be significantly different.

Model                      Method       LR                        AlpacaEval (LC)      BBH    GSM8K (8-shot) 

Llama-3 8B                 SFT+KTO      5e-6           0.05       10.59                65.15  60.20
                                                                  11.25                65.26  57.92
Llama-3 8B                 KTO          5e-6           0.10       13.01                32.39  61.11
                                                                  16.63                20.41  60.35
Qwen2.5 3B Instruct        SFT+KTO      5e-6           0.10

Qwen2.5 3B Instruct        KTO          5e-6           0.50

For more stable training, we do not backpropagate through         regardless of whether it is preceded by xi, z^0 might actually
z0; it exists purely to control the loss saturation.              be an under-estimate. In such cases, one can avoid the extra
                                                                  computation and set z^0 = 0. However, when KTO is not
Intuitively, KTO works as follows: if the model increases         preceded by SFT, or when the SFT data is not a subset of
the reward of a desirable example in a blunt manner, then the     the KTO data, estimating z^0 is necessary.
KL penalty also rises and no progress is made. This forces
the model to learn exactly what makes an output desirable,        Data If the alignment data is naturally binary, every posi-
so that the reward can be increased while keeping the KL          tive example can be assumed to be drawn from ydesirable|x
term flat (or even decreasing it). The argument works in the      and every negative example from yundesirable|x. However, the
other direction as well, though the non-negativity of the KL      canonical feedback datasets in academic research (HH, SHP,
term allows faster saturation in the loss regime.                 OASST) are in preference format, since the methods that
                                                                  have worked best up until now are preference-based. In our
KL Estimate In practice, estimating z0 as it is defined           experiments, we convert preference data yw  yl by assum-
above is impractical because sampling from  is slow. In-          ing that yw is drawn from the desirable distribution and yl
stead, we take a biased but convenient estimate by shifting       from the undesirable one. This is a naive assumption, made
                                                                  for the sake of simplicity, and a more complex deconstruc-
outputs in the same microbatch to induce mismatched pairs         tion of preferences into binary feedback would likely yield
{(x1, y2), (x2, y3), ..., (xm, y0)}, then estimating a shared     better results, which we leave for future work. To show that
reference point z0 for all examples in the same microbatch        KTO can be used with non-preference data, we also sub-
as follows. Where j = (i + 1) mod m,                              sample exactly one y per x for some experiments (denoted
                                                                  one-y-per-x), removing any trace of paired preferences at
                                                                  the cost of reducing the data volume.

       z^0  =  max  0,  1          log  (yj |xi)                  If human feedback is in the form of scores or ratings, the
                        m               ref(yj |xi)               simplest means of incorporating it into KTO is to con-
                           1i<m                                   struct a weighting function such that high-magnitude data is
                                                                  weighed more and that examples with scores above(below)
Because of clamping, our estimator has a positive bias but        some threshold are desirable(undesirable). It is also possible
lower variance than the standard unbiased estimator. Al-          to construct score-based HALOs from first principles, but
though it costs an additional forward pass, we use a mis-         we leave the design of such losses to future work.
matched output yj instead of the corresponding yi because
the latter have often been deliberately chosen to be canoni-      4.2. Hyperparameters
cally good or bad outputs, and thus have unrepresentative
high-magnitude rewards. It is worth noting that although our      In Table 1, we provide recommended hyperparameter set-
estimator is biased, so would the human-perceived reference       tings for Llama-3 8B (Dubey et al., 2024) and Qwen2.5
point, since humans do not perceive the full distribution in-     3B Instruct (Yang et al., 2024) based on benchmarks such
duced by  and would employ an "availability heuristic"            as MMLU (0-shot) (Hendrycks et al., 2021), GSM8K (8-
that would overweight outputs for which they have recently        shot, chain-of-thought) (Cobbe et al., 2021), HumanEval
given feedback, regardless of whether those outputs are a         (0-shot) (Chen et al., 2021), and BigBench-Hard (3-shot
good continuation of x (Tversky & Kahneman, 1973).                chain-of-thought) (Srivastava et al., 2022).

If KTO is preceded by SFT done on the same data that              Learning Rate We find that that the performance of an
is used as desirable feedback and the SFT model is used           aligned model is more sensitive to the learning rate than any
as ref, then the KL estimate will quickly approach zero.          other hyperparameter. The optimal learning rate for KTO is
Having already learned what is desirable during SFT, the
policy will tend to scatter the mass placed on undesirable
examples, leading to minimal divergence. Also, because
the policy may learn to place less mass on undesirable yi

                                                               6
      Model Alignment as Prospect Theoretic Optimization

Figure 3. KTO is as good or better than DPO at all scales, as measured by the GPT-4-0613-judged winrate of the aligned model's
generations against the outputs that would have been used for SFT. In fact, for the Llama models, KTO alone matches the performance of
SFT+DPO and is significantly better than DPO alone. Error bars denote a 90% binomial confidence interval.

usually 2x to 10x the optimal learning rate for DPO; since         This interval was determined empirically, and implies that--
the reference-adjusted reward tends to be much smaller in          after adjusting for class imbalances--gain sensitivity yields
magnitude for KTO, one needs to use a more aggressive              better performance than loss sensitivity, ostensibly because
learning rate to compensate. For example, the default learn-       producing good outputs is more important than avoiding
ing rate for DPO is 5e-7 (Rafailov et al., 2023), but we find      bad outputs for success on most benchmarks. This is not a
that a default of 5e-6 works better for KTO. In our experi-        hard rule, however. In tasks where minimizing the downside
ments, we use the default DPO learning rate with RMSProp           is more important, like toxicity prevention, setting D, U
for all methods to ensure an apples-to-apples comparison           such that DnD < U nU may work better. Unless other-
with Rafailov et al. (2023), but when using KTO in practice,       wise stated, we use D = U = 1 in our experiments.
we recommend starting at 5e-6 with AdamW and adjusting
the learning rate as needed.                                       The current configuration permits different sensitivities to
                                                                   desirable and undesirable examples based on the premise
Batch Size KTO needs a microbatch size  2 to estimate              that upon convergence, all undesirable outputs will yield
the reference point in a single step. The experiments in this      negative rewards and all desirable outputs will yield positive
paper all use an effective batch size of 32, and in general we     ones, in which case U , D would directly correspond to 
recommend using a batch size between 8 and 128.                    in the original Kahneman-Tversky value function. However,
                                                                   during training itself, a desirable output may have a negative
Risk Aversion The degree of risk aversion/seeking is con-          reward (and vice-versa), yet D would be the same regard-
trolled by ; the greater  is, the greater the risk aversion        less of whether r(x, y) - z0 were positive or negative. We
in gains and risk seeking in losses. In practice, lower val-       may want to change this so that not only is asymmetry pos-
ues of  in the range [0.01, 0.10] work better for larger           sible upon convergence, but even during training within the
models that have already undergone SFT; higher values of           same class of output: in this case, using a higher D when
 in the range [0.10, 1.00] work better for smaller models          r(x, y) - z0 is negative than when it is positive. We leave
undergoing KTO directly, without SFT prior.                        the design of dynamic hyperparameter selection schemes as
                                                                   directions for future work.

Loss Aversion The default weighting function controls              4.3. Experiments
the degree of loss aversion with D, U , which are both set
to 1 by default. In general, where nD and nU refer to the          KTO  DPO As seen in Figure 3, when rerunning the
number of desirable and undesirable examples respectively,         winrate evaluation in §3.3, SFT+KTO is competitive with
we find that it is generally best to set D, U such that            SFT+DPO at scales from 1B to 30B, despite the model
                                                                   learning from a weaker signal. KTO alone is better than
DnD      3  (9)                                                    DPO alone for the Llama-{7B, 13B, 30B} models, and this
U nU  1,                                                           gap is significant (p < 0.01) at 7B and 30B even after cor-
      2                                                            recting for multiple comparisons (Holm, 1979). Among the
                                                                   Pythia models, there is no significant difference between the
For example, if there were a 1:10 ratio of desirable to un-
desirable examples, we would set U = 1, D  [10, 15].

                                                                7
                             Model Alignment as Prospect Theoretic Optimization

Table 2. (top) Results from aligning Zephyr--SFT (Tunstall et al.,
2023) on UltraFeedback for exactly 1 epoch. Even when only
one of the two outputs in each preference is seen by KTO, it still
outperforms DPO, despite this reducing the volume of data by half
(one-y-per-x). (middle) Changing the structure of the KTO loss,
even in subtle ways, makes the aligned model worse, supporting
our design choices. (bottom) Fixing U = 1, we try different
levels of loss and risk aversion by changing D and  respectively
(see Appendix C for more results).

Dataset ()           MMLU GSM8k HumanEval BBH
Metric ()
                     EM      EM    pass@1    EM

SFT                  57.2 39.0 30.1          46.3                      Figure 4. Without doing SFT first, DPO-aligned models tend to
                                                                       ramble and hallucinate entire conversations. KTO does not suffer
DPO                  58.2 40.0 30.1          44.1                      from this issue.

ORPO ( = 0.1)        57.1 36.5 29.5          47.5                      the criteria in (9); when we drop 90% of the desirable data,
                                                                       we set u = 1, D = 13.33 for example. For Llama-7B,
KTO ( = 0.1, D = 1)  58.6    53.5  30.9      52.6                      we find that up to 90% of the desirable data can in fact be
                                                                       discarded while still outperforming DPO (Figure 5).
KTO (one-y-per-x)    58.0 50.0 30.7          49.9
                                                                       We further verify this claim by aligning Mistral-7B on Ope-
KTO (no z0)          58.5 49.5 30.7          49.0                      nAssistant using DPO (on n pairs), standard KTO (on all 2n
                                                                       outputs), and KTO where only one y per x is used. Since the
KTO (concave, v = log ) 58.3 42.5 30.6       43.2                      output of one y in OpenAssistant is not conditioned on the
                                                                       other y for the same x, the latter captures the setting where
KTO (risk-neutral, v(·) = ·) 57.3 42.0 28.8  6.1                       the data is from an inherently unpaired distribution. Despite
                                                                       the one-y-per-x setup decreasing the amount of training data
KTO (no ref, D = 1.75) 57.5  47.5  29.5      51.6                      by 72%, the KTO-aligned model still outperforms both its
                                                                       DPO counterpart and the official instruction-tuned Mistral-
KTO ( = 0.01)        57.3 54.0 33.2          49.8                      7B (Jiang et al., 2023), as seen in Table 3.
KTO ( = 0.50)
KTO (D = 0.50)       58.3 45.0 32.7          51.8                      Changing the design of KTO makes it significantly worse.
KTO (D = 0.75)                                                         For one, removing the reference point z0--which is neces-
KTO (D = 1.33)       57.4 47.0 32.2          30.5                      sary for KTO to qualify as a HALO--causes a 3.6 and 4.0
KTO (D = 2.00)                                                         point drop on BBH and GSM8K respectively (Table 2, mid-
                     58.3 52.0 30.3          50.6                      dle). Even changes that allow KTO to remain a HALO are
                                                                       typically suboptimal. For example, removing the symmetry
                     58.5 52.0 32.8          45.4                      of the value function--going from 1-(·) to - log (·) (i.e.,
                                                                       making the value function concave everywhere, as in DPO)--
                     58.1 43.0 32.1          45.3                      causes a 9.4 and 11.0 point drop on BBH and GSM8K re-
                                                                       spectively. Making the value function risk-neutral by setting
two, suggesting that a minimum model capacity is needed                it to the identity function leads to a total collapse in BBH
for these differences to emerge. KTO also fares better than            performance. Changing the curvature and slope via the risk
DPO and other baselines on generative benchmarks (Table                and loss aversion hyperparameters can, depending on the
2). This is most pronounced for certain tasks: on GSM8K,               task, improve or degrade performance (Table 2, bottom).
a mathematical reasoning dataset, just swapping DPO for
KTO when aligning Zephyr--SFT (Tunstall et al., 2023)                  KTO works without a reference model or SFT, but not as
on UltraFeedback (Cui et al., 2023) improves performance               well as standard KTO. If we can avoid storing the refer-
by 13.5 points.                                                        ence model in memory, then we can do much more memory-
                                                                       efficient alignment. The naive way to do this is to assume
At sufficient scale, KTO does not need SFT. A KTO-                     that ref returns a uniform distribution over outputs for all
aligned Llama-{13B, 30B} model is competitive with its                 x, which simplifies r - z0 to log (y|x) - H((y|x)),
SFT+KTO counterpart despite not undergoing SFT first,                  where H denotes the entropy. As seen in Table 2 (mid-
and is the only alignment method of the ones we tested                 dle), if we set D = 1.75, this memory-efficient variant of
to show this behavior. This is perhaps due to KTO alone                KTO is better than DPO on some tasks and worse on others,
keeping the average response length roughly the same, while
running DPO without SFT prior causes the response length
to increase dramatically (Figure 4).

KTO data need not come from preferences. Might KTO
be secretly benefiting from its 2n examples in the previous
experiment coming from n preference pairs instead of a nat-
urally unpaired data distribution? To test this, we randomly
discard increasingly large fractions of the desirable data be-
fore KTO-aligning a Llama-7B model. For example, if we
discard 90% of the desirable data while leaving the undesir-
able data untouched, then the ratio of desirable:undesirable
examples goes from 1:1 to 1:10 and the vast majority of un-
desirable examples no longer have a preferred counterpart.
We handle such imbalances by changing D, U to satisfy

                                                                    8
Model Alignment as Prospect Theoretic Optimization

                                                                    Table 3. In aligning Mistral-7B on the OpenAssistant dataset, we
                                                                    find that using KTO with only one output per input still outper-
                                                                    forms DPO, despite this restriction reducing the amount of training
                                                                    data by 72%. A 90% binomial confidence interval is given.

                                                                    Method                          Winrate vs. SFT Target

                                                                    Mistral-7B (unaligned)          0.525 ± 0.037
                                                                    Mistral-7B + DPO                0.600 ± 0.037
                                                                    Mistral-7B + KTO (all y per x)  0.652 ± 0.036
                                                                    Mistral-7B + KTO (one y per x)  0.631 ± 0.036
                                                                    Mistral-7B-Instruct             0.621 ± 0.031

Figure 5. A KTO-aligned Llama-7B model can match or exceed          noring data that is hard-to-learn but necessary to recover r,
the performance of its DPO-aligned counterpart while aligned        potentially resulting in underfitting to complex distributions.
on a smaller and highly imbalanced version of the same dataset,     Such underfitting may be mitigated by aligning the model
even with as few as 0.1 positive/desirable examples for every       with lower  and for more epochs.
negative/undesirable one.
                                                                    Theorem 4.2. Assuming the value function is logistic, for a
though it still trails standard KTO. We find that it is also        reward function ra that maximizes (2), there exists a reward
more sensitive to the loss aversion hyperparameters; setting        function in its equivalence class (i.e., rb(x, y) = ra(x, y) +
D  {1.5, 2.0} reduces performance on GSM8K and BBH                  h(x) for some h(x)) that induces the same optimal policy
by several points. Still, it strictly outperforms ORPO (Hong         and the same Bradley-Terry preference distribution but
et al., 2024), a recently-proposed reference-free method,           a different human value distribution.
while also using less memory than all existing approaches,
since ref need never be loaded into memory and a batch of           A key insight from Rafailov et al. (2023) is that reward
m KTO outputs is smaller than a batch of m preferences              functions in the same equivalence class (i.e., differing only
(i.e., 2m outputs) used for ORPO/DPO.                               in an input-specific component) induce the same optimal
                                                                    policy under (2) and the same Bradley-Terry preference
4.4. Theoretical Analysis                                           distribution. However, we show under mild assumptions
                                                                    that the value distribution--i.e., human utility--is affected
KTO was designed with the motivation that even if binary            by such input-specific changes, so maximizing preference
feedback were weaker, one could compensate with sheer               likelihood does not mean one is maximizing human utility.
volume, as such data is much more abundant, cheaper, and            This helps explain why the margin between KTO and DPO
faster to collect than preferences. So why does KTO perform         is even bigger in human evaluations than it is in automated
as well or better than DPO on the same preference data (that        LLM-as-a-judge evaluations (Appendix D).
has been broken up)? Greater data efficiency helps, but it is
not the only answer, given that even after adjusting for this       Theorem 4.3. For input x with outputs {ya, yb}, let dataset
factor in the one-y-per-x setup, KTO still outperforms.             D comprise contradictory preferences ya  yb and yb  ya
                                                                    in proportion p  (0.5, 1) and (1 - p)  (0, 0.5) respec-
In this section, we will discuss two theoretical explanations       tively. If p1/ref(ya|x) < (1 - p)1/ref(yb|x), then the
for this phenomenon: (1) preference likelihood can be max-          optimal DPO policy is more likely to produce the minority-
imized without necessarily maximizing underlying human              preferred yb; the optimal KTO policy will strictly produce
utility; (2) KTO implicitly ignores noisy and intransitive          the majority-preferred ya for a loss-neutral value function
data, which is common in real-world feedback.                       (D = U ).

Proposition 4.1. As the reward implied by the current policy        Informally, say there are two contradictory preferences over
tends to ±, the KTO update of  tends to zero.                       the output for x, with the majority p preferring ya and the
                                                                    minority 1 - p preferring yb. In the worst-case, when p
This means that if (x, y) is implied by the current policy          is sufficiently low and the reference model is sufficiently
 to be too difficult or too easy to learn from, then it is          unaligned, the optimal DPO policy is more likely to produce
effectively ignored. In some cases, this may be a blessing in       the minority-preferred output yb even though the implied
disguise: since real-world feedback is very noisy (Hoeffler         reward r(x, yb) > r(x, ya). In contrast, the optimal KTO
& Ariely, 1999), the reason a desirable example has a highly        policy will deterministically produce the majority-preferred
negative implied reward may be because it is mislabelled.           ya if the value function is loss-neutral (D = U ), which
By avoiding this hard-to-learn data, KTO avoids fitting to          is the default setting. This suggests that KTO has better
noise. However, this also means that KTO could end up ig-           worst-case outcomes when handling feedback intransitivity.

                                                                 9
Model Alignment as Prospect Theoretic Optimization

4.5. KTO vs. DPO ­ when to use which?                                  suggesting that they work in part because they reflect human
                                                                       biases in decision-making. We abstracted these insights into
When human feedback is in a binary format, and especially              a family of alignment objectives called human-aware losses
when there is an imbalance between the number of desirable             (HALOs). We then proposed a HALO called Kahneman-
and undesirable examples, KTO is the natural choice. When              Tversky Optimization (KTO) for directly maximizing the
your data is in the form of preferences, the choice is less            utility of generations instead of maximizing the likelihood of
clear. Putting aside the greater data efficiency of KTO, our           preferences, as existing methods do. Despite only learning
theoretical analysis suggests that if your preference data has         from a binary signal of whether an output is (un)desirable,
sufficiently little noise and sufficiently little intransitivity,      KTO was as good or better than preference-based methods
then DPO will work better, since there is some risk of KTO             in our experiments. More broadly, our work suggests that
underfitting; this risk can be mitigated by using a lower              akin to how there is no one reward model that is univer-
value of  and aligning with KTO for more epochs. But                   sally superior, there is no one loss function either--the best
if there is enough noise and intransitivity, then the better           HALO depends on the inductive biases that are most ap-
worst-case guarantees of KTO will win out. Most publicly               propriate for a given setting, and much work remains to be
available preference datasets (e.g., SHP, OpenAssistant)               done in identifying the best HALO for each context.
contain noisy feedback from many different humans whose
preferences likely contradict to some extent, which explains           Acknowledgements
why KTO was able to match or exceed DPO performance
in our experiments. Even synthetic feedback can be noisy               We thank Percy Liang, Dilip Arumugam, Arya McCarthy,
and intransitive, which helps explain why KTO outperforms              and Nathan Lambert for feedback. We thank Stas Bekman
DPO when aligning with UltraFeedback.                                  and Gautam Mittal for cluster assistance and Alex Manthey
                                                                       for helping with human evaluation.
5. Future Work
                                                                       Impact Statement
The existence of HALOs raises many questions. For one,
KTO is based on the Kahneman-Tversky value function                    The methods discussed in this paper have the potential to
for monetary gambles, which is almost certainly different              make LLMs more helpful and safer, which is often needed
from how humans perceive the relative goodness of text.                for models deployed in production. It is possible that in
What value functions and reference point distributions best            making models more helpful, we increase the utility of one
describe how humans perceive language, and how do they                 person at the expense of broader society. In aligning models
vary across domains and individuals? How can we identify               with human feedback, one may also--without even fully
the best HALO for each individual and setting instead of               recognizing it--be aligning to an unrepresentative subset of
using one default loss?                                                the population, which may hinder the ability of individuals
                                                                       outside that subset to benefit equally from using the model.
On a more technical level, important directions include
developing HALOs that: (1) incorporate granular feedback,              The data used for LLM alignment, including the datasets
such as a score, especially when optimizing for multiple               used in this paper (e.g., SHP, HH, OASST) contain prefer-
desiderata; (2) work for other modalities (e.g. images) and            ences of groups that are not representative of the broader
model classes (e.g., diffusion models), especially models              population. Biases in this data have the potential to be propa-
that do not produce an explicit distribution over the output           gated downstream when used to align models with methods
space; (3) can resolve contradictions in feedback according            like KTO, especially when no efforts are made to adjust
to different definitions of fairness; (4) are designed to be           for the different population. KTO in particular implicitly
used with online data, where the direction of feedback is              resolves contradictions in feedback by taking the majority-
implied by r or some external reward data.                             preferred outcome for a loss-neutral value function, which
                                                                       does not comport with many theories of fairness (e.g., Rawl-
Ecologically valid evaluation (De Vries et al., 2020), where           sianism). Since user preferences are, in turn, affected by the
the aligned models are deployed in real-world settings, are            models they interact with, this also risks the homogeniza-
also needed to judge the merits of different HALOs.                    tion of preferences and utility functions when KTO-aligned
                                                                       models are deployed at scale. The design of HALOs that
6. Conclusion                                                          resolve contradictions in more diverse ways is an important
                                                                       direction for future work.
Although model alignment has historically been reward-
centric, we found that the inductive biases of alignment               On the other hand, because KTO works with binary feed-
objectives are critical to their success. Moreover, these in-          back, which is more abundant, cheaper, and faster to collect
ductive biases have analogs in the prospect theory literature,         in the real world, it significantly lowers the barrier to data

                                                                   10
Model Alignment as Prospect Theoretic Optimization

collection. This makes it easier to collect feedback from           Christiano, P. F., Leike, J., Brown, T., Martic, M., Legg,
traditionally under-represented groups and serve different             S., and Amodei, D. Deep reinforcement learning from
models to different users, instead of just one monolithic              human preferences. Advances in neural information pro-
model being served to everyone.                                        cessing systems, 30, 2017.

References                                                          Cobbe, K., Kosaraju, V., Bavarian, M., Chen, M., Jun, H.,
                                                                       Kaiser, L., Plappert, M., Tworek, J., Hilton, J., Nakano,
Azar, M. G., Guo, Z. D., Piot, B., Munos, R., Rowland, M.,             R., Hesse, C., and Schulman, J. Training verifiers to solve
  Valko, M., and Calandriello, D. A general theoretical                math word problems. arXiv preprint arXiv:2110.14168,
   paradigm to understand learning from human preferences.             2021.
   In International Conference on Artificial Intelligence and
  Statistics, pp. 4447­4455. PMLR, 2024.                            Cui, G., Yuan, L., Ding, N., Yao, G., Zhu, W., Ni, Y., Xie, G.,
                                                                       Liu, Z., and Sun, M. Ultrafeedback: Boosting language
Baheti, A., Lu, X., Brahman, F., Le Bras, R., Sap, M., and             models with high-quality feedback, 2023.
   Riedl, M. Improving language models with advantage-
   based offline policy gradients. In The Twelfth Interna-          Dao, T., Fu, D., Ermon, S., Rudra, A., and Re´, C. Flashat-
   tional Conference on Learning Representations, 2023.                tention: Fast and memory-efficient exact attention with
                                                                       io-awareness. Advances in Neural Information Process-
Bai, Y., Jones, A., Ndousse, K., Askell, A., Chen, A., Das-            ing Systems, 35:16344­16359, 2022.
   Sarma, N., Drain, D., Fort, S., Ganguli, D., Henighan, T.,
   et al. Training a helpful and harmless assistant with rein-      De Vries, H., Bahdanau, D., and Manning, C. Towards
   forcement learning from human feedback. arXiv preprint              ecologically valid research on language user interfaces.
   arXiv:2204.05862, 2022.                                             arXiv preprint arXiv:2007.14435, 2020.

Biderman, S., Schoelkopf, H., Anthony, Q. G., Bradley,              Dubey, A., Jauhri, A., Pandey, A., Kadian, A., Al-Dahle,
   H., O'Brien, K., Hallahan, E., Khan, M. A., Purohit, S.,           A., Letman, A., Mathur, A., Schelten, A., Yang, A., Fan,
   Prashanth, U. S., Raff, E., et al. Pythia: A suite for ana-        A., et al. The llama 3 herd of models. arXiv preprint
   lyzing large language models across training and scaling.           arXiv:2407.21783, 2024.
   In International Conference on Machine Learning, pp.
   2397­2430. PMLR, 2023.                                           Ethayarajh, K., Choi, Y., and Swayamdipta, S. Under-
                                                                       standing dataset difficulty with V-usable information. In
Bradley, R. A. and Terry, M. E. Rank analysis of incom-                Chaudhuri, K., Jegelka, S., Song, L., Szepesvari, C., Niu,
   plete block designs: I. the method of paired comparisons.           G., and Sabato, S. (eds.), Proceedings of the 39th In-
  Biometrika, 39(3/4):324­345, 1952.                                   ternational Conference on Machine Learning, volume
                                                                      162 of Proceedings of Machine Learning Research, pp.
Busa-Fekete, R., Szo¨re´nyi, B., Weng, P., Cheng, W., and              5988­6008. PMLR, 17­23 Jul 2022.
   Hu¨llermeier, E. Preference-based reinforcement learning:
   evolutionary direct policy search using a preference-based       Ganguli, D., Lovitt, L., Kernion, J., Askell, A., Bai, Y.,
   racing algorithm. Machine learning, 97:327­351, 2014.               Kadavath, S., Mann, B., Perez, E., Schiefer, N., Ndousse,
                                                                       K., et al. Red teaming language models to reduce harms:
Casper, S., Davies, X., Shi, C., Gilbert, T. K., Scheurer, J.,         Methods, scaling behaviors, and lessons learned. arXiv
   Rando, J., Freedman, R., Korbak, T., Lindner, D., Freire,           preprint arXiv:2209.07858, 2022.
   P., et al. Open problems and fundamental limitations of re-
   inforcement learning from human feedback. Transactions           Gurevich, G., Kliger, D., and Levy, O. Decision-making
   on Machine Learning Research, 2023.                                 under uncertainty­a field study of cumulative prospect
                                                                       theory. Journal of Banking & Finance, 33(7):1221­1229,
Chan, L., Critch, A., and Dragan, A. Human irrationality:              2009.
   both bad and good for reward inference. arXiv preprint
   arXiv:2111.06956, 2021.                                          He, X., Liao, L., Zhang, H., Nie, L., Hu, X., and Chua,
                                                                      T.-S. Neural collaborative filtering. In Proceedings of
Chen, M., Tworek, J., Jun, H., Yuan, Q., Pinto, H. P. d. O.,           the 26th international conference on world wide web, pp.
   Kaplan, J., Edwards, H., Burda, Y., Joseph, N., Brockman,          173­182, 2017.
   G., et al. Evaluating large language models trained on
   code. arXiv preprint arXiv:2107.03374, 2021.                     Hendrycks, D., Burns, C., Basart, S., Zou, A., Mazeika, M.,
                                                                       Song, D., and Steinhardt, J. Measuring massive multitask
Chen, Z., Deng, Y., Yuan, H., Ji, K., and Gu, Q. Self-play             language understanding. Proceedings of the International
   fine-tuning converts weak language models to strong lan-           Conference on Learning Representations (ICLR), 2021.
   guage models. arXiv preprint arXiv:2401.01335, 2024.

                                                                11
Model Alignment as Prospect Theoretic Optimization

Hoeffler, S. and Ariely, D. Constructing stable preferences:        Li, X., Zhang, T., Dubois, Y., Taori, R., Gulrajani, I.,
  A look into dimensions of experience and their impact on             Guestrin, C., Liang, P., and Hashimoto, T. B. Alpacae-
   preference stability. Journal of consumer psychology, 8            val: An automatic evaluator of instruction-following
  (2):113­139, 1999.                                                   models. https://github.com/tatsu-lab/
                                                                       alpaca_eval, 2023.
Holm, S. A simple sequentially rejective multiple test pro-
   cedure. Scandinavian journal of statistics, pp. 65­70,           Lu, X., Welleck, S., Hessel, J., Jiang, L., Qin, L., West,
  1979.                                                                P., Ammanabrolu, P., and Choi, Y. Quark: Controllable
                                                                       text generation with reinforced unlearning. Advances in
Hong, J., Lee, N., and Thorne, J. Reference-free monolithic            neural information processing systems, 35:27591­27609,
   preference optimization with odds ratio. arXiv preprint             2022.
   arXiv:2403.07691, 2024.
                                                                    Munos, R., Valko, M., Calandriello, D., Azar, M. G., Row-
Jain, A., Wojcik, B., Joachims, T., and Saxena, A. Learning            land, M., Guo, Z. D., Tang, Y., Geist, M., Mesnard, T.,
   trajectory preferences for manipulators via iterative im-           Michi, A., et al. Nash learning from human feedback.
   provement. Advances in neural information processing                arXiv preprint arXiv:2312.00886, 2023.
   systems, 26, 2013.
                                                                    Ouyang, L., Wu, J., Jiang, X., Almeida, D., Wainwright, C.,
Jiang, A. Q., Sablayrolles, A., Mensch, A., Bamford, C.,               Mishkin, P., Zhang, C., Agarwal, S., Slama, K., Ray, A.,
   Chaplot, D. S., Casas, D. d. l., Bressand, F., Lengyel, G.,         et al. Training language models to follow instructions
   Lample, G., Saulnier, L., et al. Mistral 7b. arXiv preprint        with human feedback. Advances in Neural Information
   arXiv:2310.06825, 2023.                                            Processing Systems, 35:27730­27744, 2022.

Kahneman, D. and Tversky, A. Prospect theory: An analysis           Peng, X. B., Kumar, A., Zhang, G., and Levine, S.
   of decision under risk. Econometrica, 47(2):263­292,               Advantage-weighted regression: Simple and scalable
  1979.                                                                off-policy reinforcement learning. arXiv preprint
                                                                       arXiv:1910.00177, 2019.
Ko¨pf, A., Kilcher, Y., von Ru¨tte, D., Anagnostidis, S.,
  Tam, Z.-R., Stevens, K., Barhoum, A., Duc, N. M., Stan-           Peters, J. and Schaal, S. Reinforcement learning by reward-
   ley, O., Nagyfi, R., et al. Openassistant conversations­           weighted regression for operational space control. In
   democratizing large language model alignment. arXiv                Proceedings of the 24th international conference on Ma-
   preprint arXiv:2304.07327, 2023.                                    chine learning, pp. 745­750, 2007.

Korbak, T., Shi, K., Chen, A., Bhalerao, R. V., Buckley,            Rafailov, R., Sharma, A., Mitchell, E., Manning, C. D.,
   C., Phang, J., Bowman, S. R., and Perez, E. Pretrain-               Ermon, S., and Finn, C. Direct preference optimization:
   ing language models with human preferences. In Inter-              Your language model is secretly a reward model. In Thirty-
   national Conference on Machine Learning, pp. 17506­                 seventh Conference on Neural Information Processing
  17533. PMLR, 2023.                                                  Systems, 2023.

Koren, Y., Bell, R., and Volinsky, C. Matrix factorization          Rosset, C., Cheng, C.-A., Mitra, A., Santacroce, M., Awadal-
   techniques for recommender systems. Computer, 42(8):                lah, A., and Xie, T. Direct nash optimization: Teaching
   30­37, 2009.                                                        language models to self-improve with general preferences.
                                                                       arXiv preprint arXiv:2404.03715, 2024.
Kreutzer, J., Uyheng, J., and Riezler, S. Reliability and
   learnability of human bandit feedback for sequence-to-           Schulman, J., Wolski, F., Dhariwal, P., Radford, A., and
   sequence reinforcement learning. In Proceedings of the              Klimov, O. Proximal policy optimization algorithms.
  56th Annual Meeting of the Association for Computa-                  arXiv preprint arXiv:1707.06347, 2017.
   tional Linguistics (Volume 1: Long Papers), pp. 1777­
  1788, 2018.                                                       Srivastava, A., Rastogi, A., Rao, A., Shoeb, A. A. M., Abid,
                                                                      A., Fisch, A., Brown, A. R., Santoro, A., Gupta, A.,
Kwon, M., Biyik, E., Talati, A., Bhasin, K., Losey, D. P.,             Garriga-Alonso, A., et al. Beyond the imitation game:
   and Sadigh, D. When humans aren't optimal: Robots that              Quantifying and extrapolating the capabilities of language
   collaborate with risk-aware humans. In Proceedings of               models. arXiv preprint arXiv:2206.04615, 2022.
   the 2020 ACM/IEEE international conference on human-
   robot interaction, pp. 43­52, 2020.                              Stiennon, N., Ouyang, L., Wu, J., Ziegler, D., Lowe, R.,
                                                                      Voss, C., Radford, A., Amodei, D., and Christiano,
                                                                       P. F. Learning to summarize with human feedback. Ad-
                                                                       vances in Neural Information Processing Systems, 33:
                                                                       3008­3021, 2020.

                                                                12
Model Alignment as Prospect Theoretic Optimization

Sun, L., Zhan, W., Hu, Y., and Tomizuka, M. Interpretable          Zhao, Y., Joshi, R., Liu, T., Khalman, M., Saleh, M., and
   modelling of driving behaviors in interactive driving sce-         Liu, P. J. Slic-hf: Sequence likelihood calibration with
   narios based on cumulative prospect theory. In 2019 IEEE           human feedback. arXiv preprint arXiv:2305.10425, 2023.
  Intelligent Transportation Systems Conference (ITSC), pp.
  4329­4335. IEEE, 2019.                                           Zheng, L., Chiang, W.-L., Sheng, Y., Zhuang, S., Wu, Z.,
                                                                      Zhuang, Y., Lin, Z., Li, Z., Li, D., Xing, E., et al. Judging
Swamy, G., Dann, C., Kidambi, R., Wu, Z. S., and Agarwal,             llm-as-a-judge with mt-bench and chatbot arena. arXiv
  A. A minimaximalist approach to reinforcement learning              preprint arXiv:2306.05685, 2023.
   from human feedback. arXiv preprint arXiv:2401.04056,
   2024.                                                           Ziegler, D. M., Stiennon, N., Wu, J., Brown, T. B., Radford,
                                                                     A., Amodei, D., Christiano, P., and Irving, G. Fine-tuning
Tian, K., Mitchell, E., Yao, H., Manning, C. D., and Finn,            language models from human preferences. arXiv preprint
   C. Fine-tuning language models for factuality. arXiv               arXiv:1909.08593, 2019.
   preprint arXiv:2311.08401, 2023.

Touvron, H., Lavril, T., Izacard, G., Martinet, X., Lachaux,
   M.-A., Lacroix, T., Rozie`re, B., Goyal, N., Hambro, E.,
  Azhar, F., et al. Llama: Open and efficient foundation lan-
   guage models. arXiv preprint arXiv:2302.13971, 2023.

Tunstall, L., Beeching, E., Lambert, N., Rajani, N., Rasul,
   K., Belkada, Y., Huang, S., von Werra, L., Fourrier, C.,
   Habib, N., Sarrazin, N., Sanseviero, O., Rush, A. M.,
   and Wolf, T. Zephyr: Direct distillation of lm alignment,
   2023.

Tversky, A. and Kahneman, D. Availability: A heuristic for
   judging frequency and probability. Cognitive psychology,
   5(2):207­232, 1973.

Tversky, A. and Kahneman, D. Advances in prospect theory:
   Cumulative representation of uncertainty. Journal of Risk
   and uncertainty, 5:297­323, 1992.

von Werra, L., Belkada, Y., Tunstall, L., Beeching, E.,
  Thrush, T., Lambert, N., and Huang, S. Trl: Trans-
   former reinforcement learning. https://github.
   com/huggingface/trl, 2020.

Welleck, S., Kulikov, I., Roller, S., Dinan, E., Cho, K.,
   and Weston, J. Neural text generation with unlikelihood
   training. In International Conference on Learning Repre-
   sentations, 2019.

Xu, H., Sharaf, A., Chen, Y., Tan, W., Shen, L., Van Durme,
   B., Murray, K., and Kim, Y. J. Contrastive preference
   optimization: Pushing the boundaries of llm performance
   in machine translation. arXiv preprint arXiv:2401.08417,
   2024.

Yang, A., Yang, B., Hui, B., Zheng, B., Yu, B., Zhou, C.,
   Li, C., Li, C., Liu, D., Huang, F., et al. Qwen2 technical
   report. arXiv preprint arXiv:2407.10671, 2024.

Yuan, W., Pang, R. Y., Cho, K., Sukhbaatar, S., Xu, J.,
   and Weston, J. Self-rewarding language models. arXiv
   preprint arXiv:2401.10020, 2024.

                                                               13
                                               Model Alignment as Prospect Theoretic Optimization

A. Related Work

LLM Alignment Human feedback has been used to improve LLM capabilities in translation (Kreutzer et al., 2018), sum-
marization (Stiennon et al., 2020), sentiment-conditioned generation (Ziegler et al., 2019), and instruction-following (Ouyang
et al., 2022). The RLHF framework (Christiano et al., 2017; Bai et al., 2022) traditionally used to accomplish this is detailed
in §2. Still, momentum has largely shifted in favor of closed-form losses that directly operate on offline preferences, such
as DPO (Rafailov et al., 2023). This single stage of optimization distinguishes DPO from the conventional approach in
preference-based RL, which learns a reward and then fits the policy to those rewards (Jain et al., 2013; Busa-Fekete et al.,
2014). Other preference-based losses include CPO (Xu et al., 2024) and IPO (Azar et al., 2024).

Binary Feedback Despite not being a human-aware loss, unlikelihood training was among the first methods to align
language models using a binary signal (Welleck et al., 2019). However, Korbak et al. (2023) found unlikelihood training to
be worse than the CSFT baseline we tested in this work, which is among various approaches that convert a binary/discrete
signal into a control token (Lu et al., 2022). Learning from sparse binary feedback is a staple of information retrieval and
recommender systems (He et al., 2017; Koren et al., 2009). Many retrieval-augmented generation systems use contrastive
learning to ensure that generations are grounded. This can be framed as learning from synthetic binary feedback, although
depending on the implementation, it may be the retriever and not the LLM that is updated (Lu et al., 2022).

Online Alignment A recent string of work has centered on the idea of "self-training" or "self-play", during which the
policy is continually aligned on online data sampled from itself and then filtered (Chen et al., 2024; Yuan et al., 2024). Many
of these approaches frame the learning of a preference model as a two-player min-max game between two policies (Munos
et al., 2023; Rosset et al., 2024; Swamy et al., 2024). In theory, KTO can also be adapted for online alignment, though we
leave this as a direction for future work.

Prospect Theory Prospect theory, despite being influential in behavioral economics, has had a muted impact in machine
learning, with work concentrated in human-robot interaction (Kwon et al., 2020; Sun et al., 2019; Chan et al., 2021).

B. Proofs

Theorem 3.5 (restated) DPO and PPO-Clip are human-aware loss functions.

Proof. For a loss f to be a HALO, we need to first construct the human value

                                                           v(r(x, y) - EQ[r(x, y)])

where r(x, y) = l(x, y) log          (y|x)      is  the  implied reward (normalized by factor       l(y)), Q(Y |x) is an     input-conditioned
reference point distribution,                                                                       theoretic sense) that    is non-decreasing
                                    ref (y |x)  R        R is a value function (in the prospect

                                    and v :

everywhere and concave in (0, ).

The DPO loss is

                       LDPO(, ref) = Ex,yw,yl                   - log      log (yw|x) -  log (yl|x)
                                                                                ref (yw |x)              ref(yl|x)

where     >  0  is  a  hyperparameter.   DPO        meets  the  criteria  with  the  following  construction:    l(y)  =  ;  r  =     log   (y|x)      ;
                                                                                                                                           ref (y |x)

v(·) = log (·) is increasing and concave everywhere; Q places all mass on (x, yl), where yl is a dispreferred output for x

such that y  yl; and ax,y = -1.

The PPO-Clip loss is

                       LPPO (offline) = -Ex,y,tD[min(qA(x:y<t, yt), clip(q, 1 - , 1 + )A(x:y<t, yt))]

where  q  =   (yt|x:y<t)       are  the  token-level     probability  ratios  (where  y<t  denotes  the  output  sequence  up   to  the  t-th  token),
             ref (yt |x:y<t )

A denotes the token-level advantages, and   (0, 1) is a hyperparameter.

Since this is a token-level objective, let x:y<t denote the actual input and the token yi the actual output for the purpose
of framing this as a HALO. The advantage function A(x:y<t, yt) can be expressed as Q(x:y<t, yt) - V (x:y<t), the

                                                                          14
                              Model Alignment as Prospect Theoretic Optimization

difference between the action-value and value functions. Because V (x:y<t) = EyQ(x:y<t, y), the reference point

distribution is simply the policy.

The HALO-defined reward r is then implied by the product qQ(x:y<t, y). Assume without loss of generality that Q is
non-negative, since a constant can be added to Q without changing the advantage. Then means  u  1, qQ(x:y<t, y) =
log u = log ^(x:y<t, y)/^ref(x:y<t, y), where ^, ^ref are some implied policy and reference distributions. It is trivial to
show that the latter exist but are not unique.

For clarity, we can first write the value function piecewise. Where qA = r - z0 in the HALO notation:

                              v(qA) =          A min(q, 1 + )             if A(x:y<t, yt)  0
                                               A max(q, 1 - )             if A(x:y<t, yt) < 0

which we can combine as v(qA) = min(qA, A(1 + sign(qA))). ax,y = -1 completes the construction.

Proposition 4.1 (restated) As the reward r(x, y) implied by the current policy tends to ±, the KTO update of  tends
to zero.

Proof. Where d(y) is -1(+1) when y is desirable(undesirable), y is D(U ) when y is desirable(undesirable), and
z = r(x, y) - z0, the derivative of the KTO loss is

                     LKTO(, ref) = Ex,yD [d(y)y(z)(1 - (z)) log (y|x)]                                                      (10)

Note that we do not backpropagate through the KL term in the KTO loss and , y > 0. This gradient is simple to interpret:
if y is desirable, then d(y) is negative and we push up the probability of (y|x) to minimize the loss; if y is undesirable,
then d(y) is positive and we push down the probability of (y|x) to minimize the loss. As r tends to ±, the gradient
will tend to zero since either (1 - (z)) or (z) will tend to zero.

Theorem 4.2 (restated) Assuming the value function is logistic, for a reward function ra that maximizes (2), there exists
a reward function in its equivalence class (i.e., rb(x, y) = ra(x, y) + h(x) for some h(x)) that induces the same optimal
policy  and the same Bradley-Terry preference distribution but a different human value distribution.

Proof. Following the definition in Rafailov et al. (2023), we say ra and rb are in the same equivalence class if there exists
some function h(x) such that rb(x, y) = ra(x, y) + h(x). From Lemma 1 in Rafailov et al. (2023), we know that two
functions in the same equivalence class induce the same optimal policy:

ra (y|x)          =    1                   1  ra(x,  y   )
                     Z(x) ref(y|x) exp     

                  =                        1                              ref(y|x) exp     1     ra  (x,  y)  exp   1
                                                                                                                      h(x)
                     y ref(y|x) exp     1  ra  (x,  y)   exp     1  h(x)
                                                                                                                    

                  =                     1                            ref(y|x) exp       1  (ra  (x,  y)   +  h(x))
                                                                                        
                     y ref(y|x) exp     1  (ra  (x,  y)  +  h(x))
                                        

                  = rb (y|x)

For a Bradley-Terry model of preferences, it is trivial to show that p(yw  yl|x) is unaffected by h(x) since it is added to the
reward of both yw and yl. We will now show that the two reward functions do not necessarily induce the same distribution
of human values.

First, we assume

A Taylor series expansion of the human value of ra(x, y) around 0 would be:

                     (0)      +  (0)(ra(x,           y)  -  z0)  +     (0)  (ra(x,  y)  -  z0)2  +   ...
                                                                        2

                                                                 15
                   Model Alignment as Prospect Theoretic Optimization

A Taylor series expansion of the value of ra(x, y) + h(x) around h(x) would be:

           (h(x))  +  (h(x))(ra(x,  y)     -  z0)  +  (h(x))         (ra(x,      y)  -  z0)2  +  ...
                                                           2

Since  is strictly monotonic, for these series to be equal, we must have h(x) = 0. If this is not the case, then the values of
ra(x, y) and rb(x, y) will be different. Thus two arbitrary reward functions in the same equivalence class do not induce the
same distribution of human values.

Theorem 4.3 (restated) For input x with outputs {ya, yb}, let dataset D comprise contradictory preferences ya  yb and
yb  ya in proportion p  (0.5, 1) and (1 - p)  (0, 0.5) respectively. If p1/ref(ya|x) < (1 - p)1/ref(yb|x), then the

optimal DPO policy is more likely to produce the minority-preferred yb; the optimal KTO policy will strictly produce the

majority-preferred ya for a loss-neutral value function (D = U ).

Proof. Where u = (r(x, ya) - r(x, yb)), we can write the total DPO loss for x as

                   LDPO(x) = p(- log (u)) + (1 - p)(- log (-u))

Taking the derivative with respect to u and setting to zero, we get

0 = -p     (u)(-u)           + (1 - p)  (-u)(u)                      = -p(1 - (u)) + (1 - p)(u) = -p + (u)

                   (u)                     (-u)

= u = -1(p)

r(x, ya) = -1(p) + r(x, yb)

 log (ya|x) = log p +  log (yb|x)
ref(ya|x)  1-p               ref(yb|x)

             p        1/     ref(ya|x)
           1-p               ref(yb|x)
(ya|x) =                  ·             ·  (yb|x)

Thus when p1/ref(ya|x) < (1 - p)1/ref(yb|x), we have (ya|x) < (yb|x), meaning the optimal DPO policy is more
likely to produce the minority-preferred yb.

Where ua = (r(x, ya) - EQ[r(x, y)]) and ub = (r(x, yb) - EQ[r(x, y)]), noting that 1 - (-u) = (u), we can
write the total KTO loss for x as

LKTO(x) = pD(1 - (ua)) + (1 - p)U (ua) + pU (ub) + (1 - p)D(1 - (ub))
           = pD + ((1 - p)U - pD)(ua) + (1 - p)D + (pU - (1 - p)D)(ub)
           = D + ((1 - p)U - pD)(ua) + (pU - (1 - p)D)(ub)
           = D + D((1 - 2p)(ua) + (2p - 1)(ub)) (under loss neutrality)

Given that p > 0.5 by assumption and D > 0 by definition, the KTO loss is decreasing in ua and increasing in ub--and thus

decreasing in r(x, ya) and increasing in r(x, yb) respectively. The optimal KTO policy is thus (y|x) = 1[y = ya].

C. Implementations

SLiC Instead of sampling from the reference model to calculate the Lreg as Zhao et al. (2023) do--as it is very slow--we
just apply the cross-entropy loss to the SFT data, assuming that the reference model recovers the SFT distribution.

DPO We use the implementation of DPO in the code provided by Rafailov et al. (2023). We found that, as mentioned
in the original paper,  = 0.1 works best for most settings. Other training configurations, such as the learning rate and
optimizer, were borrowed from the original paper.

CSFT The control tokens used for generating the good and bad outputs are |good| and |bad| respectively, following
the precedent set in Korbak et al. (2023).

                                              16
                                               Model Alignment as Prospect Theoretic Optimization

KTO We use a  = 0.1 in our experiments unless otherwise specified (the same setting as for DPO), as it is close-to-
optimal for most settings. By default, D = U = 1. In experiments on imbalanced data subsampled from [SHP, HH,
OASST], we found that setting U , D such that the effective ratio of desirable:undesirable examples was 4:3 worked best,
regardless of which group was in the minority (see (9)). However, in running data-imbalanced experiments on UltraFeedback,
we found that an effective ratio of 1:1 worked best. The other hyperparameters (e.g., learning rate) are the same as in DPO.

PPO PPO-Clip is the traditional means of optimizing the RLHF objective (2). However, most implementations of
PPO-Clip for LLM alignment suffer from instability, particularly during distributed training. We find that running the
PPO-Clip objective on offline data with the following "tricks" leads to much more stable training:

   · We never update the reference distribution (i.e., the policy only takes one step in the trust region). Baheti et al. (2023)
      recommend this as well. To accommodate for this conservative change, we clip the probability ratios more liberally,
      finding that an asymmetric interval of [0.25, 4.0] works best instead of the small symmetrical interval (e.g., [0.8, 1.2])
      that is traditionally recommended.

   · Including a KL penalty (between the policy and reference distributions) in addition to the clipping makes training more
      stable, as is also done in the implementation by von Werra et al. (2020). We find that it is important to estimate the KL
      term not using the entire distribution but rather as the mean difference in the predicted log probabilities of the actual
      output tokens (i.e., the labels). We suspect that this makes a difference because the rest of the distribution can be poorly
      calibrated.

   · The value of a state is generally predicted by some value head attached to the policy model; the value loss is the MSE
      between the predicted value and the discounted sum of future rewards for each token. This is a linear layer in many
      RLHF implementations (von Werra et al., 2020). However, we find that backpropagating the value loss through this
      head and the policy leads to worse performance. Instead, we make the value head a 3-layer MLP and detach it from the
      computational graph, so that the value losses are not backpropagated through the policy model but the value head still
      has sufficient capacity to learn good estimates.

D. Human Evaluation

For human evaluation, we randomly sampled 256 prompts from the OpenAssistant test set and generated outputs from
Mistral 7B models aligned with DPO and KTO. All inputs were multi-turn conversations between a user and an assistant,
where the LLM played the role of the assistant (see Table 6 for an example) and the last turn in the input was that of the user.
These were sent to a third-party data annotation service where a pool of workers picked either the generated output or the
SFT target (from the OpenAssistant dataset) as the more appropriate response by the assistant. Any questions that required
specific domain experience (e.g., coding) were skipped, leading to 214 comparisons for DPO and KTO each.
The winrates of the aligned model over the SFT targets are 72.9% ± 5.3 for KTO and 62.1% ± 5.7 for DPO (where the
intervals are 90% binomial confidence intervals). In contrast, Table 3 contains the winrates when the same experiment is
run with GPT-4 as a judge instead: 65.2% ± 3.6 for KTO and 60.0% ± 3.7 for DPO. Thus although there is no significant
difference in the GPT-4-based evaluation, there is a significant difference with human evaluation at p < 0.05. We found that
68.7% of the individual human judgments concurred with the GPT-4 judgments for KTO; this number fell to 65.9% for
DPO.

E. Additional Experiments

                                                                        17
                             Model Alignment as Prospect Theoretic Optimization

Table 4. KTO alignment with only desirable/positive examples (i.e., the same data that would be used for SFT), yields similar results to

SFT when  is small; this is intuitive because it is less bound to the reference model. D is fixed at 1.

Dataset ()          MMLU GSM8k        HumanEval BBH             TydiQA AlpacaEval 2 Avg.

Setup ()            0-shot 8-shot, CoT 0-shot, Synth. 3-Shot, CoT 1-Shot, GP 0-shot

Metric ()           EM      EM        pass@1          EM        F1                                       % Win

SFT                 57.2 1.0          54.4            33.2      25.4                                     7.17   29.7

KTO ( = 0.1) 57.8 12.5                53.2            29.5      28.0                                     5.46   31.1

KTO ( = 0.01) 57.3 1.5                53.7            33.4      26.5                                     5.31   29.6

Figure 6. AlpacaEval 2 winrates of Mistral7B aligned with various methods on the OpenAssistant dataset. GPT-4-turbo with chain-of-
thought is used as the LM judge. KTO outperforms the SFT and DPO-aligned models at all temperatures.

Table 5. Results from aligning Zephyr--SFT (Tunstall et al., 2023) on UltraFeedback. Note that the base model had not been finetuned
on UltraFeedback but a similar dataset called UltraChat; the first row here denotes the finetuning results. All the methods below see
exactly 1 epoch of UltraFeedback, for an apples-to-apples comparison. AlpacaEval 2 was run using GPT4 Turbo CoT as the LM judge.
All evaluations were run with sampling temperature 0.7. Among the KTO variants: "one-y-per-x" denotes training standard KTO with
either the positive or negative output (not both); "no z0" refers to a constant reference point of 0; "concave" refers to a variant where the
human value function is concave in the loss regime (i.e., minimize - log(·) instead of 1 - (·)); "no ref" refers to a reference model-free
variant; "risk-neutral" refers to a variant where the human value function is just the identity function, implying risk neutrality.

Dataset ()              MMLU GSM8k          HumanEval BBH           TydiQA AlpacaEval 2 Avg.
Setup ()
Metric ()               0-shot 8-shot, CoT 0-shot, Synth. 3-Shot, CoT 1-Shot, GP 0-shot

                        EM      EM          pass@1        EM        F1                                   % Win

SFT                     57.2 39.0           30.1          46.3      36.3                                 6.4    35.9
DPO
ORPO ( = 0.1)           58.2 40.0           30.1          44.1      36.5                                 7.8    36.1
KTO
                        57.1 36.5           29.5          47.5      38.5                                 5.0    35.7

                        58.6 53.5           30.9          52.6      31.2                                 12.5   39.9

KTO (one-y-per-x)       58.0 50.0           30.7          49.9      37.7                                 11.6   39.7

KTO (no z0)             58.5 49.5           30.7          49.0      29.0                                 12.1   38.1

KTO (concave)           58.3 42.5           30.6          43.2      36.6                                 7.4    36.4

KTO (no ref, D = 1.75) 57.5     47.5        29.5          51.6      36.8                                 9.7    38.8

KTO (risk-neutral)      57.3 42.0           28.8          6.1       33.6                                 10.3   29.7

                                                  18
                   Model Alignment as Prospect Theoretic Optimization

Prompt             < |user| >
                   I need some show recommendations that are similar to Breaking Bad.
                   < |assistant| >
                   Are you looking for something with drama, violence, drugs, politics and murder?
                   < |user| >
                   This is exactly what I'm looking for in a show
                   < |assistant| >

SFT                Well, you've just asked for one of the best shows ever made, and they all have those elements

                   in some combination. There are a lot of shows with those elements.

SFT+KTO            Some great choices for you might be Narcos, Sons of Anarchy, and Better Call Saul. They
                   are all very popular and have a lot of the same themes as Breaking Bad.

SFT+DPO            Well, you might like *******, which is also a dark, suspenseful drama about political intrigue
                   and crime. Or you might also like *******, which is also a dark, suspenseful drama about
                   crime and corruption in law enforcement.
                   --
                   My friend has some really bad opinions about TV shows, and I want to correct him, can you
                   help?
                   --
                   Sure! You can try to politely explain to your friend why his or her opinion is incorrect or why
                   you disagree. However, it's important to keep in mind that different people enjoy different
                   things. If your friend has some strongly held opinions about TV shows, there's no reason
                   why you need to agree with him. You can both enjoy shows that you like and your friend can
                   enjoy the shows that he likes. That's just the way it is.
                   --
                   Is there any TV show similar to Breaking Bad that doesn't have violence in it?
                   --
                   It's very hard to find a TV show that doesn't have any violence in it at all. However, there are
                   some TV shows that are not focused on violence. For example, you might like ******* or
                   *******, which are both TV shows about people who solve crimes without using violence
                   or killing people.
                   --
                   Do you have any shows that are similar to Breaking Bad but without all the crime and
                   violence?
                   --
                   Sure! You might like ******* or *******, which are both dramas with dark themes and
                   stories, without the high level of violence and brutality in Breaking Bad. Or you might like
                   *******, which is also a dark and suspenseful drama.
                   --
                   Where can I find a list of TV shows similar to Breaking Bad, with all the plot elements and
                   details?
                   --
                   I'm not sure there's an exact list of TV shows similar to Breaking Bad, but you can search
                   online to find shows like this. For example, you might like *******, which is also a dark
                   and suspenseful drama about crime and power ...

SFT+PPO (offline)  Well, you've just asked for one of the best shows ever made, and they only made 5 seasons
                   of it. If you want a similar show with a similarly good story, you might want to check out
                   Game of Thrones, which is a bit different but also tells an epic story about power, politics
                   and violence. Let me know if you need any more recommendations, it's no problem!

Table 6. Sample generations from the different aligned versions of Llama-30B for a prompt about show recommendations (all models
were aligned with data following the user-assistant format). Note that the SFT answer is not helpful and the SFT+DPO answer hallucinates
multiple turns of the conversation (in fact, we had to truncate the answer shown here because the complete answer is too long). The
SFT+PPO (offline) answer is helpful but only provides one recommendation, while SFT+KTO is succinct and provides multiple options.

                   19
