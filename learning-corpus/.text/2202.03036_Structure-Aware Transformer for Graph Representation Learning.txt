                                          Structure-Aware Transformer for Graph Representation Learning

                                          Dexiong Chen * 1 2 Leslie O'Bray * 1 2 Karsten Borgwardt 1 2

arXiv:2202.03036v3 [stat.ML] 13 Jun 2022                          Abstract                                with successful applications in drug discovery (Gaudelet
                                                                                                          et al., 2021), protein design (Ingraham et al., 2019), social
                                                The Transformer architecture has gained growing           network analysis (Fan et al., 2019), and so on. A large class
                                                attention in graph representation learning recently,      of GNNs build multilayer models, where each layer oper-
                                                as it naturally overcomes several limitations of          ates on the previous layer to generate new representations
                                                graph neural networks (GNNs) by avoiding their            using a message-passing mechanism (Gilmer et al., 2017)
                                                strict structural inductive biases and instead only       to aggregate local neighborhood information.
                                                encoding the graph structure via positional en-
                                                coding. Here, we show that the node represen-             While many different message-passing strategies have been
                                                tations generated by the Transformer with posi-           proposed, some critical limitations have been uncovered in
                                                tional encoding do not necessarily capture struc-         this class of GNNs. These include the limited expressive-
                                                tural similarity between them. To address this is-        ness of GNNs (Xu et al., 2019; Morris et al., 2019), as well
                                                sue, we propose the Structure-Aware Transformer,          as known problems such as over-smoothing (Li et al., 2018;
                                                a class of simple and flexible graph Transform-           2019; Chen et al., 2020; Oono & Suzuki, 2020) and over-
                                                ers built upon a new self-attention mechanism.            squashing (Alon & Yahav, 2021). Over-smoothing mani-
                                                This new self-attention incorporates structural in-       fests as all node representations converging to a constant
                                                formation into the original self-attention by ex-         after sufficiently many layers, while over-squashing occurs
                                                tracting a subgraph representation rooted at each         when messages from distant nodes are not effectively prop-
                                                node before computing the attention. We propose           agated through certain "bottlenecks" in a graph, since too
                                                several methods for automatically generating the          many messages get compressed into a single fixed-length
                                                subgraph representation and show theoretically            vector. Designing new architectures beyond neighborhood
                                                that the resulting representations are at least as        aggregation is thus essential to solve these problems.
                                                expressive as the subgraph representations. Em-
                                                pirically, our method achieves state-of-the-art per-      Transformers (Vaswani et al., 2017), which have proved to
                                                formance on five graph prediction benchmarks.             be successful in natural language understanding (Vaswani
                                                Our structure-aware framework can leverage any            et al., 2017), computer vision (Dosovitskiy et al., 2020),
                                                existing GNN to extract the subgraph representa-          and biological sequence modeling (Rives et al., 2021), of-
                                                tion, and we show that it systematically improves         fer the potential to address these issues. Rather than only
                                                performance relative to the base GNN model, suc-          aggregating local neighborhood information in the message-
                                                cessfully combining the advantages of GNNs and            passing mechanism, the Transformer architecture is able
                                                Transformers. Our code is available at https:             to capture interaction information between any node pair
                                               //github.com/BorgwardtLab/SAT.                             via a single self-attention layer. Moreover, in contrast to
                                                                                                          GNNs, the Transformer avoids introducing any structural
                                          1. Introduction                                                 inductive bias at intermediate layers, addressing the expres-
                                                                                                          sivity limitation of GNNs. Instead, it encodes structural
                                          Graph neural networks (GNNs) have been established as           or positional information about nodes only into input node
                                          powerful and flexible tools for graph representation learning,  features, albeit limiting how much information it can learn
                                                                                                          from the graph structure. Integrating information about the
                                             *Equal contribution 1Department of Biosystems Science        graph structure into the Transformer architecture has thus
                                          and Engineering, ETH Zu¨rich, Switzerland 2SIB Swiss In-        gained growing attention in the graph representation learn-
                                          stitute of Bioinformatics, Switzerland. Correspondence to:      ing field. However, most existing approaches only encode
                                          Dexiong Chen <dexiong.chen@bsse.ethz.ch>, Leslie O'Bray         positional relationships between nodes, rather than explic-
                                          <leslie.obray@bsse.ethz.ch>.                                    itly encoding the structural relationships. As a result, they
                                                                                                          may not identify structural similarities between nodes and
                                          Proceedings of the 39 th International Conference on Machine    could fail to model the structural interaction between nodes
                                          Learning, Baltimore, Maryland, USA, PMLR 162, 2022. Copy-       (see Figure 1). This could explain why their performance
                                          right 2022 by the author(s).
              Structure-Aware Graph Transformer

   G1     G2                                                     2. Related Work
u      v
                                                                 We present here the work most related to ours, namely the
Figure 1: Position-aware vs. structure-aware: Using a            work stemming from message passing GNNs, positional
positional encoding based on shortest paths in G1 and G2         representations on graphs, and graph Transformers.
respectively (assuming all edges have equal weight), node u
and v would receive identical encodings since their shortest     Message passing graph neural networks Message pass-
paths to all other nodes are the same in both graphs. How-       ing graph neural networks have recently been one of the
ever, their structures are different, with v forming a triangle  leading methods for graph representation learning. An early
with its red neighbors.                                          seminal example is the GCN (Kipf & Welling, 2017), which
                                                                 was based on performing convolutions on the graph. Gilmer
was dominated by sparse GNNs in several tasks (Dwivedi           et al. (2017) reformulated the early GNNs into a framework
et al., 2022).                                                   of message passing GNNs, which has since then become
                                                                 the predominant framework of GNNs in use today, with
Contribution In this work, we address the critical ques-         extensive examples (Hamilton et al., 2017; Xu et al., 2019;
tion of how to encode structural information into a Trans-       Corso et al., 2020; Hu et al., 2020b; Velickovic´ et al., 2018;
former architecture. Our principal contribution is to intro-     Li et al., 2020a; Yang et al., 2022). However, as mentioned
duce a flexible structure-aware self-attention mechanism         above, they suffer from problems of limited expressiveness,
that explicitly considers the graph structure and thus cap-      over-smoothing, and over-squashing.
tures structural interaction between nodes. The resulting
class of Transformers, which we call the Structure-Aware         Absolute encoding Because of the limited expressive-
Transformer (SAT), can provide structure-aware representa-       ness of GNNs, there has been some recent research into
tions of graphs, in contrast to most existing position-aware     the use of absolute encoding (Shaw et al., 2018), which
Transformers for graph-structured data. Specifically:            consists of adding or concatenating positional or structural
                                                                 representations to the input node features. While it is of-
   · We reformulate the self-attention mechanism in              ten called an absolute positional encoding, we refer to it
      Vaswani et al. (2017) as a kernel smoother and extend      more generally as an absolute encoding to include both po-
      the original exponential kernel on node features to also   sitional and structural encoding, which are both important
      account for local structures, by extracting a subgraph     in graph modeling. Absolute encoding primarily considers
      representation centered around each node.                  position or location relationships between nodes. Examples
                                                                 of position-based methods include the Laplacian positional
   · We propose several methods for automatically generat-       encoding (Dwivedi & Bresson, 2021; Kreuzer et al., 2021),
      ing the subgraph representations, enabling the resulting   Weisfeiler­Lehman-based positional encoding (Zhang et al.,
      kernel smoother to simultaneously capture structural       2020), and random walk positional encoding (RWPE) (Li
      and attributed similarities between nodes. The result-     et al., 2020b; Dwivedi et al., 2022), while distance-based
      ing representations are theoretically guaranteed to be     methods include distances to a predefined set of nodes (You
      at least as expressive as the subgraph representations.    et al., 2019) and shortest path distances between pairs of
                                                                 nodes (Zhang et al., 2020; Li et al., 2020b). Dwivedi et al.
   · We demonstrate the effectiveness of SAT models on           (2022) extend these ideas by using a trainable absolute en-
      five graph and node property prediction benchmarks         coding.
      by showing it achieves better performance than state-
      of-the-art GNNs and Transformers. Furthermore, we          Graph Transformers While the absolute encoding meth-
      show how SAT can easily leverage any GNN to com-           ods listed above can be used with message passing GNNs,
      pute the node representations which incorporate sub-       they also play a crucial role in the (graph) Transformer ar-
      graph information and outperform the base GNN, mak-        chitecture. Graph Transformer (Dwivedi & Bresson, 2021)
      ing it an effortless enhancer of any existing GNN.         provided an early example of how to generalize the Trans-
                                                                 former architecture to graphs, using Laplacian eigenvectors
   · Finally, we show that we can attribute the performance      as an absolute encoding and computing attention on the
      gains to the structure-aware aspect of our architecture,   immediate neighborhood of each node, rather than on the
      and showcase how SAT is more interpretable than the        full graph. SAN (Kreuzer et al., 2021) also used the Lapla-
      classic Transformer with an absolute encoding.             cian eigenvectors for computing an absolute encoding, but
                                                                 computed attention on the full graph, while distinguishing
We will present the related work and relevant background in      between true and created edges. Many graph Transformer
Sections 2 and 3 before presenting our method in Section 4       methods also use a relative encoding (Shaw et al., 2018) in
and our experimental findings in Section 5.
                                         Structure-Aware Graph Transformer

addition to absolute encoding. This strategy incorporates         former layer, as shown below:
representations of the relative position or distances between
nodes on the graph directly into the self-attention mecha-                    X = X + Attn(X),
nism, as opposed to the absolute encoding which is only                                                                              (2)
applied once to the input node features. Mialon et al. (2021)
propose a relative encoding by means of kernels on graphs                    X = FFN(X ) := ReLU(X W1)W2.
to bias the self-attention calculation, which is then able to
incorporate positional information into Transformers via the      Multiple layers can be stacked to form a Transformer model,
choice of kernel function. Other recent work seeks to incor-      which ultimately provides node-level representations of the
porate structural information into the graph Transformer, for     graph. As the self-attention is equivariant to permutations
example by encoding some carefully selected graph theo-           of the input nodes, the Transformer will always generate the
retic properties such as centrality measures and shortest path    same representations for nodes with the same attributes re-
distances as positional representations (Ying et al., 2021) or    gardless of their locations and surrounding structures in the
by using GNNs to integrate the graph structure (Rong et al.,      graph. It is thus necessary to incorporate such information
2020; Jain et al., 2021; Mialon et al., 2021; Shi et al., 2021).  into the Transformer, generally via absolute encoding.

In this work, we combine the best of both worlds from mes-        Absolute encoding Absolute encoding refers to adding
sage passing GNNs and from the Transformer architecture.          or concatenating the positional or structural representations
We incorporate both an absolute as well as a novel relative       of the graph to the input node features before the main
encoding that explicitly incorporates the graph structure,        Transformer model, such as the Laplacian positional encod-
thereby designing a Transformer architecture that takes both      ing (Dwivedi & Bresson, 2021) or RWPE (Dwivedi et al.,
local and global information into account.                        2022). The main shortcoming of these encoding methods is
                                                                  that they generally do not provide a measure of the structural
                                                                  similarity between nodes and their neighborhoods.

3. Background                                                     Self-attention as kernel smoothing As noticed by Mi-
                                                                  alon et al. (2021), the self-attention in Eq. (1) can be rewrit-
In the following, we refer to a graph as G = (V, E, X),           ten as a kernel smoother
where the node attributes for node u  V is denoted by
xu  X  Rd and the node attributes for all nodes are               Attn(xv) =                 exp(xv, xu)   )  f  (xu  ),  v    V,
stored in X  Rn×d for a graph with n nodes.                                                 wV exp(xv, xw
                                                                                        uV

                                                                                                                               (3)

3.1. Transformers on Graphs                                       where f (x) = WVx is the linear value function and exp
                                                                  is a (non-symmetric) exponential kernel on Rd × Rd param-
While GNNs use the graph structure explicitly, Transform-
ers remove that explicit structure, and instead infer relations   eterized by WQ and WK:
between nodes by leveraging the node attributes. In this
sense, the Transformer (Vaswani et al., 2017) ignores the         exp(x, x ) := exp WQx, WKx / dout , (4)
graph structure and rather considers the graph as a (multi-)
set of nodes, and uses the self-attention mechanism to infer      where ·, · is the dot product on Rd. With this form, Mialon
the similarity between nodes. The Transformer itself is com-      et al. (2021) propose a relative positional encoding strategy
posed of two main blocks: a self-attention module followed        via the product of this kernel and a diffusion kernel on the
by a feed-forward neural network. In the self-attention mod-      graph, which consequently captures the positional similarity
ule, the input node features X are first projected to query       between nodes. However, this method is only position-
(Q), key (K) and value (V) matrices through a linear projec-      aware, in contrast to our structure-aware encoding that will
tion such that Q = XWQ, K = XWK and V = XWV                       be presented in Section 4.
respectively. We can compute the self-attention via

Attn(X)  :=              QKT       )V    Rn×dout ,  (1)           4. Structure-Aware Transformer
             softmax( 
                             dout                                 In this section, we will describe how to encode the graph
                                                                  structure into the self-attention mechanism and provide a
where dout refers to the dimension of Q, and                      class of Transformer models based on this framework.
WQ, WK, WV are trainable parameters. It is common
to use multi-head attention, which concatenates multiple          4.1. Structure-Aware Self-Attention
instances of Eq. (1) and has shown to be effective in prac-
tice (Vaswani et al., 2017). Then, the output of the self-        As presented above, self-attention in the Transformer can be
attention is followed by a skip-connection and a feed-            rewritten as a kernel smoother where the kernel is a trainable
forward network (FFN), which jointly compose a Trans-             exponential kernel defined on node features, and which only
Input graph                   Structure-Aware Graph Transformer                            Transformer layer
                                  Structure extractor

                              ...                                                          Feed-forward network
                                                               GNN
                                                                                           Multi-head self-attention
                              ...

                                                                                           Q  K                  V

                              Extract k-hop subgraphs         Update node representations

Figure 2: Overview of an example SAT layer that uses the k-subgraph GNN extractor as its structure extractor. The structure
extractor generates structure-aware node representations which are used to compute the query (Q) and key (K) matrices
in the Transformer layer. Structure-aware node representations are generated in the k-subgraph GNN extractor by first
extracting the k-hop subgraph centered at each node (here, k = 1) and then using a GNN on each subgraph to generate a
node representations using the full subgraph information. While the structure extractor can use any class of subgraphs, the
one illustrated here defined on the class of k-hop subgraphs has a reasonable computation-expressiveness trade-off.

captures attributed similarity between a pair of nodes. The       simplicity, we assume there are no edge attributes, but our
problem with this kernel smoother is that it cannot filter out    method can easily incorporate edge attributes as long as the
nodes that are structurally different from the node of interest   structure extractor can accommodate them. The edge at-
when they have the same or similar node features. In order        tributes are consequently not considered in the self-attention
to also incorporate the structural similarity between nodes,      computation, but are incorporated into the structure-aware
we consider a more generalized kernel that additionally           node representations. In the structure extractors presented
accounts for the local substructures around each node. By         in this paper, this means that edge attributes were included
introducing a set of subgraphs centered at each node, we          whenever the base GNN was able to handle edge attributes.
define our structure-aware attention as:

SA-attn(v) :=                  graph(SG(v), SG(u))    f  (xu  ),  k-subtree GNN extractor A straightforward way to ex-
                              wV graph(SG(v), SG(w))              tract local structural information at node u is to apply any
                          uV
                                                                  existing GNN model to the input graph with node features
                                                         (5)      X and take the output node representation at u as the sub-
                                                                  graph representation at u. More formally, if we denote by
where SG(v) denotes a subgraph in G centered at a node            GNNG(k) an arbitrary GNN model with k layers applied to
v associated with node features X and graph can be any            G with node features X, then

kernel that compares a pair of subgraphs. This new self-

attention function not only takes the attributed similarity into

account but also the structural similarity between subgraphs.                              (u, G) = GNN(Gk)(u).       (7)

It thus generates more expressive node representations than

the original self-attention, as we will show in Section 4.4.      This extractor is able to represent the k-subtree structure
                                                                  rooted at u (Xu et al., 2019). While this class of structure
Moreover, this self-attention is no longer equivariant to any     extractors is fast to compute and can flexibly leverage any
                                                                  existing GNN, they cannot be more expressive than the
permutation of nodes but only to nodes whose features and         Weisfeiler­Lehman test due to the expressiveness limitation
                                                                  of message passing GNNs (Xu et al., 2019). In practice, a
subgraphs coincide, which is a desirable property.                small value of k already leads to good performance, while
                                                                  not suffering from over-smoothing or over-squashing.
In the rest of the paper, we will consider the following form
of graph that already includes a large class of expressive and
computationally tractable models:

graph(SG(v), SG(u)) = exp((v, G), (u, G)), (6)

where (u, G) is a structure extractor that extracts vector        k-subgraph GNN extractor A more expressive extractor
representations of some subgraph centered at u with node          is to use a GNN to directly compute the representation of
features X. We provide several alternatives of the structure      the entire k-hop subgraph centered at u rather than just
extractor below. It is worth noting that our structure-aware      the node representation u. Recent work has explored the
self-attention is flexible enough to be combined with any         idea of using subgraphs rather than subtrees around a node
model that generates representations of subgraphs, includ-        in GNNs, with positive experimental results (Zhang & Li,
ing GNNs and (differentiable) graph kernels. For notational       2021; Wijesinghe & Wang, 2022), as well as being strictly
                   Structure-Aware Graph Transformer

more powerful than the 1-WL test (Zhang & Li, 2021). We           into the link between the self-attention and kernel methods.
follow the same setup as is done in Zhang & Li (2021),
and adapt our GNN extractor to utilize the entire k-hop           4.2. Structure-Aware Transformer
subgraph. The k-subgraph GNN extractor aggregates the
updated node representations of all nodes within the k-hop        Having defined our structure-aware self-attention function,
neighborhood using a pooling function such as summation.          the other components of the Structure-Aware Transformer
Formally, if we denote by Nk(u) the k-hop neighborhood            follow the Transformer architecture as described in Sec-
of node u including itself, the representation of a node u is:    tion 3.1; see Figure 2 for a visual overview. Specifically,
                                                                  the self-attention function is followed by a skip-connection,
(u, G) =           GNNG(k)(v).  (8)                               a FFN and two normalization layers before and after the
                                                                  FFN. In addition, we also include the degree factor in the
          vNk (u)                                                 skip-connection, which was found useful for reducing the
                                                                  overwhelming influence of highly connected graph compo-
We observe that prior to the pooling function, the k-             nents (Mialon et al., 2021), i.e.,
subgraph GNN extractor is equivalent to using the k-subtree
GNN extractor within each k-hop subgraph. So as to cap-           xv = xv + 1/ dv SA-attn(v),  (9)
ture the attributed similarity as well as structural similarity,
we augment the node representation from k-subgraph GNN            where dv denotes the degree of node v. After a Transformer
extractor with the original node features via concatenation.      layer, we obtain a new graph with the same structure but
While this extractor provides more expressive subgraph            different node features G = (V, E, X ), where X corre-
representations than the k-subtree extractor, it requires enu-    sponds to the output of the Transformer layer.
merating all k-hop subgraphs, and consequently does not
scale as well as the k-subtree extractor to large datasets.       Finally, for graph property prediction, there are various
                                                                  ways to aggregate node-level representations into a graph
Other structure extractors Finally, we present a list of          representation, such as by taking the average or sum. Al-
other potential structure extractors for different purposes.      ternatively, one can use the embedding of a virtual [CLS]
One possible choice is to directly learn a number of "hidden      node (Jain et al., 2021) that is attached to the input graph
graphs" as the "anchor subgraphs" to represent subgraphs          without any connectivity to other nodes. We compare these
for better model interpretability, by using the concepts intro-   approaches in Section 5.
duced in Nikolentzos & Vazirgiannis (2020). While Niko-
lentzos & Vazirgiannis (2020) obtain a vector representation      4.3. Combination with Absolute Encoding
of the input graph by counting the number of matching
walks between the whole graph and each of the hidden              While the self-attention in Eq. (5) is structure-aware, most
graphs, one could extend this to the node level by comparing      absolute encoding techniques are only position-aware and
the hidden graphs to the k-hop subgraph centered around           could therefore provide complementary information. In-
each node. The adjacency matrix of the hidden graphs              deed, we find that the combination leads to further perfor-
is a trainable parameter in the network, thereby enabling         mance improvements, which we show in Section 5. We
end-to-end training to identify which subgraph structures         choose to use the RWPE (Dwivedi et al., 2022), though any
are predictive. Then, for a trained model, visualizing the        other absolute positional representations, including learn-
learned hidden graphs provides useful insights about the          able ones, can also be used.
structural motifs in the dataset.
                                                                  We further argue that only using absolute positional en-
Furthermore, more domain-specific GNNs could also be              coding with the Transformer would exhibit a too relaxed
used to extract potentially more expressive subgraph rep-         structural inductive bias which is not guaranteed to generate
resentations. For instance, Bodnar et al. (2021) recently         similar node representations even if two nodes have simi-
proposed a new kind of message passing scheme operat-             lar local structures. This is due to the fact that distance or
ing on regular cell complexes which benefits from prov-           Laplacian-based positional representations generally serve
ably stronger expressivity for molecules. Our self-attention      as structural or positional signatures but do not provide a
mechanism can fully benefit from the development of more          measure of structural similarity between nodes, especially
domain-specific and expressive GNNs.                              in the inductive case where two nodes are from different
                                                                  graphs. This is also empirically affirmed in Section 5 by
Finally, another possible structure extractor is to use a non-    their relatively worse performance without using our struc-
parametric graph kernel (e.g. a Weisfeiler-Lehman graph           tural encoding. In contrast, the subgraph representations
kernel) on the k-hop subgraphs centered around each node.         used in the structure-aware attention can be tailored to mea-
This provides a flexible way to combine graph kernels and         sure the structural similarity between nodes, and thus gener-
deep learning, which might offer new theoretical insights         ate similar node-level representations if they possess similar
                                   Structure-Aware Graph Transformer

attributes and surrounding structures. We can formally state   Table 1: Comparison of SAT to SOTA methods on graph
this in the following theorem:                                 regression and classification tasks. ZINC results use edge
                                                               weights where applicable, otherwise without edge weights.
Theorem 1. Assume that f is a Lipschitz mapping with
the Lipschitz constant denoted by Lip(f ) and the structure      indicates we obtained the results ourselves by adapting
extractor  is bounded by a constant C on the space of          the code provided by the original paper. means that higher
subgraphs. For any pair of nodes v and v in two graphs         is better for the performance metric; indicates lower is
G = (V, E, X) and G = (V , E , X ) with the same num-          better.
ber of nodes |V | = |V |, the distance between their repre-
sentations after the structure-aware attention is bounded by:                     ZINC         CLUSTER       PATTERN

SA-attn(v) - SA-attn(v )  C1[ hv - hv                          # GRAPHS           12,000         12,000          14,000
                                                               AVG. # NODES        23.2           117.2           118.9
                                                               AVG. # EDGES        49.8          4,303.9         6,098.9
                                                               METRIC              MAE         ACCURACY        ACCURACY

+D(H, H )] + C2D(X, X ),                        (10)           GIN                0.387±0.015 64.716±1.553   85.590±0.011
                                                               GAT                0.384±0.007 70.587±0.447   78.271±0.186
                                                               PNA                0.188±0.004 67.077±0.977   86.567±0.075

where C1, C2 > 0 are constants depending on |V |, Lip(f ),     TRANSFORMER+RWPE   0.310±0.005  29.622±0.176  86.183±0.019
C and spectral norms of the parameters in SA-attn, whose       GRAPH TRANSFORMER  0.226±0.014  73.169±0.622  84.808±0.068
expressions are given in the Appendix, and hw := (w, G)        SAN                0.139±0.006  76.691±0.650  86.581±0.037
denotes the subgraph representation at node w for any          GRAPHORMER         0.122±0.006
                                                                                                       ­             ­
w  V and hw := (w , G ) similarly, and H = (hw)wV
and H = (hw )w V denote the multiset of subgraph rep-          K-SUBTREE SAT      0.102±0.005 77.751±0.121   86.865±0.043
resentations in G and G respectively. Denoting by (V, V )      K-SUBGRAPH SAT     0.094±0.008 77.856±0.104   86.848±0.037

the set of permutations from V to V , D is an optimal match-   becomes possible to study the expressiveness of the output
                                                               representations. More specifically, we formally show that
ing metric between two multisets of representations with the   the node representation from a structure-aware attention
                                                               layer is at least as expressive as its subgraph representation
same cardinality, defined as                                   given by the structure extractor, following the injectivity of
                                                               the attention function with respect to the query:
D(X, X ) := inf sup                xw - x(w) .
                                                               Theorem 2. Assume that the space of node attributes X is
                        (V,V ) wV                              countable. For any pair of nodes v and v in two graphs
                                                               G = (V, E, X) and G = (V , E , X ), assume that there
The proof is provided in the Appendix. The metric D is         exist a node u1 in V such that xu1 = xw for any w  V
an optimal matching metric between two multisets which         and a node u2 in V such that its subgraph representation
measures how different they are. This theorem shows that       (u2, G) = (w, G) for any w  V . Then, there exists a
two node representations from the SA-attn are similar if the   set of parameters and a mapping f : X  Rdout such that
graphs that they belong to have similar multisets of node      their representations after the structure-aware attention are
features and subgraph representations overall, and at the      different, i.e. SA-attn(v) = SA-attn(v ), if their subgraph
same time, the subgraph representations at these two nodes     representations are different, i.e. (v, G) = (v , G ).
are similar. In particular, if two nodes belong to the same
graph, i.e. G = G , then the second and last terms on the      Note that the assumptions made in the theorem are mild
right side of Eq. (10) are equal to zero and the distance      as one can always add some absolute encoding or random
between their representations is thus constrained by the dis-  noise to make the attributes of one node different from all
tance between their corresponding subgraph representations.    other nodes, and similarly for subgraph representations. The
However, for Transformers with absolute positional encod-      countable assumption on X is generally adopted for expres-
ing, the distance between two node representations is not      sivity analysis of GNNs (e.g. Xu et al. (2019)). We assume
constrained by their structural similarity, as the distance    f to be any mapping rather than just a linear function as in
between two positional representations does not necessarily    the definition of the self-attention function since it can be
characterize how structurally similar two nodes are. Despite   practically approximated by a FFN in multi-layer Transform-
stronger inductive biases, we will show that our model is      ers through the universal approximation theorem (Hornik,
still sufficiently expressive in the next section.             1991). Theorem 2 suggests that if the structure extractor is
                                                               sufficiently expressive, the resulting SAT model can also be
4.4. Expressivity Analysis                                     at least equally expressive. Furthermore, more expressive
                                                               extractors could lead to more expressively powerful SAT
The expressive power of graph Transformers compared to         models and thus better prediction performance, which is
classic GNNs has hardly been studied, since the soft struc-    also empirically confirmed in Section 5.
tural inductive bias introduced in absolute encoding is gen-
erally hard to characterize. Thanks to the unique design of
our SAT, which relies on a subgraph structure extractor, it
                                 Structure-Aware Graph Transformer

Table 2: Comparison of SAT to SOTA methods on OGB                CLUSTER (Dwivedi et al., 2020), PATTERN (Dwivedi
datasets.                                                        et al., 2020), OGBG-PPA (Hu et al., 2020a) and OGBG-
                                                                 CODE2 (Hu et al., 2020a).
                  OGBG-PPA OGBG-CODE2
                                                                 We compare our method to the following GNNs: GCN (Kipf
# GRAPHS           158,100       452,741                         & Welling, 2017), GraphSAGE (Hamilton et al., 2017),
AVG. # NODES         243.4         125.2                         GAT (Velickovic´ et al., 2018), GIN (Xu et al., 2019),
AVG. # EDGES        2,266.1        124.2                         PNA (Corso et al., 2020), DeeperGCN (Li et al., 2020a),
METRIC                                                           and ExpC (Yang et al., 2022). Our comparison partners also
                  ACCURACY       F1 SCORE                        include several recently proposed Transformers on graphs,
                                                                 including the original Transformer with RWPE (Dwivedi
GCN               0.6839±0.0084  0.1507±0.0018                   et al., 2022), Graph Transformer (Dwivedi & Bresson,
GCN-VIRTUAL NODE  0.6857±0.0061  0.1595±0.0018                   2021), SAN (Kreuzer et al., 2021), Graphormer (Ying et al.,
GIN               0.6892±0.0100  0.1495±0.0023                   2021) and GraphTrans (Jain et al., 2021), a model that uses
GIN-VIRTUAL NODE  0.7037±0.0107  0.1581±0.0026                   the vanilla Transformer on top of a GNN.
DEEPERGCN         0.7712±0.0071
EXPC              0.7976±0.0072           ­                      All results for the comparison methods are either taken
                                          ­                      from the original paper or from Dwivedi et al. (2020) if
                                                                 not available. We consider k-subtree and k-subgraph SAT
TRANSFORMER       0.6454±0.0033 0.1670±0.0015                    equipped with different GNN extractors, including GCN,
GRAPHTRANS                                                       GIN, GraphSAGE and PNA. For OGBG-PPA and OGBG-
                  ­              0.1830±0.0024                   CODE2, we do not run experiments for k-subgraph SAT
                                                                 models due to large memory requirements. Full details on
K-SUBTREE SAT     0.7522±0.0056 0.1937±0.0028                    the datasets, experimental setup, and hyperparameters are
                                                                 provided in the Appendix.
5. Experiments
                                                                 5.2. Comparison to State-of-the-Art Methods
In this section, we evaluate SAT models versus several
SOTA methods for graph representation learning, including        We show the performance of SATs compared to other GNNs
GNNs and Transformers, on five graph and node prediction         and Transformers in Table 1 and 2. SAT models consistently
tasks, as well as analyze the different components of our        outperform SOTA methods on these datasets, showing its
architecture to identify what drives the performance. In         ability to combine the benefits of both GNNs and Trans-
summary, we discovered the following aspects about SAT:          formers. In particular, for the CODE2 dataset, our SAT
                                                                 models outperform SOTA methods by a large margin de-
   · The structure-aware framework achieves SOTA perfor-         spite a relatively small number of parameters and minimal
      mance on graph and node classification tasks, outper-      hyperparameter tuning, which will put it at the first place on
      forming SOTA graph Transformers and sparse GNNs.           the OGB leaderboard.

   · Both instances of the SAT, namely k-subtree and k-          5.3. SAT Models vs. Sparse GNNs
      subgraph SAT, always improve upon the base GNN it
      is built upon, highlighting the improved expressiveness    Table 3 summarizes the performance of SAT relative to the
      of our structure-aware approach.                           sparse GNN it uses to extract the subgraph representations,
                                                                 across different GNNs. We observe that both variations
   · We show that incorporating the structure via our            of SAT consistently bring large performance gains to its
      structure-aware attention brings a notable improvement     base GNN counterpart, making it a systematic enhancer of
      relative to the vanilla Transformer with RWPE that just    any GNN model. Furthermore, PNA, which is the most
      uses node attribute similarity instead of also incorpo-    expressive GNN we considered, has consistently the best
      rating structural similarity. We also show that a small    performance when used with SAT, empirically validating
      value of k already leads to good performance, while        our theoretical finding in Section 4.4. k-subgraph SAT also
      not suffering from over-smoothing or over-squashing.       outperforms or performs equally as k-subtree SAT in almost
                                                                 all the cases, showing its superior expressiveness.
   · We show that choosing a proper absolute positional
      encoding and a readout method improves performance,        5.4. Hyperparameter Studies
      but to a much lesser extent than incorporating the struc-
      ture into the approach.                                    While Table 3 showcases the added value of the SAT rel-
                                                                 ative to sparse GNNs, we now dissect the components of
Furthermore, we note that SAT achieves SOTA performance
while only considering a small hyperparameter search space.
Performance could likely be further improved with more
hyperparameter tuning.

5.1. Datasets and Experimental Setup

We assess the performance of our method with five medium
to large benchmark datasets for node and graph prop-
erty prediction, including ZINC (Dwivedi et al., 2020),
                                                          Structure-Aware Graph Transformer

Table 3: Since SAT uses a GNN to extract structures, we compare the performance of the original sparse GNN to SAT which
uses that GNN ("base GNN"). Across different choices of GNNs, we observe that both k-subtree and k-subgraph SATs
always outperform the original sparse GNN it uses. The evaluation metrics are the same as in Table 1.

                                                        ZINC          CLUSTER       PATTERN
                                       W/ EDGE ATTR. W/O EDGE ATTR.        ALL           ALL

PNA GRAPHSAGE GIN GCN  BASE GNN        0.192±0.015  0.367±0.011      68.498±0.976  71.892±0.334
                       K-SUBTREE SAT   0.127±0.010  0.174±0.009      77.247±0.094  86.749±0.065
                       K-SUBGRAPH SAT  0.114±0.005  0.184±0.002      77.682±0.098  86.816±0.028

                       BASE GNN        0.209±0.009  0.387±0.015      64.716±1.553  85.590±0.011
                       K-SUBTREE SAT   0.115±0.005  0.166±0.007      77.255±0.085  86.759±0.022
                       K-SUBGRAPH SAT  0.095±0.002  0.162±0.013      77.502±0.282  86.746±0.014

                       BASE GNN        ­            0.398±0.002      63.844±0.110  50.516±0.001
                       K-SUBTREE SAT                                 77.592±0.074  86.818±0.043
                       K-SUBGRAPH SAT  ­            0.164±0.004      77.657±0.185  86.838±0.010

                       BASE GNN        ­            0.168±0.005      67.077±0.977  86.567±0.075
                       K-SUBTREE SAT                                 77.751±0.121  86.865±0.043
                       K-SUBGRAPH SAT  0.188±0.004  0.320±0.032      77.856±0.104  86.848±0.037
                                       0.102±0.005  0.147±0.001
                                       0.094±0.008  0.131±0.002

SAT on the ZINC dataset to identify which aspects of the         Comparison of readout methods Finally, we compare
architecture bring the biggest performance gains.                the performance of SAT models using different readout
                                                                 methods for aggregating node-level representations on the
Effect of k in SAT The key contribution of SAT is its            ZINC dataset in Figure 3c, including the CLS pooling dis-
ability to explicitly incorporate structural information in the  cussed in Section 4.2. Unlike the remarkable influence of
self-attention. Here, we seek to demonstrate that this infor-    the readout method in GNNs (Xu et al., 2019), we observe
mation provides crucial predictive information, and study        very little impact in SAT models.
how the choice of k affects the results. Figure 3a shows
how the test MAE is impacted by varying k for k-subtree          5.5. Model Interpretation
and k-subgraph extractors using PNA on the ZINC dataset.
All models use the RWPE. k = 0 corresponds to the vanilla        In addition to performance improvement, we show that SAT
Transformer only using absolute positional encoding, i.e.        offers better model interpretability compared to the classic
not using structure. We find that incorporating structural in-   Transformer with only absolute positional encoding. We
formation leads to substantial improvement in performance,       respectively train a SAT model and a Transformer with a
with optimal performance around k = 3 for both k-subtree         CLS readout on the Mutagenicity dataset, and visualize the
and k-subgraph extractors. As k increases beyond k = 4,          attention scores between the [CLS] node and other nodes
the performance in k-subtree extractors deteriorated, which      learned by SAT and the Transformer in Figure 4. The salient
is consistent with the observed phenomenon that GNNs             difference between the two models is that SAT has structure-
work best in shallower networks (Kipf & Welling, 2017).          aware node embeddings, and thus we can attribute the fol-
We observe that k-subgraph does not suffer as much from          lowing interpretability gains to that. While both models
this issue, underscoring a new aspect of its usefulness. On      manage to identify some chemical motifs known for muta-
the other hand, k-subtree extractors are more computation-       genicity, such as NO2 and NH2, the attention scores learned
ally efficient and scalable to larger OGB datasets.              by SAT are sparser and more informative, meaning that
                                                                 SAT puts more attention weights on these known mutagenic
Effect of absolute encoding We assess here whether the           motifs than the Transformer with RWPE. The vanilla Trans-
absolute encoding brought complementary information to           former even fails to put attention on some important atoms
SAT. In Figure 3b, we conduct an ablation study showing          such as the H atoms in the NH2 group. The only H atoms
the results of SAT with and without absolute positional en-      highlighted by SAT are those in the NH2 group, suggesting
coding, including RWPE and Laplacian PE (Dwivedi et al.,         that our SAT indeed takes the structure into account. More
2020). Our SAT with a positional encoding outperforms            focus on these discriminative motifs makes the SAT model
its counterpart without it, confirming the complementary         less influenced by other chemical patterns that commonly
nature of the two encodings. However, we also note that          exist in the dataset, such as benzene, and thus leads to over-
the performance gain brought by the absolute encoding is         all improved performance. More results are provided in the
far less than the gain obtained by using our structure-aware     Appendix.
attention, as shown in Figure 3a (comparing the instance
of k = 0 to k > 0), emphasizing that our structure-aware
attention is the more important aspect of the model.
                                                Structure-Aware Graph Transformer

                              k-subtree SAT                                             None                                                                                          Mean
                                                                                                                                                                                      Sum
                              k-subgraph SAT                                            LAP                                                                                           CLS

0.30                                            0.2                                     RW                 0.2

                                                     0.177

0.25
Test MAE
                                                                       Test MAE0.149
                                                                                                                                              Test MAE
0.20
0.15                                                               0.102                0.097 0.091 0.094  0.1                                          0.102 0.102 0.098     0.094 0.089 0.093

                                                0.1

0.10

      0  1      2          3  4              5       k-subtree SAT                      k-subgraph SAT                                                  k-subtree SAT         k-subgraph SAT

                   k-hops                                                            x                                                                                     x

         (a) Effect of k                             (b) Effect of absolute encoding                                                                    (c) Effect of readout method

Figure 3: We provide an analysis of the different drivers of performance in SAT on the ZINC dataset (lower is better). In
Figure 3a, we show how changing the size of k affects performance (k=0 is equivalent to a vanilla Transformer that is not
structure-aware). Figure 3b shows the effect of different absolute encoding methods, and Figure 3c shows the effect of
different readout methods.

C O Cl H N         SAT attn                     Transformer attn                     more expressive than the 1-WL test, such as the recent
      molecule                                                                       topological GNN introduced by Horn et al. (2021). Addi-
                                                                               0.16  tionally, the SAT framework is flexible and can incorporate
                                                                                     any structure extractor which produces structure-aware node
                                 0.25                       0.14                     representations, and could even be extended beyond using
                                                                                     GNNs, such as differentiable graph kernels.
                                 0.20                       0.12
                                                                                     Another important area for future work is to focus on re-
                                                            0.10                     ducing the high memory cost and time complexity of the
                                                                                     self-attention computation, as is being done in recent efforts
                                 0.15                       0.08                     for developing a so-called linear transformer, which has
                                                                                     linear complexity in both time and space requirements (Tay
                                 0.10                       0.06                     et al., 2020; Wang et al., 2020; Qin et al., 2022).

                                 0.05                       0.04                     Acknowledgements

                                                            0.02                     This work was supported in part by the Alfried Krupp Prize
                                                                                     for Young University Teachers of the Alfried Krupp von
                                 0.00                       0.00                     Bohlen und Halbach-Stiftung (K.B.). The authors would
                                                                                     also like to thank Dr. Bastian Rieck and Dr. Carlos Oliver
Figure 4: Attention visualization of SAT and the Trans-                              for their insightful feedback on the manuscript, which
former. The center column shows the attention weights of                             greatly improved it.
the [CLS] node learned by our SAT model and the right
column shows the attention weights learned by the clas-                              References
sic Transformer with the random walk positional encoding
(RWPE).                                                                              Abbe, E. Community detection and stochastic block mod-
                                                                                        els: Recent developments. Journal of Machine Learning
6. Discussion                                                                           Research (JMLR), 18(177):1­86, 2018. (Cited on 17)

We introduced the SAT model, which successfully incor-                               Alon, U. and Yahav, E. On the bottleneck of graph neural
porates structural information into the Transformer archi-                              networks and its practical implications. In International
tecture and overcomes the limitations of the absolute en-                               Conference on Learning Representations (ICLR), 2021.
coding. In addition to SOTA empirical performance with                                  (Cited on 1)
minimal hyperparameter tuning, SAT also provides better
interpretability than the Transformer.                                               Alsentzer, E., Finlayson, S. G., Li, M. M., and Zitnik, M.
                                                                                        Subgraph neural networks. In Proceedings of Neural
Limitations As mentioned above, k-subgraph SAT has
higher memory requirements than k-subtree SAT, which
can restrict its applicability if access to high memory GPUs
is restricted. We see the main limitation of SAT is that it
suffers from the same drawbacks as the Transformer, namely
the quadratic complexity of the self-attention computation.

Future work Because SAT can be combined with any
GNN, a natural extension of our work is to combine SAT
with structure extractors which have shown to be strictly
Structure-Aware Graph Transformer

  Information Processing Systems, NeurIPS, 2020. (Cited              discovery and development. Briefings in Bioinformatics,
   on 21)                                                            22(6):bbab159, 2021. (Cited on 1)

Bodnar, C., Frasca, F., Otter, N., Wang, Y. G., Lio`, P., Mont-   Gilmer, J., Schoenholz, S. S., Riley, P. F., Vinyals, O., and
   ufar, G. F., and Bronstein, M. Weisfeiler and lehman              Dahl, G. E. Neural message passing for quantum chem-
   go cellular: Cw networks. In Advances in Neural Infor-            istry. In International Conference on Machine Learning
   mation Processing Systems (NeurIPS), 2021. (Cited on             (ICML), 2017. (Cited on 1, 2)
   5)
                                                                  Hamilton, W. L., Ying, R., and Leskovec, J. Inductive
Chen, D., Lin, Y., Li, W., Li, P., Zhou, J., and Sun, X.             representation learning on large graphs. In Advances in
   Measuring and relieving the over-smoothing problem for           Neural Information Processing Systems (NeurIPS), 2017.
   graph neural networks from the topological view. In              (Cited on 2, 7)
  Proceedings of the AAAI Conference on Artificial Intelli-
   gence, 2020. (Cited on 1)                                      Horn, M., De Brouwer, E., Moor, M., Moreau, Y., Rieck, B.,
                                                                     and Borgwardt, K. Topological graph neural networks.
Corso, G., Cavalleri, L., Beaini, D., Lio`, P., and Velickovic´,     2021. (Cited on 9)
   P. Principal neighbourhood aggregation for graph nets.
   In Advances in Neural Information Processing Systems           Hornik, K. Approximation capabilities of multilayer feed-
  (NeurIPS), 2020. (Cited on 2, 7)                                   forward networks. Neural networks, 4(2):251­257, 1991.
                                                                    (Cited on 6, 15)
Dosovitskiy, A., Beyer, L., Kolesnikov, A., Weissenborn,
   D., Zhai, X., Unterthiner, T., Dehghani, M., Minderer, M.,     Hu, W., Fey, M., Zitnik, M., Dong, Y., Ren, H., Liu, B.,
   Heigold, G., Gelly, S., et al. An image is worth 16x16            Catasta, M., and Leskovec, J. Open graph benchmark:
  words: Transformers for image recognition at scale. In             Datasets for machine learning on graphs. In Advances in
  International Conference on Learning Representations              Neural Information Processing Systems (NeurIPS), 2020a.
  (ICLR), 2020. (Cited on 1)                                        (Cited on 7, 16, 17)

Dwivedi, V. P. and Bresson, X. A generalization of trans-         Hu, W., Liu, B., Gomes, J., Zitnik, M., Liang, P., Pande, V.,
   former networks to graphs. In AAAI Workshop on Deep               and Leskovec, J. Strategies for pre-training graph neu-
  Learning on Graphs: Methods and Applications, 2021.                ral networks. In International Conference on Learning
  (Cited on 2, 3, 7)                                                Representations (ICLR), 2020b. (Cited on 2)

Dwivedi, V. P., Joshi, C. K., Laurent, T., Bengio, Y., and        Ingraham, J., Garg, V., Barzilay, R., and Jaakkola, T. Gener-
   Bresson, X. Benchmarking graph neural networks. arXiv             ative models for graph-based protein design. In Advances
   preprint arXiv:2003.00982, 2020. (Cited on 7, 8, 16, 17)          in Neural Information Processing Systems (NeurIPS),
                                                                     2019. (Cited on 1)
Dwivedi, V. P., Luu, A. T., Laurent, T., Bengio, Y., and Bres-
   son, X. Graph neural networks with learnable structural        Irwin, J. J., Sterling, T., Mysinger, M. M., Bolstad, E. S.,
   and positional representations. In International Confer-          and Coleman, R. G. Zinc: A free tool to discover chem-
   ence on Learning Representations, 2022. (Cited on 2, 3,           istry for biology. Journal of Chemical Information and
   5, 7, 17)                                                        Modeling, 52(7):1757­1768, 2012. (Cited on 16)

Fan, W., Ma, Y., Li, Q., He, Y., Zhao, E., Tang, J., and Yin,     Jain, P., Wu, Z., Wright, M., Mirhoseini, A., Gonzalez, J. E.,
   D. Graph neural networks for social recommendation. In            and Stoica, I. Representing long-range context for graph
  The World Wide Web Conference, 2019. (Cited on 1)                  neural networks with global attention. In Advances in
                                                                    Neural Information Processing Systems (NeurIPS), 2021.
Gao, B. and Pavel, L. On the properties of the softmax func-        (Cited on 3, 5, 7)
   tion with application in game theory and reinforcement
   learning. arXiv preprint arXiv:1704.00805, 2017. (Cited        Kersting, K., Kriege, N. M., Morris, C., Mutzel,
   on 14)                                                            P., and Neumann, M. Benchmark data sets for
                                                                     graph kernels, 2016. http://graphkernels.cs.
Gao, H. and Ji, S. Graph u-nets. In International Conference         tu-dortmund.de. (Cited on 20)
   on Machine Learning, pp. 2083­2092, 2019. (Cited on
   21)                                                            Kipf, T. N. and Welling, M. Semi-supervised classifica-
                                                                     tion with graph convolutional networks. In International
Gaudelet, T., Day, B., Jamasb, A. R., Soman, J., Regep,             Conference on Learning Representations (ICLR), 2017.
   C., Liu, G., Hayter, J. B., Vickers, R., Roberts, C., Tang,      (Cited on 2, 7, 8)
  J., et al. Utilizing graph machine learning within drug
Structure-Aware Graph Transformer

Kreuzer, D., Beaini, D., Hamilton, W. L., Le´tourneau, V.,   Qin, Z., Sun, W., Deng, H., Li, D., Wei, Y., Lv, B., Yan, J.,
   and Tossou, P. Rethinking graph transformers with spec-      Kong, L., and Zhong, Y. cosformer: Rethinking softmax
   tral attention. In Advances in Neural Information Pro-       in attention. In International Conference on Learning
   cessing Systems (NeurIPS), 2021. (Cited on 2, 7)            Representations, 2022. (Cited on 9)

Li, G., Mu¨ller, M., Thabet, A., and Ghanem, B. Deepgcns:    Rives, A., Meier, J., Sercu, T., Goyal, S., Lin, Z., Liu, J.,
   Can gcns go as deep as cnns? In Proceedings of the           Guo, D., Ott, M., Zitnick, C. L., Ma, J., et al. Biological
  International Conference on Computer Vision (ICCV),           structure and function emerge from scaling unsupervised
   2019. (Cited on 1)                                           learning to 250 million protein sequences. Proceedings of
                                                                the National Academy of Sciences, 118(15), 2021. (Cited
Li, G., Xiong, C., Thabet, A., and Ghanem, B. Deepergcn:        on 1)
  All you need to train deeper gcns, 2020a. (Cited on 2, 7)
                                                             Rong, Y., Bian, Y., Xu, T., Xie, W., Wei, Y., Huang, W., and
Li, P., Wang, Y., Wang, H., and Leskovec, J. Distance en-       Huang, J. Self-supervised graph transformer on large-
   coding: Design provably more powerful neural networks        scale molecular data. In Advances in Neural Information
   for graph representation learning. In Advances in Neu-      Processing Systems (NeurIPS), 2020. (Cited on 3)
   ral Information Processing Systems (NeurIPS), 2020b.
  (Cited on 2)                                               Shaw, P., Uszkoreit, J., and Vaswani, A. Self-attention
                                                               with relative position representations. In Proceedings
Li, Q., Han, Z., and Wu, X. Deeper insights into graph          of the North American Chapter of the Association for
   convolutional networks for semi-supervised learning. In     Computational Linguistics (NAACL), 2018. (Cited on 2)
  Proceedings of the AAAI Conference on Artificial Intelli-
   gence, 2018. (Cited on 1)                                 Shi, Y., Huang, Z., Feng, S., Zhong, H., Wang, W., and
                                                                Sun, Y. Masked label prediction: Unified message pass-
Loshchilov, I. and Hutter, F. Sgdr: Stochastic gradient         ing model for semi-supervised classification. In Zhou,
   descent with warm restarts. In International Conference      Z.-H. (ed.), Proceedings of the Thirtieth International
   on Learning Representations (ICLR), 2016. (Cited on 17)     Joint Conference on Artificial Intelligence (IJCAI-21), pp.
                                                               1548­1554. International Joint Conferences on Artificial
Loshchilov, I. and Hutter, F. Decoupled weight decay reg-       Intelligence Organization, 8 2021. (Cited on 3)
   ularization. In International Conference on Learning
  Representations (ICLR), 2018. (Cited on 17)                Tay, Y., Dehghani, M., Bahri, D., and Metzler, D. Efficient
                                                                transformers: A survey. arXiv preprint arXiv:2009.06732,
Mesquita, D., Souza, A. H., and Kaski, S. Rethinking            2020. (Cited on 9)
   pooling in graph neural networks. In Advances in Neural
  Information Processing Systems (NeurIPS), 2020. (Cited     Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones,
   on 21)                                                       L., Gomez, A. N., Kaiser, L., and Polosukhin, I. Atten-
                                                                tion is all you need. In Advances in Neural Information
Mialon, G., Chen, D., Selosse, M., and Mairal, J. Graphit:     Processing Systems (NeurIPS), 2017. (Cited on 1, 2, 3,
   Encoding graph structure in transformers, 2021. (Cited      17)
   on 3, 5)
                                                             Velickovic´, P., Cucurull, G., Casanova, A., Romero, A.,
Micchelli, C. A., Xu, Y., and Zhang, H. Universal kernels.      Lio`, P., and Bengio, Y. Graph Attention Networks. In
  Journal of Machine Learning Research (JMLR), 7(12),          International Conference on Learning Representations
   2006. (Cited on 16)                                         (ICLR), 2018. (Cited on 2, 7)

Morris, C., Ritzert, M., Fey, M., Hamilton, W. L., Lenssen,  Wang, S., Li, B. Z., Khabsa, M., Fang, H., and Ma, H.
  J. E., Rattan, G., and Grohe, M. Weisfeiler and leman go      Linformer: Self-attention with linear complexity, 2020.
   neural: Higher-order graph neural networks. In Proceed-     (Cited on 9)
   ings of the AAAI Conference on Artificial Intelligence,
   2019. (Cited on 1)                                        Wijesinghe, A. and Wang, Q. A new perspective on "how
                                                                graph neural networks go beyond weisfeiler-lehman?". In
Nikolentzos, G. and Vazirgiannis, M. Random walk graph         International Conference on Learning Representations,
   neural networks. In Advances in Neural Information           2022. (Cited on 4)
  Processing Systems (NeurIPS), 2020. (Cited on 5)
                                                             Xu, K., Hu, W., Leskovec, J., and Jegelka, S. How powerful
Oono, K. and Suzuki, T. Graph neural networks exponen-          are graph neural networks? In International Conference
   tially lose expressive power for node classification. In     on Learning Representations (ICLR), 2019. (Cited on 1,
  International Conference on Learning Representations          2, 4, 6, 7, 8, 13, 15)
  (ICLR), 2020. (Cited on 1)
                                                         Structure-Aware Graph Transformer

Yang, M., Wang, R., Shen, Y., Qi, H., and Yin, B. Breaking
   the expression bottleneck of graph neural networks. IEEE
  Transactions on Knowledge and Data Engineering, pp.
  1­1, 2022. doi: 10.1109/TKDE.2022.3168070. (Cited on
   2, 7)

Ying, C., Cai, T., Luo, S., Zheng, S., Ke, G., He, D., Shen, Y.,
   and Liu, T.-Y. Do transformers really perform badly for
   graph representation? In Advances in Neural Information
  Processing Systems (NeurIPS), 2021. (Cited on 3, 7)

Ying, Z., You, J., Morris, C., Ren, X., Hamilton, W., and
   Leskovec, J. Hierarchical graph representation learning
  with differentiable pooling. Advances in neural informa-
   tion processing systems, 31, 2018. (Cited on 21)

You, J., Ying, R., and Leskovec, J. Position-aware graph
   neural networks. In International Conference on Machine
  Learning (ICML), 2019. (Cited on 2)

Zhang, J., Zhang, H., Xia, C., and Sun, L. Graph-bert: Only
   attention is needed for learning graph representations.
   arXiv preprint arXiv:2001.05140, 2020. (Cited on 2)

Zhang, M. and Li, P. Nested graph neural networks. In Pro-
   ceedings of the 35th Conference on Neural Information
  Processing Systems (NeurIPS), 2021. (Cited on 4, 5)
                                   Structure-Aware Graph Transformer

                                   Appendix

This appendix provides both theoretical and experimental materials and is organized as follows: Section A provides a
more detailed background on graph neural networks. Section B presents proofs of Theorem 1 and 2. Section C provides
experimental details and additional results. Section D provides details on the model interpretation and additional visualization
results.

A. Background on Graph Neural Networks

The overarching idea of a graph neural network is to iteratively update a node's embedding by incorporating information sent
from its neighbors. Xu et al. (2019) provide a general framework of the steps incorporated in this process by generalizing
the different frameworks into AGGREGATE, COMBINE and READOUT steps. The various flavors of GNNs can be typically
understood as variations within these three functions. For a given layer l, the AGGREGATE step aggregates (e.g. using
the sum or mean) the representations of the neighbors of a given node, which is then combined with the given node's
representation from the previous layer in the COMBINE step. This is followed by a non-linear function, such as ReLU,
and the updated node representations are then passed to the next layer. These two steps are repeated for as many layers
as there are in the network. It is worth noting that the output of these two steps provides representations of nodes which
accounts for local sub-structures of size only increased by one, which would thus require a very deep network to capture
interactions between the given node and all other nodes (the depth should not be smaller than the diameter of the graph). At
the end of the network, the READOUT function provides a pooling function to convert the representations to the appropriate
output-level granularity (e.g. node-level or graph-level). Both the AGGREGATE and READOUT steps must be invariant to
node permutations.

B. Theoretical Analysis

B.1. Controllability of the Representations from the Structure-Aware Attention

Theorem 1. Assume that f is a Lipschitz mapping with the Lipschitz constant denoted by Lip(f ) and the structure extractor
 is bounded by a constant C on the space of subgraphs. For any pair of nodes v and v in two graphs G = (V, E, X) and
G = (V , E , X ) with the same number of nodes |V | = |V | = n, the distance between their representations after the
structure-aware attention is bounded by:

SA-attn(v) - SA-attn(v )  C1[ hv - hv + D(H, H )] + C2D(X, X ),                    (11)

where hw := (w, G) denotes the subgraph representation at node w for any w  V and hw := (w , G ) similarly, and
H = (hw)wV and H = (hw )w V denote the multiset of subgraph representations in G and G respectively. Denoting
by (V, V ) the set of permutations between V and V , D is a matching metric between two multisets of representations

with the same cardinality, defined as

                                   D(X, X ) := inf sup                xw - x(w) .

                                                           (V,V ) wV

C1 and C2 are constants given by:

                                     2
                         C1 = dout nLip(f )C WQ  WK , C2 = Lip(f ).

Proof. Let us denote by

                                   zv = ( WQhv, WKhw )wV  Rn,

                                   zv = ( WQhv , WKhw )w V  Rn,

and by softmax(z)  Rn for any z  Rn with its i-th coefficient

                                   softmax(z)i =            
                                                  jne=x1pe(xzpi/(zjd/oudt)out) .
                                                     Structure-Aware Graph Transformer

Then, we have
    SA-Attn(v) - SA-Attn(v )

=      softmax(zv)wf (xw) -                       softmax(zv )w f (xw )

   wV                                     w V

=      (softmax(zv)w - softmax(zv )(w))f (xw) + softmax(zv )(w)f (xw) -                                softmax(zv )w (f (xw ))

   wV                                                            wV                               w V

       (softmax(zv)w - softmax(zv )(w))f (xw) +                          softmax(zv )w (f (x-1(w )) - f (xw ))

   wV                                                               w V

where  : V  V is an arbitrary permutation and we used the triangle inequality. Now we need to bound the two terms
respectively. We first bound the second term:

            softmax(zv )w (f (x-1(w )) - f (xw ))                              softmax(zv )w f (x-1(w )) - f (xw )

       w V                                                               w V

                                                                               softmax(zv )w Lip(f ) x-1(w ) - xw

                                                                         w V

                                                                     = Lip(f )       softmax(zv )w x-1(w ) - xw

                                                                                w V

                                                                      Lip(f ) sup x-1(w ) - xw

                                                                                      w V

                                                                     = Lip(f ) sup xw - x(w)

                                                                                      wV

where the first inequality is a triangle inequality, the second inequality uses the Lipschitzness of f . And for the first term, we
can upper-bound it by

                                               (softmax(zv)w - softmax(zv )(w))f (xw)

                                          wV

                                           softmax(zv) - softmax((zv ))                 f (xw) 2

                                                                                wV

                                               1                 
                                                     zv - (zv )     nLip(f ),
                                               dout

where by abuse of notation, (z)  Rn denotes the vector whose w-th entry is z(w) for any z  Rn. The first inequality
comes from a simple matrix norm inequality, and the second inequality uses the fact that softmax function is 1/ dout-
Lipschitz (see e.g. Gao & Pavel (2017)). Then, we have

       zv - (zv )) 2 =                                                                                               2

                                      wV  WQhv, WKhw - WQhv , WKh(w)

                          =                                                                                                                          2

                                      wV  WQhv, WK(hw - h(w)) + WQ(hv - hv ), WKh(w)

            2                             WQhv, WK(hw - h(w)) 2 + WQ(hv - hv ), WKh(w) 2

               wV

            2                             WQhv 2 WK(hw - h(w)) 2 + WQ(hv - hv ) 2 WKh(w) 2

                   wV

            2                             C2      WQ  2  WK      2  hw - h(w)   2+   WQ  2  hv - hv    2C2                                              WK  2
                                                                                                                                                            

               wV

             2nC2                         WQ      2  WK  2       hv - hv 2 + sup hw - h(w) 2 ,
                                                         
                                                                                wV
                                                          Structure-Aware Graph Transformer

where the first inequality comes from (a + b)2  2(a2 + b2), the second one uses the Cauchy-Schwarz inequality and
the third one uses the definition of spectral norm and the bound of the structure extractor function. Then, we obtain the
following inequality

                                (softmax(zv)w - softmax(zv )(w))f (xw)

                           wV

                           2
             dout nLip(f )C WQ  WK                                  hv - hv + sup hw - h(w)

                                                                                           wV

By combining the upper bounds of the first and the second term, we obtain an upper bound for the distance between the
structure-aware attention representations:

               SA-attn(v) - SA-attn(v )  C1  hv - hv + sup hw - h(w)                     + C2 sup xw - x(w) ,
for any permutation   (V, V ), where
                                                                    wV                             wV

                                                           2
                                               C1 = dout nLip(f )C WQ  WK 
                                               C2 = Lip(f ).

Finally, by taking the infimum over the set of permutations, we obtain the inequality in the theorem.

B.2. Expressivity Analysis

Here, we assume that f can be any continuous mapping and it is approximated by an MLP network through the universal
approximation theorem (Hornik, 1991) in practice.

Theorem 2. Assume that the space of node attributes X is countable. For any pair of nodes v and v in two graphs
G = (V, E, X) and G = (V , E , X ), assume that there exists a node u1 in V such that xu1 = xw for any w  V and
a node u2 in V such that its subgraph representation (u2, G) = (w, G) for any w  V . Then, there exists a set of
parameters and a mapping f : X  Rdout such that their representations after the structure-aware attention are different,
i.e. SA-attn(v) = SA-attn(v ), if their subgraph representations are different, i.e. (v, G) = (v , G ).

Proof. This theorem amounts to showing the injectivity of the original dot-product attention with respect to the query, that

is to show                                                           exp(hv, hu)
                                                                    wV exp(hv, hw
                               Attn(hv, xv, G) =                                   )  f  (xu)

                                                                uV

is injective in hv, where

                              exp(h, h ) := exp WQh + bQ, WKh + bK / dout .                                    (12)

Here we consider the offset terms that were omitted in Eq. (1). Let us prove the contrapositive of the theorem. We assume
that Attn(hv, xv, G) = Attn(hv , xv , G ) for any set of parameters and any mapping f and want to show that hv = hv .

Without loss of generality, we assume that G and G have the same number of nodes, that is |V | = |V | = n. Otherwise,

one can easily add some virtual isolated nodes to the smaller graph. Now if we take WQ = WK = 0, all the softmax

coefficients will be identical and we have

                                                 f (xw) =                f (xw ).

                                             wV                     w V

Thus, by Lemma 5 of Xu et al. (2019), there exists a mapping f such that the multisets X and X are identical.

As a consequence, we can re-enumerate the nodes in two graphs by a sequence V (by abuse of notation, we keep using V
here) such that xu = xu for any u  V . Then, we can rewrite the equality Attn(hv, xv, G) = Attn(hv , xv , G ) as

                               exp(hv, hu)       -                   exp(hv , hu)        f (xu) = 0.
                                                                    wV exp(hv , hw)
                           uV  wV exp(hv, hw)
                             Structure-Aware Graph Transformer

Now since there exists a node u1 in V such that its attributes are different from all other nodes, i.e. xu1 = xw for any w  V ,
we can find a mapping f such that f (xu1 ) is not in the span of (f (xw))wV,w=u1 . Then, by their independence we have

                              exp(hv, hu1 ) =            exp(hv , hu1 ) ,
                             wV exp(hv, hw)             wV exp(hv , hw)

for any WQ, WK, bQ and bK .

On the one hand, if we take WQ = 0, we have for any WK, bQ and bK that

                                                                                         
 exp ( bQ, WKhu1 + bK / dout) =                          exp ( bQ, WKhu1 + bK / dout) .
wV exp ( bQ, WKhw + bK / dout)                          wV exp ( bQ, WKhw + bK / dout)

On the other hand if we take bQ = 0 we have for any WQ, WK and bK that

                                                                                            
 exp ( WQhv, WKhu1 + bK / dout) =                       exp ( WQhv , WKhu1 + bK / dout)
wV exp ( WQhv, WKhw + bK / dout)                            exp (  WQhv  , WKhw          +  bK /  dout)
                                                        wV                                    
                                                     =
                                                        exp ( WQhv , WKhu1 + bK / dout) ,
                                                        wV exp ( WQhv , WKhw + bK / dout)

where the second equality is obtained by replacing bQ with WQhv in the above equality. Then, we can rewrite the above
equality as below:

     exp  WQhv, WK(hw - hu1 )  = exp                        WQhv , W K(hw - hu1 ) .
                       dout                                               dout
wV                                 wV

                                                                                                                                           
If we denote by  : Rdout  H the feature mapping associated with the dot product kernel exp(t, t ) = exp( t, t / dout)

and H the correspond reproducing kernel Hilbert space, we then have for any WQ and WK that

                             (WQhv) - (WQhv ), (WK(hw - hu1 )) = 0.

                               wV                                          H

Since by assumption there exists a u2  V such that hu2 - hu1 = 0 and exp is a universal kernel (Micchelli et al., 2006),
WK  (WK(hu2 - hu1 )) is dense in H and we have (WQhv) = (WQhv ). We can then conclude, by the injectivity
of , that

                                                              WQhv = WQhv ,

for any WQ, and thus hv = hv . Now by taking hv = (v, G) and hv = (v , G ), we obtain the theorem.

C. Experimental Details and Additional Results

In this section, we provide implementation details and additional experimental results.

C.1. Computation Details

All experiments were performed on a shared GPU cluster equipped with GTX1080, GTX1080TI, GTX2080TI and TITAN
RTX. About 20 of these GPUs were used simultaneously, and the total computational cost of this research project was about
1k GPU hours.

C.2. Datasets Description

We provide details of the datasets used in our experiments, including ZINC (Irwin et al., 2012), CLUSTER (Dwivedi et al.,
2020), PATTERN (Dwivedi et al., 2020), OGBG-PPA (Hu et al., 2020a) and OGBG-CODE2 (Hu et al., 2020a). For each
dataset, we follow their respective training protocols and use the standard train/validation/test splits and evaluation metrics.

ZINC. The ZINC dataset is a graph regression dataset comprised of molecules, where the task is to predict constrained
solubility. Like Dwivedi et al. (2020), we use the subset of 12K molecules and follow their same splits.
                                                          Structure-Aware Graph Transformer

Table 4: Hyperparameters for SAT models trained on different datasets. RWPE-p indicates using p steps in the random walk
positional encoding, which results in a p-dimensional vector as the positional representation for each node.

Hyperparameter           ZINC   CLUSTER PATTERN OGBG-PPA           OGBG-CODE2
                            6                                               4
#Layers                    64      16   6             3
Hidden dimensions                  48                                     256
FFN hidden dimensions       8       8   64            128
#Attention heads                                                         {4, 8}
Dropout                  mean     None  2×Hidden dimensions
Size of subgraphs k    RWPE-20  RWPE-3                                   mean
Readout method                          8             8                  None
Absolute PE              0.001                                           0.0001
                          128           {0.0, 0.1, 0.2, 0.3, 0.4}
Learning rate             2000                                             32
Batch size                5000          {1, 2, 3, 4}                       30
#Epochs                   1e-5                                         2 epochs
Warm-up steps                           None          mean                1e-6
Weight decay
                                        RWPE-7        None

                                0.0005  0.0003         0.0003
                                  32      32              32
                                 200     200             200

                                 5000    5000         10 epochs
                                 1e-4    1e-4            1e-4

PATTERN and CLUSTER. PATTERN and CLUSTER Dwivedi et al. (2020) are synthetic datasets that were created
using Stochastic Block Models (Abbe, 2018). The goal for both datasets is node classification, with PATTERN focused on
detecting a given pattern in the dataset, and with CLUSTER focused on identifying communities within the graphs. For
PATTERN, the binary class label corresponds to whether a node is part of the predefined pattern or not; for CLUSTER, the
multi-class label indicates membership in a community. We use the splits as is used in Dwivedi et al. (2020).

OGBG-PPA. PPA (Hu et al., 2020a) is comprised of protein-protein association networks where the goal is to correctly
classify the network into one of 37 classes representing the category of species the network is from. Nodes represent proteins
and edges represent associations between proteins. Edge attributes represent information relative to the association, such as
co-expression. We use the standard splits provided by Hu et al. (2020a).

OGBG-CODE2. CODE2 (Hu et al., 2020a) is a dataset containing source code from the Python programming language.
It is made up of Abstract Syntax Trees where the task is to correctly classify the sub-tokens that comprise the method name.
We use the standard splits provided by Hu et al. (2020a).

C.3. Hyperparameter Choices and Reproducibility

Hyperparameter choice. In general, we perform a very limited hyperparameter search to produce the results in Table 1
and Table 2. The hyperparameters for training SAT models on different datasets are summarized in Table 4, where only the
dropout rate and the size of the subgraph k are tuned (k  {1, 2, 3, 4}). We use fixed RWPE (Dwivedi et al., 2022) with SAT
on ZINC, PATTERN and CLUSTER. In all experiments, we use the validation set to select the dropout rate and the size of
the subtree or subgraph k  {1, 2, 3, 4}. All other hyperparameters are fixed for simplicity, including setting the readout
method to mean pooling. We did not use RWPE on OGBG-PPA and OGBG-CODE2 as we observed very little performance
improvement. Note that we only use k = 1 for the k-subgraph SAT models on CLUSTER and PATTERN due to its large
memory requirement, which already leads to performance boost compared to the k-subtree SAT using a larger k. Reported
results are the average over 4 seeds on ZINC, PATTERN and CLUSTER, as is done in Dwivedi et al. (2020), and averaged
over 10 seeds on OGBG-PPA and OGBG-CODE2.

Optimization. All our models are trained with the AdamW optimizer (Loshchilov & Hutter, 2018) with a standard
warm-up strategy suggested for Transformers in Vaswani et al. (2017). We use either the L1 loss or the cross-entropy loss
depending on whether the task is regression or classification. The learning rate scheduler proposed in the Transformer is
used on the ZINC, PATTERN and CLUSTER datasets and a cosine scheduler (Loshchilov & Hutter, 2016) is used on the
larger OGBG-PPA and OGBG-CODE2 datasets.
                                                          Structure-Aware Graph Transformer

Table 5: Number of parameters and training time per epoch for k-subtree SAT models using the hyperparameters in Table 4.
Various GNNs are used as the base GNN in SAT.

                ZINC       CLUSTER PATTERN OGBG-PPA OGBG-CODE2

Base GNN        421k                         #Parameters
GCN             495k
GIN             523k             571k        380k          766k                14,030k
PNA                              684k                                          14,554k
                  6s             741k        455k          866k                15,734k
Base GNN          6s
GCN               9s                         493k          1,088k
GIN
PNA                              GPU time on a single TITAN RTX/epoch

                                 142s        40s           308s                40min
                                                                               40min
                                 144s        62s           310s                55min

                                 178s        90s           660s

Table 6: Test MAE for SAT models using different structure extractors and readout methods on the ZINC dataset.

                                 W/O EDGE ATTRIBUTES                     W/ EDGE ATTRIBUTES

                BASE GNN   MEAN         SUM           CLS          MEAN        SUM           CLS

K-SUBTREE SAT   GCN        0.174±0.009  0.170±0.010  0.167±9.005  0.127±0.010  0.117±0.008  0.115±0.007
K-SUBGRAPH SAT  GIN        0.166±0.007  0.162±0.010  0.157±0.002  0.115±0.005  0.112±0.008  0.104±0.003
                GRAPHSAGE  0.164±0.004  0.165±0.008  0.156±0.005
                PNA        0.147±0.001  0.142±0.008  0.135±0.004          -            -            -
                                                                  0.102±0.005  0.102±0.003  0.098±0.008
                GCN
                GIN        0.184±0.002  0.186±0.007  0.184±0.007  0.114±0.005  0.103±0.002  0.103±0.008
                GRAPHSAGE  0.162±0.013  0.158±0.007  0.162±0.005  0.095±0.002  0.097±0.002  0.098±0.010
                PNA        0.168±0.005  0.165±0.005  0.169±0.005
                           0.131±0.002  0.129±0.003  0.128±0.004          -            -            -
                                                                  0.094±0.008  0.089±0.002  0.093±0.009

Number of parameters and computation time. In Table 5, we report the number of parameters and the training time
per epoch for SAT with k-subtree GNN extractors using the hyperparameters selected from Table 4. Note that the number of
parameters used in our SAT on OGB datasets is smaller than most of the state-of-art methods.

C.4. Additional Results
We provide additional experimental results on ZINC, OGBG-PPA and OGBG-CODE2.

C.4.1. ADDITIONAL RESULTS ON ZINC
We report a more thorough comparison of SAT instances using different structure extractors and different readout methods
in Table 6. We find that SAT models with PNA consistently outperform other GNNs. Additionally, the readout methods
have very little impact on the prediction performance.

C.4.2. ADDITIONAL RESULTS ON OGBG-PPA
Table 7 summarizes the results for k-subtree SAT with different GNNs compared to state-of-the-art methods on OGBG-PPA.
All the results are computed from 10 runs using different random seeds.

C.4.3. ADDITIONAL RESULTS ON OGBG-CODE2
Table 8 summarizes the results for k-subtree SAT with different GNNs compared to state-of-the-art methods on OGBG-
CODE2. All the results are computed from 10 runs using different random seeds.
Structure-Aware Graph Transformer

Table 7: Comparison of SAT and SOTA methods on the OGBG-PPA dataset. All results are computed from 10 different
runs.

METHOD                                 OGBG-PPA
                   TEST ACCURACY VALIDATION ACCURACY
GCN
GCN-VIRTUAL NODE   0.6839±0.0084  0.6497±0.0034
GIN                0.6857±0.0061  0.6511±0.0048
GIN-VIRTUAL NODE   0.6892±0.0100  0.6562±0.0107
                   0.7037±0.0107  0.6678±0.0105
TRANSFORMER
                   0.6454±0.0033  0.6221±0.0039
K-SUBTREE SAT-GCN
K-SUBTREE SAT-GIN  0.7483±0.0048  0.7072±0.0030
K-SUBTREE SAT-PNA  0.7306±0.0076  0.6928±0.0058
                   0.7522±0.0056  0.7025±0.0064

Table 8: Comparison of SAT and SOTA methods on the OGBG-CODE2 dataset. All results are computed from 10 different
runs.

METHOD                            OGBG-CODE2
                   TEST F1 SCORE VALIDATION F1 SCORE
GCN
GCN-VIRTUAL NODE   0.1507±0.0018  0.1399±0.0017
GIN                0.1581±0.0026  0.1461±0.0013
GIN-VIRTUAL NODE   0.1495±0.0023  0.1376±0.0016
                   0.1581±0.0026  0.1439±0.0020
TRANSFORMER
GRAPHTRANS         0.1670±0.0015  0.1546±0.0018
                   0.1830±0.0024  0.1661±0.0012
K-SUBTREE SAT-GCN
K-SUBTREE SAT-GIN  0.1934±0.0020  0.1777±0.0011
K-SUBTREE SAT-PNA  0.1910±0.0023  0.1748±0.0016
                   0.1937±0.0028  0.1773±0.0023
                                                          Structure-Aware Graph Transformer

D. Model Interpretation

In this section, we provide implementation details about the model visualization.

D.1. Dataset and Training Details

We use the Mutagenicity dataset (Kersting et al., 2016), consisting of 4337 molecular graphs labeled based on their mutagenic
effect. We randomly split the dataset into train/val/test sets in a stratified way with a proportion of 80/10/10. We first train
a two-layer vanilla Transformer model using RWPE. The hidden dimension and the number of heads are fixed to 64 and
8 respectively. The CLS pooling as described in Section 4.2 is chosen as the readout method for visualization purpose.
We also train a k-subtree SAT using exactly the same hyperparameter setting except that it does not use any absolute
positional encoding. k is fixed to 2. For both models, we use the AdamW optimizer and the optimization strategy described
in Section C.3. We train enough epochs until both models converge. While the classic Transformer with RWPE achieves a
test accuracy of 78%, the k-subtree SAT achieves a 82% test accuracy.

D.2. Additional Results

Visualization of attention scores. Here, we provide additional visualization examples of attention scores of the [CLS]
node from the Mutagenicity dataset, learned by SAT and a vanilla Transformer. Figure 5 provides several examples of
attention learned weights. SAT generally learns sparser and more informative weights even for very large graph as shown in
the left panel of the middle row.
                          Structure-Aware Graph Transformer

C O Cl H N F Br S P I Na K Li Ca

molecule  SAT attn        Transformer attn                            molecule  SAT attn        Transformer attn  0.07
molecule  SAT attn                                                    molecule  SAT attn
molecule  SAT attn  0.25                    0.12                      molecule  SAT attn  0.40

                                                                                          0.35                    0.06

                    0.20                    0.10                                          0.30                    0.05

                                                                0.08                      0.25                    0.04
                    0.15
                                                                                          0.20                    0.03
                                                                0.06

                    0.10                    0.04                                          0.15                    0.02

                    0.05                    0.02                                          0.10

                                                                                          0.05                    0.01

                    0.00  Transformer attn  0.00                                          0.00  Transformer attn  0.00

                    0.35                    0.08                                          0.40                    0.14

                    0.30                    0.07                                          0.35
                                                                                                                                      0.12
                    0.25                    0.06
                                                                                          0.30
                                                                                                                  0.10
                                            0.05                                          0.25
                    0.20                                                                                          0.08
                                            0.04                                          0.20
                    0.15
                                            0.03                                          0.15                    0.06

                    0.10                    0.02                                          0.10                    0.04

                    0.05                    0.01                                          0.05                    0.02

                    0.00  Transformer attn  0.00                                          0.00  Transformer attn  0.00

                    0.25                    0.12                                          0.40                    0.07

                    0.20                    0.10                                          0.35                    0.06

                                            0.08                                          0.30                    0.05

                    0.15                                                                  0.25                    0.04

                                            0.06                                          0.20                    0.03

                    0.10                                                                  0.15                    0.02
                                                                0.04
                                                                                          0.10
                    0.05                    0.02
                                                                                          0.05                    0.01

                    0.00                    0.00                                          0.00                    0.00

Figure 5: Attention visualization of SAT and the Transformer. The middle column shows the attention weights of the [CLS]
node learned by our SAT model and the right column shows the attention weights learned by the classic Transformer with
RWPE.

E. Relationship to Subgraph Neural Networks and Graph Pooling

In the following we clarify the relationship (and differences) of SAT to Subgraph Neural Networks (Alsentzer et al., 2020)
as well as to the general topic of graph pooling.

E.1. Differences to Subgraph Neural Networks
Subgraph Neural Networks (SNN) (Alsentzer et al., 2020) explores explicitly incorporating position, neighborhood and
structural information, for the purpose of solving the problem of subgraph prediction. SNN generates representations at the
level of subgraph (rather than node). SAT, on the other hand, is instead motivated by modeling the structural interaction
(through the dot-product attention) between nodes in the Transformer architecture by generating node representations that
are structure-aware. This structure-aware aspect is achieved via a structure extractor, which can be any function that extracts
local structural information for a given node and does not necessarily need to explicitly extract subgraphs. For example, the
k-subtree GNN extractor does not explicitly extract the subgraph, but rather only uses the node representation generated
from a GNN. This aspect also makes the k-subtree SAT very scalable. In contrast to SNN, the input to the resulting SAT is
not subgraphs but rather the original graph, and the structure-aware node representations are computed as the query and key
for the dot-product attention at each layer.

E.2. Relationship to Graph Pooling
In GNNs, structural information is traditionally incorporated into node embeddings via the neighborhood aggregation
process. A supplemental way to incorporate structural information is through a process called local pooling, which is
typically based on graph clustering (Ying et al., 2018). Local pooling coarsens the adjacency matrix at layer l in the network
by, for example, applying a clustering algorithm to cluster nodes, and then replacing the adjacency matrix with the cluster
assignment matrix, where all nodes within a cluster are connected by an edge. An alternative approach to the pooling layer is
based on sampling nodes (Gao & Ji, 2019). While Mesquita et al. (2020) found that these local pooling operations currently
do not improve performance relative to more simple operations, local pooling could in theory be incorporated into the
existing SAT's k-subtree and k-subgraph GNN extractors, as it is another layer in a GNN.
