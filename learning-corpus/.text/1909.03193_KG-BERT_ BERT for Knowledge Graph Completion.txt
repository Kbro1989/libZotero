                                        KG-BERT: BERT for Knowledge Graph Completion

                                                      Liang Yao, Chengsheng Mao, Yuan Luo

                                                                          Northwestern University
                                                                              Chicago IL 60611

                                                     {liang.yao, chengsheng.mao, yuan.luo}@northwestern.edu

arXiv:1909.03193v2 [cs.CL] 11 Sep 2019                                Abstract                             sentation (Socher et al. 2013; Xie et al. 2016; Xiao et al.
                                                                                                           2017), but they learn unique text embedding for the same
                                           Knowledge graphs are important resources for many artifi-       entity/relation in different triples, which ignore contextual
                                           cial intelligence tasks but often suffer from incompleteness.   information. For instance, different words in the descrip-
                                           In this work, we propose to use pre-trained language models     tion of Steve Jobs should have distinct importance weights
                                           for knowledge graph completion. We treat triples in knowl-      connected to two relations "founded" and "isCitizenOf", the
                                           edge graphs as textual sequences and propose a novel frame-     relation "wroteMusicFor" can have two different meanings
                                           work named Knowledge Graph Bidirectional Encoder Rep-           "writes lyrics" and "composes musical compositions" given
                                           resentations from Transformer (KG-BERT) to model these          different entities. On the other hand, syntactic and seman-
                                           triples. Our method takes entity and relation descriptions of   tic information in large-scale text data is not fully utilized,
                                           a triple as input and computes scoring function of the triple   as they only employ entity descriptions, relation mentions
                                           with the KG-BERT language model. Experimental results on        or word co-occurrence with entities (Wang and Li 2016;
                                           multiple benchmark knowledge graphs show that our method        Xu et al. 2017; An et al. 2018).
                                           can achieve state-of-the-art performance in triple classifica-
                                           tion, link prediction and relation prediction tasks.               Recently, pre-trained language models such as ELMo (Pe-
                                                                                                           ters et al. 2018), GPT (Radford et al. 2018), BERT (Devlin
                                                              Introduction                                 et al. 2019) and XLNet (Yang et al. 2019) have shown great
                                                                                                           success in natural language processing (NLP), these mod-
                                        Large-scale knowledge graphs (KG) such as FreeBase (Bol-           els can learn contextualized word embeddings with large
                                        lacker et al. 2008), YAGO (Suchanek, Kasneci, and Weikum           amount of free text data and achieve state-of-the-art perfor-
                                        2007) and WordNet (Miller 1995) provide effective basis for        mance in many language understanding tasks. Among them,
                                        many important AI tasks such as semantic search, recom-            BERT is the most prominent one by pre-training the bidirec-
                                        mendation (Zhang et al. 2016) and question answering (Cui          tional Transformer encoder through masked language mod-
                                        et al. 2017). A KG is typically a multi-relational graph con-      eling and next sentence prediction. It can capture rich lin-
                                        taining entities as nodes and relations as edges. Each edge        guistic knowledge in pre-trained model weights.
                                        is represented as a triplet (head entity, relation, tail entity)
                                        ((h, r, t) for short), indicating the relation between two enti-      In this study, we propose a novel method for knowl-
                                        ties, e.g., (Steve Jobs, founded, Apple Inc.). Despite their ef-   edge graph completion using pre-trained language models.
                                        fectiveness, knowledge graphs are still far from being com-        Specifically, we first treat entities, relations and triples as
                                        plete. This problem motivates the task of knowledge graph          textual sequences and turn knowledge graph completion into
                                        completion, which is targeted at assessing the plausibility of     a sequence classification problem. We then fine-tune BERT
                                        triples not present in a knowledge graph.                          model on these sequences for predicting the plausibility of
                                                                                                           a triple or a relation. The method can achieve strong per-
                                           Much research work has been devoted to knowledge                formance in several KG completion tasks. Our source code
                                        graph completion. A common approach is called knowledge            is available at https://github.com/yao8839836/kg-bert. Our
                                        graph embedding which represents entities and relations in         contributions are summarized as follows:
                                        triples as real-valued vectors and assess triples' plausibil-
                                        ity with these vectors (Wang et al. 2017). However, most           · We propose a new language modeling method for knowl-
                                        knowledge graph embedding models only use structure in-               edge graph completion. To the best of our knowledge, this
                                        formation in observed triple facts, which suffer from the             is the first study to model triples' plausibility with a pre-
                                        sparseness of knowledge graphs. Some recent studies in-               trained contextual language model.
                                        corporate textual information to enrich knowledge repre-
                                                                                                           · Results on several benchmark datasets show that our
                                           Corresponding Author                                               method can achieve state-of-the-art results in triple clas-
                                        Copyright c 2020, Association for the Advancement of Artificial       sification, relation prediction and link prediction tasks.
                                        Intelligence (www.aaai.org). All rights reserved.
                     Related Work                                 with entities are utilized. Compared with these methods, our
                                                                  method can learn context-aware text embeddings with rich
Knowledge Graph Embedding                                         language information via pre-trained language models.

A literature survey of knowledge graph embedding meth-            Language Model Pre-training
ods has been conducted by (Wang et al. 2017). These meth-
ods can be classified into translational distance models and      Pre-trained language representation models can be divided
semantic matching models based on different scoring func-         into two categories: feature-based and fine tuning ap-
tions for a triple (h, r, t). Translational distance models use   proaches. Traditional word embedding methods such as
distance-based scoring functions. They assess the plausi-         Word2Vec (Mikolov et al. 2013) and Glove (Pennington,
bility of a triple (h, r, t) by the distance between the two      Socher, and Manning 2014) aimed at adopting feature-based
entity vectors h and t, typically after a translation per-        approaches to learn context-independent words vectors.
formed by the relation vector r. The representative mod-          ELMo (Peters et al. 2018) generalized traditional word em-
els are TransE (Bordes et al. 2013) and its extensions in-        beddings to context-aware word embeddings, where word
cluding TransH (Wang et al. 2014b). For TransE, the scor-         polysemy can be properly handled. Different from feature-
ing function is defined as the negative translational distance    based approaches, fine tuning approaches like GPT (Rad-
f (h, r, t) = -||h + r - t||. Semantic matching models            ford et al. 2018) and BERT (Devlin et al. 2019) used the
employ similarity-based scoring functions. The representa-        pre-trained model architecture and parameters as a starting
tive models are RESCAL (Nickel, Tresp, and Kriegel 2011),         point for specific NLP tasks. The pre-trained models capture
DistMult (Yang et al. 2015) and their extensions. For Dist-       rich semantic patterns from free text. Recently, pre-trained
Mult, the scoring function is defined as a bilinear function      language models have also been explored in the context of
f (h, r, t) = h, r, t . Recently, convolutional neural net-       KG. (Wang, Kulkarni, and Wang 2018) learned contextual
works also show promising results for knowledge graph             embeddings on entity-relation chains (sentences) generated
completion (Dettmers et al. 2018; Nguyen et al. 2018a;            from random walks in KG, then used the embeddings as ini-
Schlichtkrull et al. 2018).                                       tialization of KG embeddings models like TransE. (Zhang
                                                                  et al. 2019) incorporated informative entities in KG to en-
   The above methods conduct knowledge graph completion           hance BERT language representation. (Bosselut et al. 2019)
using only structural information observed in triples, while      used GPT to generate tail phrase tokens given head phrases
different kinds of external information like entity types, log-   and relation types in a common sense knowledge base which
ical rules and textual descriptions can be introduced to im-      does not cleanly fit into a schema comparing two entities
prove the performance (Wang et al. 2017). For textual de-         with a known relation. The method focuses on generat-
scriptions, (Socher et al. 2013) firstly represented entities by  ing new entities and relations. Unlike these studies, we use
averaging the word embeddings contained in their names,           names or descriptions of entities and relations as input and
where the word embeddings are learned from an external            fine-tune BERT to compute plausibility scores of triples.
corpus. (Wang et al. 2014a) proposed to jointly embed en-
tities and words into the same vector space by aligning                                    Method
Wikipedia anchors and entity names. (Xie et al. 2016) use
convolutional neural networks (CNN) to encode word se-            Bidirectional Encoder Representations from
quences in entity descriptions. (Xiao et al. 2017) proposed       Transformers (BERT)
semantic space projection (SSP) which jointly learns top-
ics and KG embeddings by characterizing the strong cor-           BERT (Devlin et al. 2019) is a state-of-the-art pre-trained
relations between fact triples and textual descriptions. De-      contextual language representation model built on a multi-
spite their success, these models learn the same textual rep-     layer bidirectional Transformer encoder (Vaswani et al.
resentations of entities and relations while words in en-         2017). The Transformer encoder is based on self-attention
tity/relation descriptions can have different meanings or im-     mechanism. There are two steps in BERT framework:
portance weights in different triples.                            pre-training and fine-tuning. During pre-training, BERT
                                                                  is trained on large-scale unlabeled general domain corpus
   To address the above problems, (Wang and Li 2016) pre-         (3,300M words from BooksCorpus and English Wikipedia)
sented a text-enhanced KG embedding model TEKE which              over two self-supervised tasks: masked language modeling
can assign different embeddings to a relation in different        and next sentence prediction. In masked language model-
triples. TEKE utilizes co-occurrences of entities and words       ing, BERT predicts randomly masked input tokens. In next
in an entity-annotated text corpus. (Xu et al. 2017) used an      sentence prediction, BERT predicts whether two input sen-
LSTM encoder with attention mechanism to construct con-           tences are consecutive. For fine-tuning, BERT is initialized
textual text representations given different relations. (An et    with the pre-trained parameter weights, and all of the pa-
al. 2018) proposed an accurate text-enhanced KG embed-            rameters are fine-tuned using labeled data from downstream
ding method by exploiting triple specific relation mentions       tasks such as sentence pair classification, question answer-
and a mutual attention mechanism between relation men-            ing and sequence labeling.
tion and entity description. Although these methods can han-
dle the semantic variety of entities and relations in distinct    Knowledge Graph BERT (KG-BERT)
triples, they could not make full use of syntactic and se-
mantic information in large scale free text data, as only en-     To take full advantage of contextual representation with
tity descriptions, relation mentions and word co-occurrence       rich language patterns, We fine tune pre-trained BERT for
   Triple Label y  {0, 1}

            C    T1h              ...       Tha Th[SEP] T1r    ...       Trb T[rSEP] T1t    ...  Ttc Tt[SEP]

                                                      KG-BERT(a)

            E[CLS] Eh1            ...       Eah Eh[SEP] Er1    ...       Erb  E[rSEP]  Et1  ...  Ect  Et[SEP]

            [CLS] Tok1h ... Tokha [SEP] Tokr1 ... Tokrb [SEP] Tokt1 ... Tokct [SEP]

                      Head Entity                              Relation                Tail Entity

            Figure 1: Illustrations of fine-tuning KG-BERT for predicting the plausibility of a triple.

Relation Label y  {1, . . . , R}                                    a special classification token [CLS]. The head entity is rep-
                                                                    resented as a sentence containing tokens Tok1h, ..., Tokha,
C  Th1      ...  Tha Tr[SEP] Tt1            ...  Tct Tt[SEP]        e.g., "Steven Paul Jobs was an American business mag-
                                                                    nate, entrepreneur and investor." or "Steve Jobs", the rela-
                 KG-BERT(b)                                         tTteioonkncbrei,scero.egnp.t,rae"isnfeoinnutgneddteoadks"e,natshseeTnottakei1tnl,cee.n..tc,iotTynotikasitcnr,eienp.grge.ts,oe"knAetenpdsplTaesoIkan1rcs,e..n.i.s-,
                                                                    an American multinational technology company headquar-
E[CLS] Eh1  ...  Eha    Er[SEP]        Et1  ...  Etc  E[tSEP]
                                                                    tered in Cupertino, California." or "Apple Inc.". The sen-
[CLS] Tokh1 ... Tokah [SEP] Tok1t ... Tokct [SEP]
                                                                    tences of entities and relations are separated by a special to-
   Head Entity                         Tail Entity
                                                                    ken [SEP]. For a given token, its input representation is con-
Figure 2: Illustrations of fine-tuning KG-BERT for predict-
ing the relation between two entities.                              structed by summing the corresponding token, segment and

knowledge graph completion. We represent entities and               position embeddings. Different elements separated by [SEP]
relations as their names or descriptions, then take the
name/description word sequences as the input sentence of            have different segment embeddings, the tokens in sentences
the BERT model for fine-tuning. As original BERT, a "sen-
tence" can be an arbitrary span of contiguous text or word          of head and tail entity share the same segment embedding
sequence, rather than an actual linguistic sentence. To model       eA, while the tokens in relation sentence have a different
the plausibility of a triple, we packed the sentences of            segment embedding eB. Different tokens in the same posi-
(h, r, t) as a single sequence. A sequence means the in-            tion i  {1, 2, 3, . . . , 512} have a same position embedding.
put token sequence to BERT, which may be two entity                 Each input token i has a input representation Ei. The token
name/description sentences or three sentences of (h, r, t)          representations are fed into the BERT model architecture
packed together.
                                                                    which is a multi-layer bidirectional Transformer encoder
   The architecture of the KG-BERT for modeling triples is
shown in Figure 1. We name this KG-BERT version KG-                 based on the original implementation described in (Vaswani
BERT(a). The first token of every input sequence is always
                                                                    et al. 2017). The final hidden vector of the special [CLS]
                                                                    token and i-th input token are denoted as C  RH and
                                                                    Ti  RH , where H is the hidden state size in pre-trained
                                                                    BERT. The final hidden state C corresponding to [CLS] is

                                                                    used as the aggregate sequence representation for comput-

                                                                    ing triple scores. The only new parameters introduced dur-

                                                                    ing triple classification fine-tuning are classification layer
                                                                    weights W  R2×H . The scoring function for a triple
                                                                     = (h, r, t) is s = f (h, r, t) = sigmoid(CW T ), s  R2
                                                                    is a 2-dimensional real vector with s0, s1  [0, 1] and
                                                                    s0 + s1 = 1. Given the positive triple set D+ and a neg-
                                                                    ative triple set D- constructed accordingly, we compute a
cross-entropy loss with s and triple labels:                      Dataset  # Ent   # Rel  # Train  # Dev   # Test
                                                                  WN11     38,696   11    112,581  2,609   10,544
L=-               (y log(s0) + (1 - y ) log(s1)) (1)               FB13    75,043   13    316,232  5,908   23,733
                                                                WN18RR     40,943   11    86,835   3,034   3,134
      D+D-                                                        FB15K    14,951         483,142  50,000  59,071
                                                                FB15k-237  14,541  1,345  272,115  17,535  20,466
where y  {0, 1} is the label (negative or positive) of that       UMLS              237    5,216    652
triple. The negative triple set D- is simply generated by                   135     46                      661
replacing head entity h or tail entity t in a positive triple
(h, r, t)  D+ with a random entity h or t , i.e.,               Table 1: Summary statistics of datasets.

D- = {(h , r, t)|h  E  h = h  (h , r, t) / D+}                  large lexical KG of English where each entity as a synset
        {(h, r, t )|t  E  t = t  (h, r, t ) / D+} (2)           which is consisting of several words and corresponds to a
                                                                distinct word sense. Freebase is a large knowledge graph of
where E is the set of entities. Note that a triple will not be  general world facts. UMLS is a medical semantic network
                                                                containing semantic types (entities) and semantic relations.
treated as a negative example if it is already in positive set  The test sets of WN11 and FB13 contain positive and neg-
D+. The pre-trained parameter weights and new weights W         ative triplets which can be used for triple classification. The
                                                                test set of WN18RR, FB15K, FB15k-237 and UMLS only
can be updated via gradient descent.                            contain correct triples, we perform link (entity) prediction
                                                                and relation prediction on these datasets. Table 1 provides
The architecture of the KG-BERT for predicting relations        statistics of all datasets we used.

is shown in Figure 2. We name this KG-BERT version KG-             For WN18RR, we use synsets definitions as entity sen-
                                                                tences. For WN11, FB15K and UMLS, we use entity names
BERT(b). We only use sentences of the two entities h and        as input sentences. For FB13, we use entity descriptions in
                                                                Wikipedia as input sentences. For FB15k-237, we used en-
t to predict the relation r between them. In our preliminary    tity descriptions made by (Xie et al. 2016). For all datasets,
                                                                we use relation names as relation sentences.
experiment, we found predicting relations with two entities
                                                                Baselines. We compare our KG-BERT with multiple
directly is better than using KG-BERT(a) with relation cor-     state-of-the-art KG embedding methods as follows: TransE
                                                                and its extensions TransH (Wang et al. 2014b), TransD (Ji
ruption, i.e., generating negative triples by replacing rela-   et al. 2015), TransR (Lin et al. 2015b), TransG (Xiao,
                                                                Huang, and Zhu 2016), TranSparse (Ji et al. 2016)
tion r with a random relation r . As KG-BERT(a), the final      and PTransE (Lin et al. 2015a), DistMult and its ex-
                                                                tension DistMult-HRS (Zhang et al. 2018) which only
hidden state C corresponding to [CLS] is used as the rep-       used structural information in KG. The neural tensor net-
                                                                work NTN (Socher et al. 2013) and its simplified ver-
resentation of the two entities. The only new parameters in-    sion ProjE (Shi and Weninger 2017). CNN models: Con-
                                                                vKB (Nguyen et al. 2018a), ConvE (Dettmers et al. 2018)
troduced in relation prediction fine-tuning are classification  and R-GCN (Schlichtkrull et al. 2018). KG embeddings
layer weights W  RR×H , where R is the number of rela-          with textual information: TEKE (Wang and Li 2016),
                                                                DKRL (Xie et al. 2016), SSP (Xiao et al. 2017), AATE (An
tions in a KG. The scoring function for a triple  = (h, r, t)   et al. 2018). KG embeddings with entity hierarchical types:
is s = f (h, r, t) = softmax(CW T ), s  RR is a R-              TKRL (Xie, Liu, and Sun 2016). Contextualized KG em-
                                              R                 beddings: DOLORES (Wang, Kulkarni, and Wang 2018).
dimensional real vector with si  [0, 1] and   i  s  i  =  1.    Complex-valued KG embeddings ComplEx (Trouillon et al.
                                                                2016) and RotatE (Sun et al. 2019). Adversarial learning
We compute the following cross-entropy loss with s  and         framework: KBGAN (Cai and Wang 2018).

relation labels:                                                Settings. We choose pre-trained BERT-Base model with
                                                                12 layers, 12 self-attention heads and H = 768 as the
                        R                                       initialization of KG-BERT, then fine tune KG-BERT with
                                                                Adam implemented in BERT. In our preliminary experi-
                  L =-           yi log(si)            (3)      ment, we found BERT-Base model can achieve better results
                                                                than BERT-Large in general, and BERT-Base is simpler and
                         D+ i=1                                 less sensitive to hyper-parameter choices. Following original
                                                                BERT, we set the following hyper-parameters in KG-BERT
where  is an observed positive triple, yi is the relation in-
dicator for the triple  , yi = 1 when r = i and yi = 0
when r = i.

                     Experiments

In this section we evaluate our KG-BERT on three experi-
mental tasks. Specifically we want to determine:

· Can our model judge whether an unseen triple fact
   (h, r, t) is true or not?

· Can our model predict an entity given another entity and
   a specific relation?

· Can our model predict relations given two entities?

Datasets. We ran our experiments on six widely used
benchmark KG datasets: WN11 (Socher et al. 2013),
FB13 (Socher et al. 2013), FB15K (Bordes et al. 2013),
WN18RR, FB15k-237 and UMLS (Dettmers et al. 2018).
WN11 and WN18RR are two subsets of WordNet, FB15K
and FB15k-237 are two subsets of Freebase. WordNet is a
fine-tuning: batch size: 32, learning rate: 5e-5, dropout rate:  0.90                                            0.90                                KG-BERT(a)
0.1. We also tried other values of these hyper-parameters        0.85                                            0.85                                TransE
in (Devlin et al. 2019) but didn't find much difference. We      0.80                                            0.80                                DistMult
tuned number of epochs for different tasks: 3 for triple clas-   0.75                                            0.75                                ComplEx
sification, 5 for link (entity) prediction and 20 for relation   0.70                                            0.70                                TransD
prediction. We found more epochs can lead to better results      0.65
in relation prediction but not in other two tasks. For triple    0.60                                KG-BERT(a)        0.05
classification training, we sample 1 negative triple for a pos-  0.55                                TransE
itive triple which can ensure class balance in binary classi-    0.50                                DistMult
fication. For link (entity) prediction training, we sample 5                                         ComplEx
negative triples for a positive triple, we tried 1, 3, 5 and 10        0.05                          TransD
and found 5 is the best.
                                                                             0.10  0.15  0.20  0.25  0.30                    0.10  0.15  0.20  0.25  0.30

                                                                             (a) WN11                                              (b) FB13

                                                                 Figure 3: Test accuracy of triple classification by varying
                                                                 training data proportions.

Method                              WN11 FB13 Avg.

NTN (Socher et al. 2013)            86.2 90.0 88.1               demonstrates the benefit of external text data. However, their
                                                                 improvements are still limited due to less utilization of rich
TransE (Wang et al. 2014b)          75.9 81.5 78.7               language patterns. The improvement of KG-BERT(a) over
                                                                 baselines on WN11 is larger than FB13, because WordNet
TransH (Wang et al. 2014b)          78.8 83.3 81.1               is a linguistic knowledge graph which is closer to linguistic
                                                                 patterns contained in pre-trained language models.
TransR (Lin et al. 2015b)           85.9 82.5 84.2
                                                                    Figure 3 reports triple classification accuracy with 5%,
TransD (Ji et al. 2015)             86.4 89.1 87.8               10%, 15%, 20% and 30% of original WN11 and FB13 train-
                                                                 ing triples. We note that KG-BERT(a) can achieve higher
TEKE (Wang and Li 2016)             86.1 84.2 85.2               test accuracy with limited training triples. For instance, KG-
                                                                 BERT(a) achieves a test accuracy of 88.1% on FB13 with
TransG (Xiao, Huang, and Zhu 2016)  87.4 87.3 87.4               only 5% training triples and a test accuracy of 87.0% on
                                                                 WN11 with only 10% training triples which are higher
TranSparse-S (Ji et al. 2016)       86.4 88.2 87.3               than some baseline models (including text-enhanced mod-
                                                                 els) with even the full training triples. These encouraging
DistMult (Zhang et al. 2018)        87.1 86.2 86.7               results suggest that KG-BERT(a) can fully utilize rich lin-
                                                                 guistic patterns in large external text data to overcome the
DistMult-HRS (Zhang et al. 2018)    88.9 89.0 89.0               sparseness of knowledge graphs.

AATE (An et al. 2018)               88.0 87.2 87.6                  The main reasons why KG-BERT(a) performs well are
                                                                 four fold: 1) The input sequence contains both entity and
ConvKB (Nguyen et al. 2018a)        87.6 88.8 88.2               relation word sequences; 2) The triple classification task is
                                                                 very similar to next sentence prediction task in BERT pre-
DOLORES (Wang, Kulkarni, and Wang 2018) 87.5 89.3 88.4           training which captures relationship between two sentences
                                                                 in large free text, thus the pre-trained BERT weights are well
KG-BERT(a)                          93.5 90.4 91.9               positioned for the inference of relationship among different
                                                                 elements in a triple; 3) The token hidden vectors are contex-
Table 2: Triple classification accuracy (in percentage) for      tual embeddings. The same token can have different hidden
different embedding methods. The baseline results are ob-        vectors in different triples, thus contextual information is ex-
tained from corresponding papers.                                plicitly used. 4) The self-attention mechanism can discover
                                                                 the most important words connected to the triple fact.
Triple Classification. Triple classification aims to judge
whether a given triple (h, r, t) is correct or not. Table 2      Link Prediction. The link (entity) prediction task predicts
presents triple classification accuracy of different methods     the head entity h given (?, r, t) or predicts the tail entity t
on WN11 and FB13. We can see that KG-BERT(a) clearly             given (h, r, ?) where ? means the missing element. The re-
outperforms all baselines by a large margin, which shows         sults are evaluated using a ranking produced by the scoring
the effectiveness of our method. We ran our models 10 times      function f (h, r, t) (s0 in our method) on test triples. Each
and found the standard deviations are less than 0.2, and the     correct test triple (h, r, t) is corrupted by replacing either its
improvements are significant (p < 0.01). To our knowl-           head or tail entity with every entity e  E, then these can-
edge, KG-BERT(a) achieves the best results so far. For more      didates are ranked in descending order of their plausibility
in-depth performance analysis, we note that TransE could         score. We report two common metrics, Mean Rank (MR)
not achieve high accuracy scores because it could not deal       of correct entities and Hits@10 which means the proportion
with 1-to-N, N-to-1, and N-to-N relations. TransH, TransR,       of correct entities in top 10. A lower MR is better while a
TransD, TranSparse and TransG outperform TransE by in-           higher Hits@10 is better. Following (Nguyen et al. 2018b),
troducing relation specific parameters. DistMult performs        we only report results under the filtered setting (Bordes et
relatively well, and can also be improved by hierarchical        al. 2013) which removes all corrupted triples appeared in
relation structure information used in DistMult-HRS. Con-
vKB shows decent results, which suggests that CNN models
can capture global interactions among the entity and relation
embeddings. DOLORES further improves ConvKB by in-
corporating contextual information in entity-relation random
walk chains. NTN also achieves competitive performances
especially on FB13, which means it's an expressive model,
and representing entities with word embeddings is helpful.
Other text-enhanced KG embeddings TEKE and AATE out-
perform their base models like TransE and TransH, which
Method                                                              WN18RR  FB15k-237      UMLS

TransE (our results)                                     MR Hits@10         MR Hits@10  MR Hits@10
TransH (our results)
TransR (our results)                                     2365 50.5          223 47.4    1.84 98.9
TransD (our results)
DistMult (our results)                                   2524 50.3          255 48.6    1.80 99.5
ComplEx (our results)
ConvE (Dettmers et al. 2018)                             3166 50.7          237 51.1    1.81 99.4
ConvKB (Nguyen et al. 2018a)
R-GCN (Schlichtkrull et al. 2018)                        2768 50.7          246 48.4    1.71 99.3
KBGAN (Cai and Wang 2018)
RotatE (Sun et al. 2019)                                 3704 47.7          411 41.9    5.52 84.6
KG-BERT(a)
                                                         3921 48.3          508 43.4    2.59 96.7

                                                         5277 48            246 49.1    ­  ­

                                                         2554 52.5          257 51.7    ­  ­

                                                                 ­  ­       ­  41.7     ­  ­

                                                                 ­  48.1    ­  45.8     ­  ­

                                                         3340 57.1          177 53.3    ­  ­

                                                         97         52.4    153 42.0    1.47 99.0

Table 3: Link prediction results on WN18RR, FB15k-237 and UMLS datasets. The baseline models denoted (our results) are
implemented using OpenKE toolkit (Han et al. 2018), other baseline results are taken from the original papers.

Method                                        Mean Rank  Hits@1     graph convolutional network R-GCN. ComplEx could not
TransE (Lin et al. 2015a)                          2.5     84.3     perform well on WN18RR and FB15k-237, but can be im-
TransR (Xie, Liu, and Sun 2016)                    2.1     91.6     proved using adversarial negative sampling in KBGAN and
DKRL (CNN) (Xie et al. 2016)                       2.5     89.0     RotatE.
DKRL (CNN) + TransE (Xie et al. 2016)              2.0     90.8
DKRL (CBOW) (Xie et al. 2016)                      2.5     82.7     Relation Prediction. This task predicts relations between
TKRL (RHE) (Xie, Liu, and Sun 2016)                1.7     92.8     two given entities, i.e., (h, ?, t). The procedure is similar to
TKRL (RHE) (Xie, Liu, and Sun 2016)                1.8     92.5     link prediction while we rank the candidates with the rela-
PTransE (ADD, len-2 path) (Lin et al. 2015a)       1.2     93.6     tion scores s . We evaluate the relation ranking using Mean
PTransE (RNN, len-2 path) (Lin et al. 2015a)       1.4     93.2     Rank (MR) and Hits@1 with filtered setting.
PTransE (ADD, len-3 path) (Lin et al. 2015a)       1.4     94.0
SSP (Xiao et al. 2017)                             1.2      ­          Table 4 reports relation prediction results on FB15K.
ProjE (pointwise) (Shi and Weninger 2017)          1.3     95.6     We note that KG-BERT(b) also shows promising results
ProjE (listwise) (Shi and Weninger 2017)           1.2     95.7     and achieves the highest Hits@1 so far. The KG-BERT(b)
ProjE (wlistwise) (Shi and Weninger 2017)          1.2     95.6     is analogous to sentence pair classification in BERT fine-
KG-BERT (b)                                        1.2     96.0     tuning and can also benefit from BERT pre-training. Text-
                                                                    enhanced models DKRL and SSP can also outperform struc-
Table 4: Relation prediction results on FB15K dataset. The          ture only methods TransE and TransH. TKRL and PTransE
baseline results are obtained from corresponding papers.            work well with hierarchical entity categories and extended
                                                                    path information. ProjE achieves very competitive results by
training, development, and test set before getting the rank-        treating KG completion as a ranking problem and optimiz-
ing lists.                                                          ing ranking score vectors.

   Table 3 shows link prediction performance of vari-               Attention Visualization. We show attention patterns of
ous models. We test some classical baseline models with             KG-BERT in Figure 4 and Figure 5. We use the visualiza-
OpenKE toolkit (Han et al. 2018)1, other results are taken          tion tool released by (Vig 2019)2. Figure 4 depicts the at-
from the original papers. We can observe that: 1) KG-               tention patterns of KG-BERT(a). A positive training triple
BERT(a) can achieve lower MR than baseline models, and it           ( twenty dollar bill NN 1, hypernym, note NN 6)
achieves the lowest mean ranks on WN18RR and FB15k-                 from WN18RR is taken as the example. The entity descrip-
237 to our knowledge. 2) The Hits@10 scores of KG-                  tions "a United States bill worth 20 dollars" and "a piece of
BERT(a) is lower than some state-of-the-art methods. KG-            paper money" as well as the relation name "hypernym" are
BERT(a) can avoid very high ranks with semantic related-            used as the input sequence. We observe that some important
ness of entity and relation sentences, but the KG structure         words such as "paper" and "money" have higher attention
information is not explicitly modeled, thus it could not rank       scores connected to the label token [CLS], while some less
some neighbor entities of a given entity in top 10. CNN             related words like "united" and "states" obtain less atten-
models ConvE and ConvKB perform better compared to the              tions. On the other hand, we can see that different attention

    1https://github.com/thunlp/OpenKE                                   2https://github.com/jessevig/bertviz
Figure 4: Illustrations of attention patterns of KG-BERT(a).      Figure 5: Illustrations of attention patterns of KG-BERT(b).
A positive training triple ( twenty dollar bill NN 1,             The example is taken from FB15K. Two entities 20th cen-
 hypernym, note NN 6) from WN18RR is used as the                  tury and World War II are used as input, the relation label is
example. Different colors mean different attention heads.         /time/event/includes event.
Transparencies of colors reflect the attention scores. We
show the attention weights between [CLS] and other tokens                    Conclusion and Future Work
in layer 11 of the Transformer model.
                                                                  In this work, we propose a novel knowledge graph comple-
heads focus on different tokens. [SEP] is highlighted by the      tion method termed Knowledge Graph BERT (KG-BERT).
same six attention heads, "a" and "piece" are highlighted by      We represent entities and relations as their name/description
the three same attention heads, while "paper" and "money"         textual sequences, and turn knowledge graph completion
are highlighted by other four attention heads. As mentioned       problem into a sequence classification problem. KG-BERT
in (Vaswani et al. 2017), multi-head attention allows KG-         can make use of rich language information in large amount
BERT to jointly attend to information from different repre-       free text and highlight most important words connected to a
sentation subspaces at different positions, different attention   triple. The proposed method demonstrates promising results
heads are concatenated to compute the final attention values.     by outperforming state-of-the-art results on multiple bench-
Figure 5 illustrates attention patterns of KG-BERT(b). The        mark KG datasets.
triple (20th century, /time/event/includes event, World War
II) from FB15K is taken as input. We can see similar atten-          Some future directions include improving the results by
tion patterns as in KG-BERT(a), six attention heads attend        jointly modeling textual information with KG structures, or
to "century" in head entity, while other three attention heads    utilizing pre-trained models with more text data like XLNet.
focus on "war" and "ii" in tail entity. Multi-head attention      And applying our KG-BERT as a knowledge-enhanced lan-
can attend to different aspects of two entities in a triple.      guage model to language understanding tasks is an interest-
                                                                  ing future work we are going to explore.
Discussions. From experimental results, we note that KG-
BERT can achieve strong performance in three KG comple-                                  References
tion tasks. However, a major limitation is that BERT model
is expensive, which makes the link prediction evaluation          An, B.; Chen, B.; Han, X.; and Sun, L. 2018. Accurate
very time consuming, link prediction evaluation needs to re-      text-enhanced knowledge graph representation learning. In
place head or tail entity with almost all entities, and all cor-  NAACL, 745­755.
rupted triple sequences are fed into the 12 layer Transformer
model. Possible solutions are introducing 1-N scoring mod-        Bollacker, K.; Evans, C.; Paritosh, P.; Sturge, T.; and Taylor,
els like ConvE or using lightweight language models.              J. 2008. Freebase: a collaboratively created graph database
                                                                  for structuring human knowledge. In SIGMOD, 1247­1250.

                                                                  Bordes, A.; Usunier, N.; Garcia-Duran, A.; Weston, J.; and
                                                                  Yakhnenko, O. 2013. Translating embeddings for modeling
                                                                  multi-relational data. In NIPS, 2787­2795.

                                                                  Bosselut, A.; Rashkin, H.; Sap, M.; Malaviya, C.; Celiky-
                                                                  ilmaz, A.; and Choi, Y. 2019. COMET: Commonsense
                                                                  transformers for automatic knowledge graph construction.
                                                                  In ACL, 4762­4779.

                                                                  Cai, L., and Wang, W. Y. 2018. KBGAN: Adversarial learn-
                                                                  ing for knowledge graph embeddings. In NAACL, 1470­
                                                                  1480.

                                                                  Cui, W.; Xiao, Y.; Wang, H.; Song, Y.; Hwang, S.-w.; and
                                                                  Wang, W. 2017. KBQA: learning question answering over
                                                                  qa corpora and knowledge bases. Proceedings of the VLDB
                                                                  Endowment 10(5):565­576.
Dettmers, T.; Minervini, P.; Stenetorp, P.; and Riedel, S.       Sun, Z.; Deng, Z.-H.; Nie, J.-Y.; and Tang, J. 2019. Ro-
2018. Convolutional 2d knowledge graph embeddings. In            tate: Knowledge graph embedding by relational rotation in
AAAI, 1811­1818.                                                 complex space. In ICLR.
                                                                 Trouillon, T.; Welbl, J.; Riedel, S.; Gaussier, E´ .; and
Devlin, J.; Chang, M.-W.; Lee, K.; and Toutanova, K. 2019.       Bouchard, G. 2016. Complex embeddings for simple link
Bert: Pre-training of deep bidirectional transformers for lan-   prediction. In ICML, 2071­2080.
guage understanding. In NAACL, 4171­4186.
                                                                 Vaswani, A.; Shazeer, N.; Parmar, N.; Uszkoreit, J.; Jones,
Han, X.; Cao, S.; Lv, X.; Lin, Y.; Liu, Z.; Sun, M.; and Li, J.  L.; Gomez, A. N.; Kaiser, L.; and Polosukhin, I. 2017. At-
2018. OpenKE: An open toolkit for knowledge embedding.           tention is all you need. In NIPS, 5998­6008.
In EMNLP, 139­144.
                                                                 Vig, J. 2019. A multiscale visualization of attention in the
Ji, G.; He, S.; Xu, L.; Liu, K.; and Zhao, J. 2015. Knowledge    transformer model. arXiv preprint arXiv:1906.05714.
graph embedding via dynamic mapping matrix. In ACL,
687­696.                                                         Wang, Z., and Li, J.-Z. 2016. Text-enhanced representation
                                                                 learning for knowledge graph. In IJCAI, 1293­1299.
Ji, G.; Liu, K.; He, S.; and Zhao, J. 2016. Knowledge graph
completion with adaptive sparse transfer matrix. In AAAI.        Wang, Z.; Zhang, J.; Feng, J.; and Chen, Z. 2014a. Knowl-
                                                                 edge graph and text jointly embedding. In EMNLP.
Lin, Y.; Liu, Z.; Luan, H.; Sun, M.; Rao, S.; and Liu, S.
2015a. Modeling relation paths for representation learning       Wang, Z.; Zhang, J.; Feng, J.; and Chen, Z. 2014b. Knowl-
of knowledge bases. In EMNLP, 705­714.                           edge graph embedding by translating on hyperplanes. In
                                                                 AAAI.
Lin, Y.; Liu, Z.; Sun, M.; Liu, Y.; and Zhu, X. 2015b. Learn-
ing entity and relation embeddings for knowledge graph           Wang, Q.; Mao, Z.; Wang, B.; and Guo, L. 2017. Knowledge
completion. In AAAI.                                             graph embedding: A survey of approaches and applications.
                                                                 IEEE TKDE 29(12):2724­2743.
Mikolov, T.; Sutskever, I.; Chen, K.; Corrado, G. S.; and
Dean, J. 2013. Distributed representations of words and          Wang, H.; Kulkarni, V.; and Wang, W. Y. 2018. Dolores:
phrases and their compositionality. In NIPS, 3111­3119.          Deep contextualized knowledge graph embeddings. arXiv
                                                                 preprint arXiv:1811.00147.
Miller, G. A. 1995. Wordnet: a lexical database for english.
Communications of the ACM 38(11):39­41.                          Xiao, H.; Huang, M.; Meng, L.; and Zhu, X. 2017. SSP:
                                                                 semantic space projection for knowledge graph embedding
Nguyen, D. Q.; Nguyen, D. Q.; Nguyen, T. D.; and Phung,          with text descriptions. In AAAI.
D. 2018a. A convolutional neural network-based model
for knowledge base completion and its application to search      Xiao, H.; Huang, M.; and Zhu, X. 2016. TransG: A gener-
personalization. Semantic Web.                                   ative model for knowledge graph embedding. In ACL, vol-
                                                                 ume 1, 2316­2325.
Nguyen, D. Q.; Nguyen, T. D.; Nguyen, D. Q.; and Phung,
D. 2018b. A novel embedding model for knowledge                  Xie, R.; Liu, Z.; Jia, J.; Luan, H.; and Sun, M. 2016. Repre-
base completion based on convolutional neural network. In        sentation learning of knowledge graphs with entity descrip-
NAACL, 327­333.                                                  tions. In AAAI.

Nickel, M.; Tresp, V.; and Kriegel, H.-P. 2011. A three-         Xie, R.; Liu, Z.; and Sun, M. 2016. Representation learn-
way model for collective learning on multi-relational data.      ing of knowledge graphs with hierarchical types. In IJCAI,
In ICML, 809­816.                                                2965­2971.

Pennington, J.; Socher, R.; and Manning, C. 2014. Glove:         Xu, J.; Qiu, X.; Chen, K.; and Huang, X. 2017. Knowl-
Global vectors for word representation. In EMNLP.                edge graph representation with jointly structural and textual
                                                                 encoding. In IJCAI, 1318­1324.
Peters, M. E.; Neumann, M.; Iyyer, M.; Gardner, M.; Clark,
C.; Lee, K.; and Zettlemoyer, L. 2018. Deep contextualized       Yang, B.; Yih, W.-t.; He, X.; Gao, J.; and Deng, L. 2015.
word representations. In NAACL, 2227­2237.                       Embedding entities and relations for learning and inference
                                                                 in knowledge bases. In ICLR.
Radford, A.; Narasimhan, K.; Salimans, T.; and Sutskever,
I. 2018. Improving language understanding by generative          Yang, Z.; Dai, Z.; Yang, Y.; Carbonell, J.; Salakhutdinov,
pre-training.                                                    R.; and Le, Q. V. 2019. XLNet: Generalized autoregres-
                                                                 sive pretraining for language understanding. arXiv preprint
Schlichtkrull, M.; Kipf, T. N.; Bloem, P.; Van Den Berg, R.;     arXiv:1906.08237.
Titov, I.; and Welling, M. 2018. Modeling relational data
with graph convolutional networks. In ESWC, 593­607.             Zhang, F.; Yuan, N. J.; Lian, D.; Xie, X.; and Ma, W.-Y.
                                                                 2016. Collaborative knowledge base embedding for recom-
Shi, B., and Weninger, T. 2017. ProjE: Embedding projec-         mender systems. In KDD, 353­362. ACM.
tion for knowledge graph completion. In AAAI.
                                                                 Zhang, Z.; Zhuang, F.; Qu, M.; Lin, F.; and He, Q. 2018.
Socher, R.; Chen, D.; Manning, C. D.; and Ng, A. 2013.           Knowledge graph embedding with hierarchical relation
Reasoning with neural tensor networks for knowledge base         structure. In EMNLP, 3198­3207.
completion. In NIPS, 926­934.
                                                                 Zhang, Z.; Han, X.; Liu, Z.; Jiang, X.; Sun, M.; and Liu,
Suchanek, F. M.; Kasneci, G.; and Weikum, G. 2007. Yago:         Q. 2019. ERNIE: Enhanced language representation with
a core of semantic knowledge. In WWW, 697­706. ACM.              informative entities. In ACL, 1441­1451.
