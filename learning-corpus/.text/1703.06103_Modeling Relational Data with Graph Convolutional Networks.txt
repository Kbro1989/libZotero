                                          Modeling Relational Data with Graph Convolutional Networks

                                             Michael Schlichtkrull        Thomas N. Kipf                                                 Peter Bloem

                                                University of Amsterdam  University of Amsterdam                                          VU Amsterdam
                                          m.s.schlichtkrull@uva.nl        t.n.kipf@uva.nl                                               p.bloem@vu.nl

                                          Rianne van den Berg                  Ivan Titov                                               Max Welling

                                           University of Amsterdam       University of Amsterdam                               University of Amsterdam, CIFAR
                                          r.vandenberg@uva.nl                titov@uva.nl
                                                                                                                                     m.welling@uva.nl

arXiv:1703.06103v4 [stat.ML] 26 Oct 2017                                Abstract                                                        :country

                                             Knowledge graphs enable a wide variety of applications, in-                                                     U.S.A.        :university
                                             cluding question answering and information retrieval. De-                                                               Vaganova Academy
                                             spite the great effort invested in their creation and mainte-                          citizen_of
                                             nance, even the largest (e.g., Yago, DBPedia or Wikidata)                                           educated_at
                                             remain incomplete. We introduce Relational Graph Convo-
                                             lutional Networks (R-GCNs) and apply them to two standard         Mikhail Baryshnikov
                                             knowledge base completion tasks: Link prediction (recovery
                                             of missing facts, i.e. subject-predicate-object triples) and en-  :ballet_dancer  awarded
                                             tity classification (recovery of missing entity attributes). R-
                                             GCNs are related to a recent class of neural networks operat-                                 :award
                                             ing on graphs, and are developed specifically to deal with the                             Vilcek prize
                                             highly multi-relational data characteristic of realistic knowl-
                                             edge bases. We demonstrate the effectiveness of R-GCNs as         Figure 1: A knowledge base fragment: The nodes are en-
                                             a stand-alone model for entity classification. We further show    tities, the edges are relations labeled with their types, the
                                             that factorization models for link prediction such as DistMult    nodes are labeled with entity types (e.g., university). The
                                             can be significantly improved by enriching them with an en-       edge and the node label shown in red are the missing in-
                                             coder model to accumulate evidence over multiple inference        formation to be inferred.
                                             steps in the relational graph, demonstrating a large improve-
                                             ment of 29.8% on FB15k-237 over a decoder-only baseline.          Academy is marked as a university). It is convenient to rep-
                                                                                                               resent knowledge bases as directed labeled multigraphs with
                                                             1 Introduction                                    entities corresponding to nodes and triples encoded by la-
                                                                                                               beled edges (see Figure 1).
                                          Knowledge bases organize and store factual knowledge, en-
                                          abling a multitude of applications including question an-               We consider two fundamental SRL tasks: link predic-
                                          swering (Yao and Van Durme 2014; Bao et al. 2014; Seyler,            tion (recovery of missing triples) and entity classification
                                          Yahya, and Berberich 2015; Hixon, Clark, and Hajishirzi              (assigning types or categorical properties to entities). In
                                          2015; Bordes et al. 2015; Dong et al. 2015) and informa-             both cases, many missing pieces of information can be ex-
                                          tion retrieval (Kotov and Zhai 2012; Dalton, Dietz, and Al-          pected to reside within the graph encoded through the neigh-
                                          lan 2014; Xiong and Callan 2015b; 2015a). Even the largest           borhood structure ­ i.e. knowing that Mikhail Baryshnikov
                                          knowledge bases (e.g. DBPedia, Wikidata or Yago), despite            was educated at the Vaganova Academy implies both that
                                          enormous effort invested in their maintenance, are incom-            Mikhail Baryshnikov should have the label person, and that
                                          plete, and the lack of coverage harms downstream applica-            the triple (Mikhail Baryshnikov, lived in, Russia) must be-
                                          tions. Predicting missing information in knowledge bases is          long to the knowledge graph. Following this intuition, we
                                          the main focus of statistical relational learning (SRL).             develop an encoder model for entities in the relational graph
                                                                                                               and apply it to both tasks.
                                             Following previous work on SRL, we assume that knowl-
                                          edge bases store collections of triples of the form (subject,           Our entity classification model, similarly to Kipf and
                                          predicate, object). Consider, for example, the triple (Mikhail       Welling (2017), uses softmax classifiers at each node in the
                                          Baryshnikov, educated at, Vaganova Academy), where we                graph. The classifiers take node representations supplied by
                                          will refer to Baryshnikov and Vaganova Academy as enti-              a relational graph convolutional network (R-GCN) and pre-
                                          ties and to educated at as a relation. Additionally, we as-          dict the labels. The model, including R-GCN parameters, is
                                          sume that entities are labeled with types (e.g., Vaganova            learned by optimizing the cross-entropy loss.

                                             Equal contribution.                                                  Our link prediction model can be regarded as an autoen-
                                              Canadian Institute for Advanced Research                         coder consisting of (1) an encoder: an R-GCN producing
                                                                                                               latent feature representations of entities, and (2) a decoder:
                                                                                                               a tensor factorization model exploiting these representations
to predict labeled edges. Though in principle the decoder can            This type of transformation has been shown to be very
rely on any type of factorization (or generally any scoring           effective at accumulating and encoding features from lo-
function), we use one of the simplest and most effective fac-         cal, structured neighborhoods, and has led to significant im-
torization methods: DistMult (Yang et al. 2014). We observe           provements in areas such as graph classification (Duvenaud
that our method achieves competitive results on standard              et al. 2015) and graph-based semi-supervised learning (Kipf
benchmarks, outperforming, among other baselines, direct              and Welling 2017).
optimization of the factorization (i.e. vanilla DistMult). This
improvement is especially large when we consider the more                Motivated by these architectures, we define the following
challenging FB15k-237 dataset (Toutanova and Chen 2015).              simple propagation model for calculating the forward-pass
This result demonstrates that explicit modeling of neighbor-          update of an entity or node denoted by vi in a relational (di-
hoods in R-GCNs is beneficial for recovering missing facts            rected and labeled) multi-graph:
in knowledge bases.
                                                                                                                       
   Our main contributions are as follows. To the best of our
knowledge, we are the first to show that the GCN frame-               h(il+1)  =                   1    Wr(l)h(jl)  +  W0(l)hi(l) ,  (2)
work can be applied to modeling relational data, specifically                                     ci,r
to link prediction and entity classification tasks. Secondly,                            rR jNir
we introduce techniques for parameter sharing and to en-
force sparsity constraints, and use them to apply R-GCNs              where Nir denotes the set of neighbor indices of node i un-
to multigraphs with large numbers of relations. Lastly, we            der relation r  R. ci,r is a problem-specific normaliza-
show that the performance of factorization models, at the             tion constant that can either be learned or chosen in advance
example of DistMult, can be significantly improved by en-             (such as ci,r = |Nir|).
riching them with an encoder model that performs multiple
steps of information propagation in the relational graph.                Intuitively, (2) accumulates transformed feature vectors
                                                                      of neighboring nodes through a normalized sum. Different
          2 Neural relational modeling                                from regular GCNs, we introduce relation-specific transfor-
                                                                      mations, i.e. depending on the type and direction of an edge.
We introduce the following notation: we denote directed and           To ensure that the representation of a node at layer l + 1
labeled multi-graphs as G = (V, E, R) with nodes (entities)           can also be informed by the corresponding representation at
vi  V and labeled edges (relations) (vi, r, vj)  E, where             layer l, we add a single self-connection of a special relation
r  R is a relation type.1                                             type to each node in the data. Note that instead of simple lin-
                                                                      ear message transformations, one could choose more flexi-
2.1 Relational graph convolutional networks                           ble functions such as multi-layer neural networks (at the ex-
                                                                      pense of computational efficiency). We leave this for future
Our model is primarily motivated as an extension of GCNs              work.
that operate on local graph neighborhoods (Duvenaud et al.
2015; Kipf and Welling 2017) to large-scale relational data.             A neural network layer update consists of evaluating (2)
These and related methods such as graph neural networks               in parallel for every node in the graph. In practice, (2) can be
(Scarselli et al. 2009) can be understood as special cases of         implemented efficiently using sparse matrix multiplications
a simple differentiable message-passing framework (Gilmer             to avoid explicit summation over neighborhoods. Multiple
et al. 2017):                                                         layers can be stacked to allow for dependencies across sev-
                                                                      eral relational steps. We refer to this graph encoder model
                                                                      as a relational graph convolutional network (R-GCN). The
                                                                      computation graph for a single node update in the R-GCN
                                                                      model is depicted in Figure 2.

hi(l+1) =        gm(hi(l), hj(l)) ,  (1)                              2.2 Regularization

            mMi                                                       A central issue with applying (2) to highly multi-relational
                                                                      data is the rapid growth in number of parameters with the
where h(il)  Rd(l) is the hidden state of node vi in the              number of relations in the graph. In practice this can easily
l-th layer of the neural network, with d(l) being the di-             lead to overfitting on rare relations and to models of very
                                                                      large size.
mensionality of this layer's representations. Incoming mes-
                                                                         To address this issue, we introduce two separate meth-
sages of the form gm(·, ·) are accumulated and passed                 ods for regularizing the weights of R-GCN-layers: basis-
through an element-wise activation function (·), such as              and block-diagonal-decomposition. With the basis decom-
the ReLU(·) = max(0, ·).2 Mi denotes the set of incoming              position, each Wr(l) is defined as follows:
messages for node vi and is often chosen to be identical to
the set of incoming edges. gm(·, ·) is typically chosen to be                                     B
a (message-specific) neural network-like function or simply
                                                                                    Wr(l) =             ar(lb)Vb(l),                 (3)
a linear transformation gm(hi, hj) = W hj with a weight
matrix W such as in Kipf and Welling (2017).

    1R contains relations both in canonical direction (e.g. born in)                              b=1
and in inverse direction (e.g. born in inv).
                                                                      i.e. as a linear combination of basis transformations Vb(l) 
    2Note that this represents a simplification of the message pass-  Rd(l+1)×d(l) with coefficients a(rlb) such that only the coeffi-
ing neural network proposed in (Gilmer et al. 2017) that suffices to  cients depend on r. In the block-diagonal decomposition, we
include the aforementioned models as special cases.
rel_1 (in)                         rel_1

rel_1 (out)                                                                                                                                            Edge loss
                                                                                                                                         DistMult
              ...                                                                                                           R-GCN

rel_N (in)                                                                                                    Input
                                                                                              Node loss
rel_N (out)
                                                                                R-GCN
self-loop                                                          Input

                          + ReLU                                              encoder             encoder decoder

                                    rel_N                          (a) Entity classification     (b) Link prediction

                                  self-loop                        Figure 3: (a) Depiction of an R-GCN model for entity clas-
                                                                   sification with a per-node loss function. (b) Link predic-
                                                                   tion model with an R-GCN encoder (interspersed with fully-
                                                                   connected/dense layers) and a DistMult decoder that takes
                                                                   pairs of hidden node representations and produces a score
                                                                   for every (potential) edge in the graph. The loss is evaluated
                                                                   per edge.

Figure 2: Diagram for computing the update of a single             we only consider such a featureless approach in this work,
graph node/entity (red) in the R-GCN model. Activations            we note that it was shown in Kipf and Welling (2017) that
(d-dimensional vectors) from neighboring nodes (dark blue)         it is possible for this class of models to make use of pre-
are gathered and then transformed for each relation type           defined feature vectors (e.g. a bag-of-words description of a
individually (for both in- and outgoing edges). The result-        document associated with a specific node).
ing representation (green) is accumulated in a (normalized)
sum and passed through an activation function (such as the                        3 Entity classification
ReLU). This per-node update can be computed in parallel
with shared parameters across the whole graph.                     For (semi-)supervised classification of nodes (entities),
                                                                   we simply stack R-GCN layers of the form (2), with a
                                                                   softmax(·) activation (per node) on the output of the last
                                                                   layer. We minimize the following cross-entropy loss on all
                                                                   labeled nodes (while ignoring unlabeled nodes):

let each Wr(l) be defined through the direct sum over a set of                                K
low-dimensional matrices:
                                                                   L=-                           tik ln hi(kL) ,                                                  (5)

                     B                                                  iY k=1

            Wr(l) =       Q(blr).            (4)                   where Y is the set of node indices that have labels and h(ikL)
                                                                   is the k-th entry of the network output for the i-th labeled
                     b=1                                           node. tik denotes its respective ground truth label. In prac-
                                                                   tice, we train the model using (full-batch) gradient descent
Thereby, Wr(l) are block-diagonal matrices:                        techniques. A schematic depiction of our entity classifica-
diag(Q(1lr), . . . , Q(Bl)r) with Q(blr)  R . (d(l+1)/B)×(d(l)/B)  tion model is given in Figure 3a.

   The basis function decomposition (3) can be seen as a                            4 Link prediction
form of effective weight sharing between different relation
types, while the block decomposition (4) can be seen as a          Link prediction deals with prediction of new facts (i.e.
sparsity constraint on the weight matrices for each relation       triples (subject, relation, object)). Formally, the knowledge
type. The block decomposition structure encodes an intu-           base is represented by a directed, labeled graph G =
ition that latent features can be grouped into sets of variables   (V, E, R). Rather than the full set of edges E, we are given
which are more tightly coupled within groups than across           only an incomplete subset E^. The task is to assign scores
groups. Both decompositions reduce the number of parame-           f (s, r, o) to possible edges (s, r, o) in order to determine
ters needed to learn for highly multi-relational data (such as     how likely those edges are to belong to E.
realistic knowledge bases). At the same time, we expect that
the basis parameterization can alleviate overfitting on rare          In order to tackle this problem, we introduce a graph
relations, as parameter updates are shared between both rare       auto-encoder model, comprised of an entity encoder and a
and more frequent relations.                                       scoring function (decoder). The encoder maps each entity
                                                                   vi  V to a real-valued vector ei  Rd. The decoder re-
   The overall R-GCN model then takes the following form:          constructs edges of the graph relying on the vertex repre-
We stack L layers as defined in (2) ­ the output of the previ-     sentations; in other words, it scores (subject, relation, ob-
ous layer being the input to the next layer. The input to the      ject)-triples through a function s : Rd × R × Rd  R.
first layer can be chosen as a unique one-hot vector for each      Most existing approaches to link prediction (for example,
node in the graph if no other features are present. For the        tensor and neural factorization methods (Socher et al. 2013;
block representation, we map this one-hot vector to a dense
representation through a single linear transformation. While
Lin et al. 2015; Toutanova et al. 2016; Yang et al. 2014;         Dataset     AIFB    MUTAG          BGS            AM

Trouillon et al. 2016)) can be interpreted under this frame-      Entities    8,285     23,644    333,845    1,666,764
                                                                  Relations       45         23        103          133
work. The crucial distinguishing characteristic of our work       Edges
                                                                  Labeled    29,043     74,227    916,199    5,988,321
is the reliance on an encoder. Whereas most previous ap-          Classes       176         340        146        1,000
proaches use a single, real-valued vector ei for every vi                          4           2          2           11
V optimized directly in training, we compute representa-
tions through an R-GCN encoder with ei = hi(L), simi-             Table 1: Number of entities, relations, edges and classes
lar to the graph auto-encoder model introduced in Kipf and        along with the number of labeled entities for each of the
                                                                  datasets. Labeled denotes the subset of entities that have la-
Welling (2016) for unlabeled undirected graphs. Our full          bels and that are to be classified.

link prediction model is schematically depicted in Figure 3b.     Baselines As a baseline for our experiments, we com-
                                                                  pare against recent state-of-the-art classification results
   In our experiments, we use the DistMult factoriza-             from RDF2Vec embeddings (Ristoski and Paulheim 2016),
                                                                  Weisfeiler-Lehman kernels (WL) (Shervashidze et al. 2011;
tion (Yang et al. 2014) as the scoring function, which is         de Vries and de Rooij 2015), and hand-designed feature ex-
                                                                  tractors (Feat) (Paulheim and Fu¨mkranz 2012). Feat assem-
known to perform well on standard link prediction bench-          bles a feature vector from the in- and out-degree (per re-
marks when used on its own. In DistMult, every relation r         lation) of every labeled entity. RDF2Vec extracts walks on
is associated with a diagonal matrix Rr  Rd×d and a triple        labeled graphs which are then processed using the Skipgram
(s, r, o) is scored as                                            (Mikolov et al. 2013) model to generate entity embeddings,
                                                                  used for subsequent classification. See Ristoski and Paul-
                  f (s, r, o) = esT Rreo .                  (6)   heim (2016) for an in-depth description and discussion of
                                                                  these baseline approaches. All entity classification experi-
   As in previous work on factorization (Yang et al. 2014;        ments were run on CPU nodes with 64GB of memory.
Trouillon et al. 2016), we train the model with negative
sampling. For each observed example we sample  nega-
tive ones. We sample by randomly corrupting either the sub-
ject or the object of each positive example. We optimize
for cross-entropy loss to push the model to score observable
triples higher than the negative ones:

L  =  -        1                  y log l   f (s, r, o)  +
        (1
            +  )|E^|  (s,r,o,y)T                            (7)

                  (1 - y) log 1 - l f (s, r, o) ,                 Results All results in Table 2 are reported on the
                                                                  train/test benchmark splits from Ristoski, de Vries, and
where T is the total set of real and corrupted triples, l is the  Paulheim (2016). We further set aside 20% of the training set
logistic sigmoid function, and y is an indicator set to y = 1     as a validation set for hyperparameter tuning. For R-GCN,
for positive triples and y = 0 for negative ones.                 we report performance of a 2-layer model with 16 hidden
                                                                  units (10 for AM), basis function decomposition (Eq. 3), and
              5 Empirical evaluation                              trained with Adam (Kingma and Ba 2014) for 50 epochs us-
                                                                  ing a learning rate of 0.01. The normalization constant is
5.1 Entity classification experiments                             chosen as ci,r = |Nir|. Further details on (baseline) models
                                                                  and hyperparameter choices are provided in the supplemen-
Here, we consider the task of classifying entities in a knowl-    tary material.
edge base. In order to infer, for example, the type of an entity
(e.g. person or company), a successful model needs to rea-
son about the relations with other entities that this entity is
involved in.

Datasets We evaluate our model on four datasets3 in Re-           Model      AIFB     MUTAG       BGS         AM
source Description Framework (RDF) format (Ristoski, de
Vries, and Paulheim 2016): AIFB, MUTAG, BGS, and AM.              Feat       55.55     77.94      72.41      66.66
Relations in these datasets need not necessarily encode di-       WL         80.55     80.88      86.20      87.37
rected subject-object relations, but are also used to encode      RDF2Vec    88.88     67.20      87.24      88.33
the presence, or absence, of a specific feature for a given       R-GCN      95.83     73.23      83.10      89.29
entity. In each dataset, the targets to be classified are prop-
erties of a group of entities represented as nodes. The exact     Table 2: Entity classification results in accuracy (averaged
statistics of the datasets can be found in Table 1. For a more    over 10 runs) for a feature-based baseline (see main text
detailed description of the datasets the reader is referred to    for details), WL (Shervashidze et al. 2011; de Vries and
Ristoski, de Vries, and Paulheim (2016). We remove rela-          de Rooij 2015), RDF2Vec (Ristoski and Paulheim 2016),
tions that were used to create entity labels: employs and affil-  and R-GCN (this work). Test performance is reported on the
iation for AIFB, isMutagenic for MUTAG, hasLithogenesis           train/test set splits provided by Ristoski, de Vries, and Paul-
for BGS, and objectCategory and material for AM.                  heim (2016).

    3http://dws.informatik.uni-mannheim.de/en/research/a-            Our model achieves state-of-the-art results on AIFB and
collection-of-benchmark-datasets-for-ml                           AM. To explain the gap in performance on MUTAG and
                                                                  BGS it is important to understand the nature of these
datasets. MUTAG is a dataset of molecular graphs, which                        1MRR
was later converted to RDF format, where relations either                                                                   R-GCN
indicate atomic bonds or merely the presence of a certain                                                                  DistMult
feature. BGS is a dataset of rock types with hierarchical fea-
ture descriptions which was similarly converted to RDF for-                  0.5
mat, where relations encode the presence of a certain feature
or feature hierarchy. Labeled entities in MUTAG and BGS                        0
are only connected via high-degree hub nodes that encode a                             500 1,000 1,500 2,000 2,500
certain feature.
                                                                                                Average degree
   We conjecture that the fixed choice of normalization con-
stant for the aggregation of messages from neighboring           Figure 4: Mean reciprocal rank (MRR) for R-GCN and Dist-
nodes is partly to blame for this behavior, which can be par-    Mult on the FB15k validation data as a function of the node
ticularly problematic for nodes of high degree. A potential      degree (average of subject and object).
way to overcome this limitation is to introduce an atten-
tion mechanism, i.e. to replace the normalization constant       datasets, and furthermore corresponds to a version of our
1/ci,r with data-dependent attention weights aij,r, where        model with fixed entity embeddings in place of the R-GCN
                                                                 encoder as described in Section 4. As a second baseline, we
   j,r aij,r = 1. We expect this to be a promising avenue        add the simple neighbor-based LinkFeat algorithm proposed
for future research.                                             in Toutanova and Chen (2015).

5.2 Link prediction experiments                                     We further compare to ComplEx (Trouillon et al. 2016)
                                                                 and HolE (Nickel, Rosasco, and Poggio 2015), two state-of-
As shown in the previous section, R-GCNs serve as an ef-         the-art link prediction models for FB15k and WN18. Com-
fective encoder for relational data. We now combine our en-      plEx facilitates modeling of asymmetric relations by gen-
coder model with a scoring function (which we will refer to      eralizing DistMult to the complex domain, while HolE re-
as a decoder, see Figure 3b) to score candidate triples for      places the vector-matrix product with circular correlation.
link prediction in knowledge bases.                              Finally, we include comparisons with two classic algorithms
                                                                 ­ CP (Hitchcock 1927) and TransE (Bordes et al. 2013).
Datasets Link prediction algorithms are commonly evalu-
ated on FB15k, a subset of the relational database Freebase,     Results We provide results using two commonly used
and WN18, a subset of WordNet containing lexical relations       evaluation metrics: mean reciprocal rank (MRR) and Hits at
between words. In Toutanova and Chen (2015), a serious           n (H@n). Following Bordes et al. (2013), both metrics can
flaw was observed in both datasets: The presence of inverse      be computed in a raw and a filtered setting. We report both
triplet pairs t = (e1, r, e2) and t = (e2, r-1, e1) with t       filtered and raw MRR (with filtered MRR typically consid-
in the training set and t in the test set. This reduces a large  ered more reliable), and filtered Hits at 1, 3, and 10.
part of the prediction task to memorization of affected triplet
pairs. A simple baseline LinkFeat employing a linear classi-        We evaluate hyperparameter choices on the respective
fier on top of sparse feature vectors of observed training re-   validation splits. We found a normalization constant defined
lations was shown to outperform existing systems by a large      as ci,r = ci = r |Nir| -- in other words, applied across
margin. To address this issue, Toutanova and Chen proposed       relation types ­ to work best. For FB15k and WN18, we re-
a reduced dataset FB15k-237 with all such inverse triplet        port results using basis decomposition (Eq. 3) with two basis
pairs removed. We therefore choose FB15k-237 as our pri-         functions, and a single encoding layer with 200-dimensional
mary evaluation dataset. Since FB15k and WN18 are still          embeddings. For FB15k-237, we found block decomposi-
widely used, we also include results on these datasets using     tion (Eq. 4) to perform best, using two layers with block
the splits introduced by Bordes et al. (2013).                   dimension 5 × 5 and 500-dimensional embeddings. We reg-
                                                                 ularize the encoder through edge dropout applied before nor-
Dataset       WN18     FB15K     FB15k-237                       malization, with dropout rate 0.2 for self-loops and 0.4 for
                                                                 other edges. Using edge droupout makes our training ob-
Entities      40,943    14,951        14,541                     jective similar to that of denoising autoencoders (Vincent et
Relations          18     1,345           237                    al. 2008). We apply l2 regularization to the decoder with a
Train edges                                                      penalty of 0.01.
Val. edges   141,442   483,142      272,115
Test edges      5,000   50,000        17,535                        We use the Adam optimizer (Kingma and Ba 2014) with
                5,000   59,071        20,466                     a learning rate of 0.01. For the baseline and the other
                                                                 factorizations, we found the parameters from Trouillon et
Table 3: Number of entities and relation types along with the    al. (2016) ­ apart from the dimensionality on FB15k-237 ­
number of edges per split for the three datasets.                to work best, though to make the systems comparable we
                                                                 maintain the same number of negative samples (i.e.  = 1).
Baselines A common baseline for both experiments is di-          We use full-batch optimization for both the baselines and
rect optimization of DistMult (Yang et al. 2014). This fac-      our model.
torization strategy is known to perform well on standard
                                                                    On FB15k, local context in the form of inverse relations
                        FB15k                                                            WN18

          MRR                  Hits @                                   MRR                     Hits @

Model     Raw Filtered 1       3                                 10     Raw Filtered 1          3         10

LinkFeat         0.779                                           0.804            0.938                   0.939

DistMult  0.248 0.634   0.522 0.718 0.814                               0.526 0.813      0.701 0.921      0.943
R-GCN     0.251 0.651   0.541 0.736 0.825                               0.553 0.814      0.686 0.928      0.955
R-GCN+    0.262 0.696   0.601 0.760 0.842                               0.561 0.819      0.697 0.929      0.964

CP*       0.152  0.326  0.219  0.376                             0.532  0.075     0.058  0.049  0.080     0.125
TransE*   0.221  0.380  0.231  0.472                             0.641  0.335     0.454  0.089  0.823     0.934
HolE**    0.232  0.524  0.402  0.613                             0.739  0.616     0.938  0.930  0.945     0.949
ComplEx*  0.242  0.692  0.599  0.759                             0.840  0.587     0.941  0.936  0.945     0.947

Table 4: Results on the the Freebase and WordNet datasets. Results marked (*) taken from Trouillon et al. (2016). Results marks
(**) taken from Nickel, Rosasco, and Poggio (2015). R-GCN+ denotes an ensemble between R-GCN and DistMult ­ see main
text for details.

is expected to dominate the performance of the factoriza-                                MRR              Hits @
tions, contrasting with the design of the R-GCN model. To
better understand the difference, we plot in Figure 4 the               Model     Raw Filtered     1      3       10
FB15k performance of the best R-GCN model and the base-
line (DistMult) as functions of degree of nodes correspond-             LinkFeat         0.063                    0.079
ing to entities in the considered triple (namely, the average
of degrees for the subject and object entities). It can be seen         DistMult  0.100 0.191      0.106 0.207 0.376
that our model performs better for nodes with high degree               R-GCN     0.158 0.248      0.153 0.258 0.414
where contextual information is abundant. The observation               R-GCN+    0.156 0.249      0.151 0.264 0.417
that the two models are complementary suggests combining
the strengths of both into a single model, which we refer to            CP        0.080  0.182     0.101  0.197   0.357
as R-GCN+. On FB15k and WN18 where local and long-                      TransE    0.144  0.233     0.147  0.263   0.398
distance information can both provide strong solutions, we              HolE      0.124  0.222     0.133  0.253   0.391
expect R-GCN+ to outperform each individual model. On                   ComplEx   0.109  0.201     0.112  0.213   0.388
FB15k-237 where local information is less salient, we do not
expect the combination model to outperform a pure R-GCN              Table 5: Results on FB15k-237, a reduced version of FB15k
model significantly. To test this, we evaluate an ensemble           with problematic inverse relation pairs removed. CP, TransE,
(R-GCN+) with a trained R-GCN model and a separately                 and ComplEx were evaluated using the code published for
trained DistMult factorization model: f (s, r, t)R-GCN+ =            Trouillon et al. (2016), while HolE was evaluated using the
f (s, r, t)R-GCN + (1 - )f (s, r, t)DistMult, with  = 0.4 se-        code published for Nickel, Rosasco, and Poggio (2015).
lected on FB15k development data.
                                                                     previously discussed) inverse relation pairs have been re-
   In Table 4, we evaluate the R-GCN model and the combi-            moved and the LinkFeat baseline fails to generalize4. Here,
nation model (R-GCN+) on FB15k and WN18.                             our R-GCN model outperforms the DistMult baseline by
                                                                     a large margin of 29.8%, highlighting the importance of a
   On the FB15k and WN18 datasets, R-GCN and R-GCN+                  separate encoder model. As expected from our earlier anal-
both outperform the DistMult baseline, but like all other            ysis, R-GCN and R-GCN+ show similar performance on
systems underperform on these two datasets compared to               this dataset. The R-GCN model further compares favorably
the LinkFeat algorithm. The strong result from this baseline         against other factorization methods, despite relying on a
highlights the contribution of inverse relation pairs to high-       DistMult decoder which shows comparatively weak perfor-
performance solutions on these datasets. Interestingly, R-           mance when used without an encoder.
GCN+ yields better performance than ComplEx for FB15k,
even though the R-GCN decoder (DistMult) does not explic-                4Our numbers are not directly comparable to those reported in
itly model asymmetry in relations, as opposed to ComplEx.            Toutanova and Chen (2015), as they use pruning both for training
                                                                     and testing (see their sections 3.3.1 and 4.2). Since their pruning
   This suggests that combining the R-GCN encoder with               schema is not fully specified (e.g., values of the relation-specific
the ComplEx scoring function (decoder) may be a promising            parameter t are not given) and the code is not available, it is not
direction for future work. The choice of scoring function is         possible to replicate their set-up.
orthogonal to the choice of encoder; in principle, any scoring
function or factorization model could be incorporated as a
decoder in our auto-encoder framework.

   In Table 5, we show results for FB15k-237 where (as
                  6 Related Work                                 link prediction and entity classification. For the entity clas-
                                                                 sification problem, we have demonstrated that the R-GCN
6.1 Relational modeling                                          model can act as a competitive, end-to-end trainable graph-
                                                                 based encoder. For link prediction, the R-GCN model with
Our encoder-decoder approach to link prediction relies on        DistMult factorization as the decoding component outper-
DistMult (Yang et al. 2014) in the decoder, a special and        formed direct optimization of the factorization model, and
simpler case of the RESCAL factorization (Nickel, Tresp,         achieved competitive results on standard link prediction
and Kriegel 2011), more effective than the original RESCAL       benchmarks. Enriching the factorization model with an R-
in the context of multi-relational knowledge bases. Numer-       GCN encoder proved especially valuable for the challenging
ous alternative factorizations have been proposed and stud-      FB15k-237 dataset, yielding a 29.8% improvement over the
ied in the context of SRL, including both (bi-)linear and non-   decoder-only baseline.
linear ones (e.g., (Bordes et al. 2013; Socher et al. 2013;
Chang et al. 2014; Nickel, Rosasco, and Poggio 2015;                There are several ways in which our work could be ex-
Trouillon et al. 2016)). Many of these approaches can be         tended. For example, the graph autoencoder model could be
regarded as modifications or special cases of classic tensor     considered in combination with other factorization models,
decomposition methods such as CP or Tucker; for a com-           such as ComplEx (Trouillon et al. 2016), which can be better
prehensive overview of tensor decomposition literature we        suited for modeling asymmetric relations. It is also straight-
refer the reader to Kolda and Bader (2009).                      forward to integrate entity features in R-GCNs, which would
                                                                 be beneficial both for link prediction and entity classification
   Incorporation of paths between entities in knowledge          problems. To address the scalability of our method, it would
bases has recently received considerable attention. We can       be worthwhile to explore subsampling techniques, such as
roughly classify previous work into (1) methods creating         in Hamilton, Ying, and Leskovec (2017). Lastly, it would
auxiliary triples, which are then added to the learning objec-   be promising to replace the current form of summation over
tive of a factorization model (Guu, Miller, and Liang 2015;      neighboring nodes and relation types with a data-dependent
Garcia-Duran, Bordes, and Usunier 2015); (2) approaches          attention mechanism. Beyond modeling knowledge bases,
using paths (or walks) as features when predicting edges         R-GCNs can be generalized to other applications where re-
(Lin et al. 2015); or (3) doing both at the same time (Nee-      lation factorization models have been shown effective (e.g.
lakantan, Roth, and McCallum 2015; Toutanova et al. 2016).       relation extraction).
The first direction is largely orthogonal to ours, as we would
also expect improvements from adding similar terms to our        Acknowledgements
loss (in other words, extending our decoder). The second
research line is more comparable; R-GCNs provide a com-          We would like to thank Diego Marcheggiani, Ethan Fetaya,
putationally cheaper alternative to these path-based models.     and Christos Louizos for helpful discussions and comments.
Direct comparison is somewhat complicated as path-based          This project is supported by the European Research Council
methods used different datasets (e.g., sub-sampled sets of       (ERC StG BroadSem 678254), the SAP Innovation Center
walks from a knowledge base).                                    Network and the Dutch National Science Foundation (NWO
                                                                 VIDI 639.022.518).
6.2 Neural networks on graphs
                                                                                        References
Our R-GCN encoder model is closely related to a number
of works in the area of neural networks on graphs. It is pri-    Bao, J.; Duan, N.; Zhou, M.; and Zhao, T. 2014. Knowledge-
marily motivated as an adaption of previous work on GCNs         based question answering as machine translation. In ACL.
(Bruna et al. 2014; Duvenaud et al. 2015; Defferrard, Bres-
son, and Vandergheynst 2016; Kipf and Welling 2017) for          Bordes, A.; Usunier, N.; Garcia-Duran, A.; Weston, J.; and
large-scale and highly multi-relational data, characteristic of  Yakhnenko, O. 2013. Translating embeddings for modeling
realistic knowledge bases.                                       multi-relational data. In NIPS.

   Early work in this area includes the graph neural network     Bordes, A.; Usunier, N.; Chopra, S.; and Weston, J. 2015.
by Scarselli et al. (2009). A number of extensions to the orig-  Large-scale simple question answering with memory net-
inal graph neural network have been proposed, most notably       works. arXiv preprint arXiv:1506.02075.
(Li et al. 2016) and (Pham et al. 2017), both of which utilize
gating mechanisms to facilitate optimization.                    Bruna, J.; Zaremba, W.; Szlam, A.; and LeCun, Y. 2014.
                                                                 Spectral networks and locally connected networks on
   R-GCNs can further be seen as a sub-class of message          graphs. In ICLR.
passing neural networks (Gilmer et al. 2017), which encom-
pass a number of previous neural models for graphs, includ-      Chang, K.-W.; tau Yih, W.; Yang, B.; and Meek, C. 2014.
ing GCNs, under a differentiable message passing interpre-       Typed Tensor Decomposition of Knowledge Bases for Rela-
tation.                                                          tion Extraction. In EMNLP.

                    7 Conclusions                                Dalton, J.; Dietz, L.; and Allan, J. 2014. Entity query feature
                                                                 expansion using knowledge base links. In Proceedings of
We have introduced relational graph convolutional networks       the 37th international ACM SIGIR conference on Research
(R-GCNs) and demonstrated their effectiveness in the con-        & development in information retrieval, 365­374.
text of two standard statistical relation modeling problems:
                                                                 de Vries, G. K. D., and de Rooij, S. 2015. Substructure
                                                                 counting graph kernels for machine learning from rdf data.
Web Semantics: Science, Services and Agents on the World       Nickel, M.; Tresp, V.; and Kriegel, H.-P. 2011. A three-
Wide Web 35:71­84.                                             way model for collective learning on multi-relational data.
                                                               In ICML.
Defferrard, M.; Bresson, X.; and Vandergheynst, P. 2016.
Convolutional neural networks on graphs with fast localized    Paulheim, H., and Fu¨mkranz, J. 2012. Unsupervised gen-
spectral filtering. In NIPS.                                   eration of data mining features from linked open data. In
                                                               Proceedings of the 2nd international conference on web in-
Dong, L.; Wei, F.; Zhou, M.; and Xu, K. 2015. Question an-     telligence, mining and semantics, 31.
swering over freebase with multi-column convolutional neu-
ral networks. In ACL.                                          Pham, T.; Tran, T.; Phung, D.; and Venkatesh, S. 2017. Col-
                                                               umn networks for collective classification. In AAAI.
Duvenaud, D. K.; Maclaurin, D.; Iparraguirre, J.; Bombarell,
R.; Hirzel, T.; Aspuru-Guzik, A.; and Adams, R. P. 2015.       Ristoski, P., and Paulheim, H. 2016. Rdf2vec: Rdf graph
Convolutional networks on graphs for learning molecular        embeddings for data mining. In International Semantic Web
fingerprints. In NIPS.                                         Conference, 498­514. Springer.

Garcia-Duran, A.; Bordes, A.; and Usunier, N. 2015. Com-       Ristoski, P.; de Vries, G. K. D.; and Paulheim, H. 2016. A
posing relationships with translations. Technical Report,      collection of benchmark datasets for systematic evaluations
CNRS, Heudiasyc.                                               of machine learning on the semantic web. In International
                                                               Semantic Web Conference, 186­194. Springer.
Gilmer, J.; Schoenholz, S. S.; Riley, P. F.; Vinyals, O.; and
Dahl, G. E. 2017. Neural message passing for quantum           Scarselli, F.; Gori, M.; Tsoi, A. C.; Hagenbuchner, M.; and
chemistry. arXiv preprint arXiv:1704.01212.                    Monfardini, G. 2009. The graph neural network model.
                                                               IEEE Transactions on Neural Networks 20(1):61­80.
Guu, K.; Miller, J.; and Liang, P. 2015. Travers-
ing knowledge graphs in vector space. arXiv preprint           Seyler, D.; Yahya, M.; and Berberich, K. 2015. Generating
arXiv:1506.01094.                                              quiz questions from knowledge graphs. In Proceedings of
                                                               the 24th International Conference on World Wide Web.
Hamilton, W. L.; Ying, R.; and Leskovec, J. 2017. Induc-
tive representation learning on large graphs. arXiv preprint   Shervashidze, N.; Schweitzer, P.; Leeuwen, E. J. v.;
arXiv:1706.02216.                                              Mehlhorn, K.; and Borgwardt, K. M. 2011. Weisfeiler-
                                                               lehman graph kernels. Journal of Machine Learning Re-
Hitchcock, F. L. 1927. The expression of a tensor or a         search 12(Sep):2539­2561.
polyadic as a sum of products. Studies in Applied Mathe-
matics 6(1-4):164­189.                                         Socher, R.; Chen, D.; Manning, C. D.; and Ng, A. 2013.
                                                               Reasoning with neural tensor networks for knowledge base
Hixon, B.; Clark, P.; and Hajishirzi, H. 2015. Learning        completion. In NIPS.
knowledge graphs for question answering through conver-
sational dialog. In Proceedings of NAACL HLT, 851­861.         Toutanova, K., and Chen, D. 2015. Observed versus latent
                                                               features for knowledge base and text inference. In Proceed-
Kingma, D., and Ba, J. 2014. Adam: A method for stochastic     ings of the 3rd Workshop on Continuous Vector Space Mod-
optimization. arXiv preprint arXiv:1412.6980.                  els and their Compositionality, 57­66.

Kipf, T. N., and Welling, M. 2016. Variational graph auto-     Toutanova, K.; Lin, V.; Yih, W.-t.; Poon, H.; and Quirk, C.
encoders. arXiv preprint arXiv:1611.07308.                     2016. Compositional learning of embeddings for relation
                                                               paths in knowledge base and text. In ACL.
Kipf, T. N., and Welling, M. 2017. Semi-supervised classi-
fication with graph convolutional networks. In ICLR.           Trouillon, T.; Welbl, J.; Riedel, S.; Gaussier, E.; and
                                                               Bouchard, G. 2016. Complex embeddings for simple link
Kolda, T. G., and Bader, B. W. 2009. Tensor decompositions     prediction. In ICML.
and applications. SIAM review 51(3):455­500.
                                                               Vincent, P.; Larochelle, H.; Bengio, Y.; and Manzagol, P.-
Kotov, A., and Zhai, C. 2012. Tapping into knowledge           A. 2008. Extracting and composing robust features with
base for concept feedback: leveraging conceptnet to improve    denoising autoencoders. In ICML.
search results for difficult queries. In WSDM.
                                                               Xiong, C., and Callan, J. 2015a. Esdrank: Connecting query
Li, Y.; Tarlow, D.; Brockschmidt, M.; and Zemel, R. 2016.      and documents through external semi-structured data. In
Gated graph sequence neural networks. In ICLR.                 CIKM.

Lin, Y.; Liu, Z.; Luan, H.; Sun, M.; Rao, S.; and Liu, S.      Xiong, C., and Callan, J. 2015b. Query expansion with free-
2015. Modeling relation paths for representation learning of   base. In Proceedings of the 2015 International Conference
knowledge bases. arXiv preprint arXiv:1506.00379.              on The Theory of Information Retrieval, 111­120.

Mikolov, T.; Sutskever, I.; Chen, K.; Corrado, G. S.; and      Yang, B.; Yih, W.-t.; He, X.; Gao, J.; and Deng, L. 2014.
Dean, J. 2013. Distributed representations of words and        Embedding entities and relations for learning and inference
phrases and their compositionality. In NIPS.                   in knowledge bases. arXiv preprint arXiv:1412.6575.

Neelakantan, A.; Roth, B.; and McCallum, A. 2015. Com-         Yao, X., and Van Durme, B. 2014. Information extraction
positional vector space models for knowledge base comple-      over structured data: Question answering with freebase. In
tion. arXiv preprint arXiv:1504.06662.                         ACL.

Nickel, M.; Rosasco, L.; and Poggio, T. 2015. Holo-
graphic embeddings of knowledge graphs. arXiv preprint
arXiv:1510.04935.
Further experimental details on entity classification

For the entity classification benchmarks described in our paper, the evaluation process differs subtly between publications. To
eliminate these differences, we repeated the baselines in a uniform manner, using the canonical test/train split from (Ristoski,
de Vries, and Paulheim 2016). We performed hyperparameter optimization on only the training set, running a single evaluation
on the test set after hyperparameters were chosen for each baseline. This explains why the numbers we report differ slightly
from those in the original publications (where cross-validation accuracy was reported).

   For WL, we use the tree variant of the Weisfeiler-Lehman subtree kernel from the Mustard library.5 For RDF2Vec, we use
an implementation provided by the authors of (Ristoski and Paulheim 2016) which builds on Mustard. In both cases, we extract
explicit feature vectors for the instance nodes, which are classified by a linear SVM.

   For the MUTAG task, our preprocessing differs from that used in (de Vries and de Rooij 2015; Ristoski and Paulheim 2016)
where for a given target relation (s, r, o) all triples connecting s to o are removed. Since o is a boolean value in the MUTAG
data, one can infer the label after processing from other boolean relations that are still present. This issue is now mentioned in
the Mustard documentation. In our preprocessing, we remove only the specific triples encoding the target relation.

   Hyperparameters for baselines are chosen according to the best model performance in (Ristoski and Paulheim 2016),
i.e. WL: 2 (tree depth), 3 (number of iterations); RDF2Vec: 2 (WL tree depth), 4 (WL iterations), 500 (embedding size), 5
(window size), 10 (SkipGram iterations), 25 (number of negative samples). We optimize the SVM regularization constant
C  {0.001, 0.01, 0.1, 1, 10, 100, 1000} based on performance on a 80/20 train/validation split (of the original training set).

   For R-GCN, we choose an l2 penalty on first layer weights Cl2  {0, 5 · 10-4} and the number of basis functions B 
{0, 10, 20, 30, 40} based on validation set performance, where B = 0 refers to using no basis function decomposition. Using
the block decomposition did not improve results. Otherwise, hyperparameters are chosen as follows: 50 (number of epochs), 16
(number of hidden units), and ci,r = |Nir| (normalization constant). We do not use dropout. For AM, we use a reduced number
of 10 hidden units for R-GCN to reduce the memory footprint.

   Results with standard error (omitted in main paper due to spatial constraints) are summarized in Table 7. All entity classifi-
cation experiments were run on CPU nodes with 64GB of memory.

R-GCN setting                               AIFB    MUTAG          BGS        AM

l2 penalty                                       0  5 · 10-4   5 · 10-4  5 · 10-4
# basis functions                                0         30        40        40
# hidden units                                 16          16        16        10

Table 6: Best hyperparameter choices based on validation set performance for 2-layer R-GCN model.

Model             AIFB                         MUTAG                BGS            AM

Feat          55.55 ± 0.00                  77.94 ± 0.00       72.41 ± 0.00  66.66 ± 0.00
WL            80.55 ± 0.00                  80.88 ± 0.00       86.20 ± 0.00  87.37 ± 0.00
RDF2Vec       88.88 ± 0.00                  67.20 ± 1.24       87.24 ± 0.89  88.33 ± 0.61
R-GCN (Ours)  95.83 ± 0.62                  73.23 ± 0.48       83.10 ± 0.80  89.29 ± 0.35

Table 7: Entity classification results in accuracy (average and standard error over 10 runs) for a feature-based baseline (see
main text for details), WL (Shervashidze et al. 2011; de Vries and de Rooij 2015), RDF2Vec (Ristoski and Paulheim 2016),
and R-GCN (this work). Test performance is reported on the train/test set splits provided by (Ristoski, de Vries, and Paulheim
2016).

5https://github.com/Data2Semantics/mustard
