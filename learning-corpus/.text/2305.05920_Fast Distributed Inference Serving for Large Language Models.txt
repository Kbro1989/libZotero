                                                                                             Fast Distributed Inference Serving for Large Language Models

                                                                                                Bingyang Wu Yinmin Zhong Zili Zhang Shengyu Liu
                                                                                             Fangyue Liu Yuanhang Sun Gang Huang Xuanzhe Liu Xin Jin

                                                                                             Peking University

                                                                                                                                                                 100  Queuing Delay             Execution Time

                                                                                                                     Abstract                                                             12.3        7.2

arXiv:2305.05920v3 [cs.LG] 25 Sep 2024                                                       Large language models (LLMs) power a new generation of              80                             60.1
                                                                           Time Breakdown (%)interactive AI applications exemplified by ChatGPT. The in-
                                                                                             teractive nature of these applications demands low latency for      60                 79.6
                                                                                             LLM inference. Existing LLM serving systems use run-to-                     98.0 98.0
                                                                                             completion processing for inference jobs, which suffers from                                 87.6        92.7
                                                                                             head-of-line blocking and long latency.                             40

                                                                                                We present FastServe, a distributed inference serving sys-       20                 20.3        39.9
                                                                                             tem for LLMs. FastServe exploits the autoregressive pattern
                                                                                             of LLM inference to enable preemption at the granularity of         0 Fix-Len (load=F0i.x9-)Len (loadA=l1p)aca (load=0A.9lp)aca (loaSdh=a1r)eGPT (loaSdh=a0r.e9G) PT (load=1)
                                                                                             each output token. FastServe uses preemptive scheduling to
                                                                                             minimize latency with a novel skip-join Multi-Level Feedback             Figure 1: Head-of-line blocking in LLM inference.
                                                                                             Queue scheduler. Based on the new semi information-agnostic
                                                                                             setting of LLM inference, the scheduler leverages the input         inputs to ChatGPT to be responded instantly. Yet, the size and
                                                                                             length information to assign an appropriate initial queue for       complexity of LLMs put tremendous pressure on the under-
                                                                                             each arrival job to join. The higher priority queues than the       lying inference serving infrastructure. Enterprises provision
                                                                                             joined queue are skipped to reduce demotions. We design an          huge and expensive clusters that consist of accelerators like
                                                                                             efficient GPU memory management mechanism that proac-               GPUs and TPUs to process LLM inference jobs.
                                                                                             tively offloads and uploads intermediate state between GPU
                                                                                             memory and host memory for LLM inference. We build a sys-              LLM inference has its own unique characteristics (§2) that
                                                                                             tem prototype of FastServe and experimental results show that       are different from other deep neural network (DNN) model
                                                                                             compared to the state-of-the-art solution vLLM, FastServe           inference like ResNet [7]. DNN inference jobs are typically
                                                                                             improves the throughput by up to 31.4× and 17.9× under the          deterministic and highly-predictable [8], i.e., the execution
                                                                                             same average and tail latency requirements, respectively.           time of an inference job is mainly decided by the model and
                                                                                                                                                                 the hardware. For example, different input images have similar
                                                                                             1 Introduction                                                      execution time on the same ResNet model on a given GPU.
                                                                                                                                                                 In contrast, LLM inference jobs have a special autoregressive
                                                                                             Advancements in large language models (LLMs) open new               pattern. An LLM inference job contains multiple iterations.
                                                                                             possibilities in a wide variety of areas and trigger a new gen-     Each iteration generates one output token, and each output
                                                                                             eration of interactive AI applications. The most notable one        token is appended to the input to generate the next output
                                                                                             is ChatGPT [1] that enables users to interact with an AI agent      token in the next iteration. The execution time depends on
                                                                                             in a conversational way to solve tasks ranging from language        both the input length and the output length, the latter of which
                                                                                             translation to software engineering. The impressive capability      is not known a priori.
                                                                                             of ChatGPT makes it one of the fastest growing applications
                                                                                             in history [2]. Many organizations follow the trend to release         Existing inference serving solutions like Clockwork [8]
                                                                                             LLMs and ChatGPT-like applications, such as the New Bing            and Shepherd [9] target deterministic model inference like
                                                                                             from Microsoft [3], Gemini from Google [4], Claude-3 [5]            ResNet [7]. They rely on accurate execution time profiling to
                                                                                             from Anthropic, Qwen [6] from Alibaba, etc.                         schedule jobs, which do not work for LLM inference that has
                                                                                                                                                                 variable execution time. Orca [10], designed for LLM infer-
                                                                                                Inference serving is critical to interactive AI applications     ence, introduces iteration-level scheduling that dynamically
                                                                                             based on LLMs. In order to provide engaging user experi-            adds new jobs or removes completed ones at the end of each
                                                                                             ence, the interactive nature of these applications demands low      iteration. vLLM [11] further introduces PagedAttention to
                                                                                             latency for LLM inference. For example, users expect their          reduce the memory fragmentation of the intermediate state
                                                                                                                                                                 of LLM inference jobs. However, they both use first-come-
                                                                                                  Equal contribution.                                            first-served (FCFS) to process inference jobs. Once a job is
                                                                                                                                                                 scheduled, it runs until it finishes. Due to the limited GPU
                                                                                                                                                                 memory and the strict latency requirement, the current pro-

                                                                                                                                                              1
cessing batch cannot be expanded with an arbitrary number               GPT...    Transformer         Self-Attention
of incoming jobs, thus a long job may block the incoming
ones, known as head-of-line blocking [12]. The problem is             Decoding               Add                  Atten Out FC
particularly acute for LLM inference jobs. A large LLM in-           Transformer             FC2                         MatMul
ference job, i.e., with long input and output length, would run                             FC1
for a long time to block incoming short jobs.                        Transformer        LayerNorm           Softmax
                                                                     Embedding
   Figure 1 demonstrates the problem based on real-world                                     Add        Query Key Value
datasets. The detailed setup is in §6. Ideally, if the input                              Masked              QKV Linear
length and output length of the jobs are all the same among                           Self-Attention
jobs, there is almost no queuing delay even when the load                               LayerNorm
reaches capacity (load  1), as the first two columns in Fig-
ure 1 suggest. However, LLM datasets like ShareGPT [13]                               Figure 2: GPT-like model architecture.
and Alpaca [14] show that real-world workloads are highly
skewed. The long-tail distribution of output length leads to a       But in MLFQ, more jobs may have started but are demoted
long queuing delay. Figure 1 shows that up to 90% of total           to lower priority queues. The cache has to maintain the in-
latency is the queuing delay for real-world datasets. In this        termediate state for all started but unfinished jobs in MLFQ.
case, optimizing execution time is not enough, because it only       The cache can overflow, given the large size of intermedi-
contributes to a small portion of the end-to-end latency. In-        ate state and the limited memory capacity of GPUs. Naively,
stead, we need to optimize the queuing delay, which is the           the scheduler can pause starting new jobs when the cache is
major contributor to the end-to-end latency.                         full, but this again introduces head-of-line blocking. Instead,
                                                                     we design proactive GPU memory management mechanism
   We present FastServe, a distributed inference serving sys-        that proactively offloads the state of the jobs in low-priority
tem for LLMs. FastServe exploits the autoregressive pattern          queues to the host memory when the cache is close to full, and
of LLM inference and iteration-level scheduling to enable pre-       uploads the state back when these jobs are to be scheduled.
emption at the granularity of each output token. Specifically,       We use pipelining and asynchronous memory operations to
when one scheduled job finishes generating an output token,          improve the efficiency.
FastServe can decide whether to continue this job or preempt
it with another job in the queue. This allows FastServe to              For large models that do not fit in one GPU, FastServe
use preemptive scheduling to eliminate head-of-line blocking         leverages parallelization strategies including tensor paral-
problem and minimize latency.                                        lelism [16] and pipeline parallelism [17] to perform dis-
                                                                     tributed inference serving with multiple GPUs (§4.3). The
   The core of FastServe is a novel skip-join Multi-Level Feed-      scheduler runs multiple batches of jobs concurrently in a
back Queue (MLFQ) scheduler. MLFQ is a classic approach              pipeline to minimize pipeline bubbles. The key-value cache
to minimize latency in information-agnostic settings [15].           manager partitions the key-value cache over multiple GPUs,
Each job first enters the highest priority queue, and is de-         and handles swapping between GPU memory and host mem-
moted to the next priority queue if it does not finish after a       ory in a distributed manner.
threshold. The key difference between LLM inference and
the classic setting is that LLM inference is semi information-          We implement a system prototype of FastServe and inte-
agnostic, i.e., while the output length is not known a priori,       grate many optimization techniques like PagedAttention [11].
the input length is known. Because of the autoregressive pat-        We evaluate FastServe on different configurations of LLMs
tern of LLM inference, the input length decides the execution        with real-world LLM inference workloads. In particular, we
time to generate the first output token, which can be signifi-       evaluate the end-to-end performance of FastServe for OPT-
cantly larger than those of the later tokens (§4.1). For a long      175B [18] (an open-source LLM similar to the largest GPT-3
input and a short output, the execution time of the first output     model) on 16 NVIDIA A100 GPUs. The experiments show
token dominates the entire job. We leverage this characteristic      that compared to the state-of-the-art solution vLLM [11], Fast-
to extend the classic MLFQ with skip-join. Instead of always         Serve improves the throughput by up to 31.4× and 17.9×
entering the highest priority queue, each arrival job joins an       under the same average and tail latency requirements, respec-
appropriate queue by comparing its execution time of the             tively.
first output token with the quantum of the queues. The higher
priority queues are skipped to reduce demotions.                     2 Background and Motivation

   Preemptive scheduling introduces extra memory overhead            2.1 LLM Inference and Applications
to maintain intermediate state for started but unfinished jobs.
LLMs maintain a key-value cache for each Transformer layer           LLM inference. The LLM family [18­20] comprises a set of
to store intermediate state (§2.2). In FCFS, the cache only          language models built on the foundation of Transformer [21].
needs to store the intermediate state of the scheduled jobs          LLM inference operates in an autoregressive fashion, where
in the processing batch, limited by the maximum batch size.          the input, often known as a prompt, is processed as a sequence
                                                                     of tokens. It then produces a probability distribution for the

                                                                  2
succeeding token to be selected. This processing and selection        Queries    Initialization
mechanism for each output token is referred to as an iteration.
Once trained with a vast corpus, LLM is capable of executing                   Keys Softmax Values Result
high-quality language tasks.
                                                                               Cache                   Cache
   For instance, given the input "knowledge is", it can assign a
higher probability to "power" than to "apple". The first output                       Key-Value Cache
token is appended to the initial prompt and fed into LLM
for subsequent token generation. This process continues un-           Retrieve Update  Retrieve Update
til a unique <EOS> token, symbolizing the end of sequence,
is generated, or a predetermined maximum output length is                             Decoding
reached. This inference process markedly differs from those
of other models like ResNet, where execution time is usually          Queries  Keys    Softmax  Values
deterministic and predictable [8]. While each iteration's exe-
cution maintains these characteristics in the LLM model, the                                            Result
number of iterations (i.e., output length) is variable, resulting
in an unpredictable total inference job execution time.                                Figure 3: Demonstration of KV cache.

LLM applications. The LLMs' primary commission is to                  cific to the unique architecture and iterative generation pattern
predict the next token for an input prompt. Utilizing prompt          of LLMs. The major part of LLM's architecture is a stack of
engineering [19], downstream NLP tasks can be reformulated            Transformer layers, as shown in Figure 2. In a Transformer
as generation tasks based on LLMs. Specifically, for a trans-         layer, the Masked Self-Attention module is the core compo-
lation task, one can start the prompt by adding "Translate the        nent that distinguishes it from other architectures like CNNs.
following English text into French text" before the original          During each iteration of LLM inference, for each token, the
text. By doing so, LLM will be guided to generate the desired         attention operator requires the keys and values of preceding
translated French text in response.                                   tokens. A naive, stateless implementation always recomputes
                                                                      all the preceding keys and values in each iteration. To avoid
   ChatGPT [1] is a representative LLM-based application.             such recomputation overhead, fairseq [24] suggests saving the
After supervised fine-tuning for the conversational task and an       keys and values in a key-value cache across iterations. This
alignment procedure using Reinforcement Learning from Hu-             optimization divides the inference procedure into two distinct
man Feedback (RLHF) on the original GPT model [19], Chat-             phases: the initialization phase and the decoding phase. Fig-
GPT facilitates interactive conversations with an AI agent,           ure 3 demonstrates the key-value cache usage in both phases.
allowing users to address a wide range of tasks. These tasks in-      During the initialization phase, which corresponds to the first
clude translation, question-answering, summarization, as well         iteration, the LLM generates the key-value cache for each
as more intricate undertakings like sentiment analysis, cre-          token in the input prompt. In the subsequent decoding phase,
ative writing, and domain-specific problem-solving. Despite           the LLM only needs to compute the query, key, and value
its remarkable capabilities, the interactive nature of ChatGPT        of one newly generated token, leveraging the precomputed
imposes a considerable pressure on the underlying inference           key-value cache to facilitate the process step by step. Con-
serving infrastructure. Due to the need for rapid responses,          sequently, the execution time of iterations in the decoding
keeping low latency is crucial for ensuring the performance           phase is typically smaller compared to that of the initializa-
of ChatGPT-like interactive applications.                             tion phase, i.e., the first iteration. It is worth noting that other
                                                                      Transformer-based systems, such as HuggingFace [25] and
2.2 Inference Serving Systems                                         FasterTransformer [26], also incorporate this optimization
                                                                      technique, leading to improved efficiency during inference.
Most existing inference serving systems, such as Tensorflow
Serving [22] and Triton Inference Server [23], are agnostic              Another important optimization is iteration-level schedul-
to DNN models. They serve as an abstraction above the un-             ing proposed by Orca [10]. Naive job-level scheduling exe-
derlying execution engine, which queues the arriving jobs,            cutes a batch of jobs until all jobs finish. The early finished
dispatches jobs to available computing resources, and returns         jobs cannot be returned to the clients immediately, while
the results to clients. To fully utilize the GPUs, they typically     newly arrived jobs have to wait until the ongoing batch com-
batch jobs together for parallel processing. With batching, the       pletes processing. However, with iteration-level scheduling,
input tensors from multiple jobs are concatenated and fed into        the execution engine executes only a single iteration on the
the model as a whole. Despite better utilization, the draw-           batch at a time, generating one output token for each job. Af-
back of batching is higher memory overhead. The substantial           ter each iteration, completed jobs leave the batch, and newly
size of LLMs and the huge intermediate state restricts the            arrived jobs can join in. Nevertheless, the GPU memory ca-
maximum batch size for LLM inference [11].                            pacity limits the maximum batch size, and the strict service-
                                                                      level-objects (SLOs) of interactive applications also play a
   As the popularity of LLMs rapidly increases, inference             role in determining the appropriate batch size. vLLM [11] fur-
serving systems have evolved to include optimizations spe-            ther improves the efficiency of LLM inference by introducing

                                                                   3
PagedAttention, which allocates the key-value cache gradu-                    Scheduler                  GPU Cluster
ally in block-grained during inference instead of allocating
for the maximum output length at the beginning.                               Skip-Join MLFQ       Distributed Execution
                                                                                      (4.1)               Engine (4.3)
2.3 Opportunities and Challenges
                                                                      Job                          Pipeline Stage 0  Pipeline Stage 1 ...
Opportunity: preemptive scheduling. The major limita-                 Pool
tion of existing inference serving systems for LLMs [10, 26]                                       GPU   GPU         GPU   ... GPU
is their reliance on simple FCFS (First-Come-First-Serve)              Job 0                         1     2
scheduling and run-to-completion execution. As shown in                                                              3     4
Figure 1, this approach leads to severe head-of-line blocking.
Queuing delay contributes up to 90% of the total latency in           Job 1                        GPU   GPU         GPU   GPU
real workload, which significantly impacts the performance                                         Mem.  Mem.        Mem.  Mem.
of LLM inference. To overcome this challenge, preemptive
scheduling can be employed. In LLM inference, each job                Job 2
comprises multiple iterations, with each iteration generating
one output token. The opportunity lies in exploiting this au-         ...               Key-Value
toregressive pattern to enable preemption at the iteration level,
meaning that one job can be preempted when it finishes gener-                           Cache      Host Memory Host Memory
ating an output token for another job. Leveraging preemption
capability, the scheduler can employ preemptive scheduling                    Job Management           Distributed Key-Value
policies to prevent head-of-line blocking and optimize av-                                                     Cache (4.3)
erage latency. Nevertheless, preemptive scheduling presents                   Profiler  (4.2)
two challenges for the existing LLM inference system.
                                                                                          Figure 4: FastServe architecture.
Challenge 1: variable job size. Shortest Remaining Process-
ing Time (SRPT) [27] is a widely-used preemptive scheduling           memory-saving techniques for the key-value cache. Multi-
policy to minimize average latency. However, applying SRPT            Query Attention [28] and Group-Query Attention [29] try to
to LLM inference presents challenges due to the iterative             reduce the memory consumption by sharing key-value ten-
nature of LLMs. Unlike one-shot prediction tasks like im-             sors between attention heads. They may hurt the capability
age classification, LLM inference involves multiple iterations.       of LLMs and the memory consumption still grows linearly
While the execution time for one iteration (generating one            with the sequence length. vLLM [11] manages the key-value
output token) can be determined based on the model archi-             cache at the granularity of blocks to reduce GPU memory frag-
tecture and hardware, the total number of iterations (i.e., the       mentation. It cannot reduce the memory usage caused by the
output sequence length) remains unknown and is challenging            key-value cache itself. As the context length increases [30],
to predict since it depends on the semantics of the job. Real-        the memory consumption of the key-value cache is a hard
world datasets collected from conversations with LLMs, like           problem and is becoming increasingly important.
ShareGPT [13] and Alpaca [14], exhibit a long-tailed distribu-
tion of the output length and input length [11]. Consequently,        3 FastServe Overview
SRPT cannot be directly employed for LLM inference to
minimize the average latency.                                         3.1 Desired Properties

Challenge 2: GPU memory overhead. Preemptive schedul-                 LLMs come with unique characteristics that pose challenges
ing policies introduce additional GPU memory consumption              to distributed computation and GPU memory consumption.
during LLM inference, unlike FCFS with run-to-completion,             Our goal is to develop an efficient inference serving system
which only needs to maintain the key-value cache for ongo-            for LLMs that fulfills the following three requirements.
ing jobs. In contrast, preemptive scheduling must keep the            · Low latency and high throughput. Our focus centers on
key-value cache in GPU memory for all preempted jobs in
the pending state, to be used for future token generation. This         interactive LLM applications, where users have high expec-
key-value cache consumes a substantial amount of GPU mem-               tations for fast response. To measure it quantitatively, we
ory, leading to potential challenges. For instance, a single job        want the maximum throughput as high as possible under a
of OPT 175B with an input sequence length of 512 requires at            certain latency requirement.
least 2.3 GB of memory for the key-value cache (§4.2). Due            · Efficient GPU memory management. LLMs pose a sig-
to scarce GPU memory capacity, the size of the key-value                nificant challenge in terms of GPU memory consumption,
cache becomes a critical factor affecting the effectiveness of          which necessitates an effective GPU memory management
preemptive scheduling policies. Prior works have proposed               approach for both the model and intermediate states.
                                                                      · Scalable distributed execution. The nature of LLMs de-
                                                                        mands multiple GPUs to enable distributed inference ef-
                                                                        fectively, which requires the system to support scalable
                                                                        distributed execution cross GPU servers.

                                                                      3.2 Overall Architecture

                                                                      Figure 4 shows the architecture of FastServe. Jobs are sub-
                                                                      mitted to the job pool. The scheduler utilizes information
                                                                      from the job profiler to determine the initial job priority and
                                                                      then places the job in the skip-join MLFQ (§4.1) to mitigate
                                                                      head-of-line blocking.

                                                                   4
Iteration Time (s)   0.1        input length = 1024                   Arrival Tail       Head
                    0.08        input length = 512                     Jobs                           Higher
                    0.06        input length = 256
                    0.04        input length = 128                                               Priority
                                                                                         

                                                                                                 

                    0.02                                                                 

                    0     0  1  2             3                       ···           ···        Lower
                                                                                          Priority
                             Iteration Index

Figure 5: Execution time of the first four iterations.

   For execution, the scheduler picks the jobs based on their         Figure 6: Skip-join MLFQ with starvation prevention.
priority within the skip-join MLFQ to form a pre-defined max-
imum batch size and dispatches the batch to the distributed           is Multi-Level Feedback Queue (MLFQ) which has gained
execution engine to perform one iteration. The distributed            popularity in various scheduling systems [15, 31­34]. MLFQ
execution engine collaborates with the distributed key-value          operates multiple queues, each with a different priority level.
cache to access and update the key-value tensors relevant to          Upon arrival, a job enters the highest priority queue and gets
the respective job. To tackle the challenge of limited GPU            demoted to the next level queue if its execution time exceeds
memory capacity, the key-value cache manager proactively              a quantum. The value of quantum is a tunable parameter
swaps key-value tensors between GPU memory and host                   assigned to each queue, e.g., higher priority queues typically
memory (§4.2).                                                        have shorter quantum values.

   To accommodate extreme large models such as OPT-175B,                 Although MLFQ assumes no prior knowledge of the job
FastServe employs distributed inference, enabling both tensor         size, it is not well suited for LLM serving. Figure 5 shows
parallelism and pipeline parallelism. FastServe incorporates          the iteration time of OPT 2.7B on an NVIDIA A100, varying
extensions into the scheduler and key-value cache to enable           the input sequence length. Notably, the initialization phase
seamless support for distributed execution (§4.3).                    (i.e., the first iteration) time exceeds the decoding phase du-
                                                                      ration. As the input sequence length increases, so does the
4 FastServe Design                                                    initialization phase time. This behavior can be attributed to
                                                                      the key-value cache optimization (§2.2). During the first iter-
In this section, we first introduce the skip-join MLFQ sched-         ation, computations for all key-value tensors of input tokens
uler to minimize latency (§4.1). Then, we present a proactive         are performed and cached. In subsequent iterations, only one
KV cache management mechanism designed to effectively                 newly generated token's key-value tensors are computed, and
ameliorate the GPU memory capacity constraint (§4.2). Fi-             the rest are retrieved from the cache.
nally, we demonstrate how to apply these techniques to the
distributed settings (§4.3).                                             When employing the original MLFQ, a job is immediately
                                                                      assigned to the highest priority queue upon arrival. However,
4.1 Skip-Join MLFQ Scheduler                                          due to its substantial initialization phase time, the job may
                                                                      deplete its quantum before completing its first iteration. This
Strawman: fixed priority scheduling. To support preemp-               situation presents a scheduling dilemma. If the scheduler
tive scheduling, we need a priority-based scheduler to decide         preempts the job, intermediate activations are dropped and
which jobs to preempt and which to execute. One naive solu-           recomputed later, resulting in a waste of valuable comput-
tion is to assign a fixed priority to each job based on its input     ing resources and time. On the other hand, if the scheduler
length. In this case, when the initialization phase dominates         chooses not to preempt the job, it violates the fundamental
the total latency, the fixed priority scheduling can approximate      design purpose of MLFQ and potentially suffers from head-
the optimal performance as the SRPT policy does. However,             of-line blocking once again.
although this solution leverages the information about the ini-
tialization phase, it ignores the characteristics of the decoding     Our solution: skip-join MLFQ. The key insight of our de-
phase. Many real-world datasets like ShareGPT and Alpaca              sign is to leverage the semi information-agnostic setting of
show a long tail distribution implying that jobs with a long          LLM inference to address the aforementioned issues of the
output length also exist. When the decoding phase dominates           strawman solutions. While the number of iterations (i.e., the
the total latency, the fixed priority scheduling may deviate          output length) remains unknown beforehand, the execution
from the optimal performance of SRPT.                                 time of each iteration is predictable. For each iteration, the
                                                                      execution is similar to the traditional one-shot DNN infer-
Strawman: naive MLFQ. Due to the indeterminate job size               ence, whose execution time is highly predictable [8, 35]. A
of LLM inference, directly applying SRPT is not feasible. In          lightweight profiling process can easily collect accurate itera-
information-agnostic settings, Least-Attained Service (LAS)           tion time under different hardware, model specifications, and
has been shown to approximate SRPT effectively. Due to                input lengths in advance. Revisiting Figure 5, the initialization
the job switching overhead of LAS, the practical approach             phase time exhibits a positive correlation with the input length

                                                                   5
Algorithm 1 Skip-Join Multi-Level Feedback Queue Scheduler

1: Input: Queues Q1, Q2, ..., Qn, newly arrived jobs Jnew                                             
2: Output: Jobs to be executed Jout for one iteration
3: procedure SKIPJOINMLFQSCHEDULER                                                                    

4: Initialization: Jout  0/ .                                           (a) FCFS.            (b) Original MLFQ.
5: // Skip-join newly arrival jobs.

6: for job  Jin do                                                                                               

7:   init_time  P.getNextIterTime( job)                                                                          

8:   p job  min i, s.t. qi  init_time

9:   Qpjob .push( job)                                                (c) Skip-Join MLFQ.      (d) SRPT.

10: for job  {Q1, Q2, ..., Qn} do                                     Figure 7: Execution timeline under different scheduling algorithms.

11:  job.out putNewGeneratedToken()                                   Example. Figure 7 shows an example to demonstrate the
                                                                      effectiveness of FastServe's skip-join MLFQ scheduler. In
12:  p job  job.getCurrentPriority()                                  the example, three jobs arrive at the same time in the order
                                                                      of J1, J2, J3. T1(Ji) denotes the initialization phase time of job
13:  if job.isFinished() then                                         Ji, and T2(Ji) denotes the decoding phase time. We assume
                                                                      that both skip-join and original MLFQ utilize four priority
14:  Qpjob .pop( job)                                                 queues with quantum values of 1, 2, 4, and 8. Additionally,
                                                                      SRPT serves as the oracle with the optimal average latency.
15:  // Demote jobs.
                                                                         As Figure 7 shows, the average latency of FCFS, original
16:  if job.depleteQuantum() then                                     MLFQ, skip-join MLFQ, and SRPT are 4.23, 5, 3.3, and 3,
                                                                      respectively. FCFS and original MLFQ encounter the head-of-
17:  Qpjob .pop( job), Qpjob+.push( job)                              line blocking problem, where job J1 blocks the remaining jobs,
                                                                      leading to long average latency. Skip-join MLFQ addresses
18:  // Promote starved jobs.                                         this issue by skip-joining job J1 to the low-priority queue,
                                                                      achieving performance similar to optimal SRPT. Generally,
19:  if job.starveTime   then                                         algorithms that have access to more information perform
                                                                      better than those with limited information.
20:  Qpjob .pop( job), Q1.push( job)
                                                                      Algorithm. Algorithm 1 shows the pseudo-code of the skip-
21:  job.starveTime  0                                                join MLFQ scheduler. The scheduler has a set of priority
                                                                      queues Q1, Q2, ..., Qn with quantum values q1, q2, ..., qn, and
22: // Schedule jobs to execute.                                      receives a set of newly arrived jobs Jnew. It schedules a batch
                                                                      of MaxBatchSize jobs for execution. The skip-join part (
23: for job  {Q1, Q2, ..., Qn} do                                     in Figure 6) corresponds to lines 6­9, and the demotion and
                                                                      starvation prevention parts ( and  in Figure 6) correspond
24:  if job.isReady() and |Jout | < MaxBatchSize then                 to lines 16­17 and lines 19­21, respectively. There are two
                                                                      notable details. First, the scheduler demotes a job to an  times
25:  Jout .push( job)                                                 lower priority queue based on its next iteration time. FastServe
                                                                      sets the quantum of the lower priority queue to two times of
while fixing the hardware and model. As for the decoding              that of the higher priority queue, which aligns with previous
phase, the iteration time is roughly constant.                        work [34] on MLFQ. The quantum of the highest priority
                                                                      queue is set to the minimum iteration time. The second detail
   Based on this insight, we propose a skip-join MLFQ sched-          concerns how the scheduler identifies starved jobs governed
uler tailored for LLM inference. Our scheduler efficiently            by the parameter . FastServe tunes  based on the user-
manages the movement of jobs among various priority queues            specified SLO, which is set to 300 ms by default.
in a skip-join manner. In the MLFQ, we have n priority
queues, namely Q1, Q2, ..., Qn, each with a distinct quantum          4.2 Proactive Key-Value Cache Management
q1 < q2 < ... < qn. The conventional MLFQ scheduler ini-
tially assigns a newly arrived job to the highest priority queue,     Although the skip-join MLFQ scheduler provides iteration-
i.e., Q1. Once the job exhausts the allocated quantum in Q1,          level preemption to approximate SRPT to achieve lower la-
it is subsequently demoted to Q2. As shown in Figure 6, Fast-         tency without prior knowledge of the exact job size, it exac-
Serve differs from the original MLFQ in that when a job               erbates the pressure of GPU memory consumption. Figure 8
arrives, FastServe leverages accurate profiling to predict the        shows the key-value cache memory consumption of FCFS
initialization phase time (tinit ) and  skip-joins the job to the     and skip-join MLFQ for OPT 2.7B model under a synthetic
highest priority queue (qi) subject to qi  tinit . When a job
consumes its allotted quantum before completion, the sched-
uler demotes  the job based on its current priority and next
iteration time.

Avoiding perpetual starvation. It is important to note that the
skip-join and demotion operations may result in starvation for
jobs with long input and output. To address this problem, the
scheduler periodically examines the starved jobs and  pro-
motes them to the highest priority queue, i.e., Q1. This allows
FastServe to address head-of-line blocking while mitigating
starvation. We evaluate tail latency and show the effectiveness
of the starvation prevention mechanism in §6.2.

                                                                   6
workload. Although we choose a relatively small model and           KV Cache Memory (GB)  20                       FCFS
limit the maximum output length to 20, the peak KV cache                                                  MLFQ
memory overhead for skip-join MLFQ can be 7× larger than
that of FCFS. The GPU memory demand becomes even more                                     15
pronounced when deploying larger LLMs like OPT 175B.
                                                                                          10
   The reason under the hood is that compared to the run-to-
completion policy in the existing serving systems, iteration-                              5
level preemption provided by the skip-join MFLQ increases
the number of ongoing jobs in the system. Except for the                                  0  1                  2            3  4  5
key-value tensors of running jobs, the skip-join scheduler also
needs to store the key-value tensors for preempted jobs at the                                                     Time (s)
pending state. Unlike process states in the traditional operat-
ing system schedulers, the intermediate state, i.e., key-value      Figure 8: The key-value cache memory consumption for OPT 2.7B
tensors, of each job is much larger. Formally, for a particular     under different schedulers. The workload follows a Gamma Process
LLM inference serving job, denote the input sequence length         with rate=64 and CV=4. The maximum output length is set to 20..
by s, the output sequence length by t, the hidden dimension
of the transformer by h, and the number of transformer lay-         starvation avoidance, the killed jobs may be promoted to the
ers by l. If the model weights and all computations are in          highest-priority queue if they wait longer than the specified
FP16, the total number of bytes to store the key-value cache        STARV ELIMIT . In this case, the promoted job may kill the
for this single job is 4 × lh(s + t). Take OPT 175B as an ex-       currently executing job, which may just kill the promoted job
ample (l = 96, h = 12288). Given an input sequence length           in the previous step. It potentially results in a deadlock.
s = 512 and a minimum output sequence length t = 1, the
GPU memory overhead for a single job is as high as 2.3GB.           Our solution: proactive key-value cache swapping. Under
As the generation continues, its output sequence length t will      the strict GPU memory capacity constraints, the two strawman
increase, which further increases the GPU memory overhead.          solutions have to sacrifice either the performance of newly ar-
                                                                    rived jobs or the efficiency of low-priority jobs. To overcome
   At the same time, GPU memory is a scarce resource when           this dilemma, our key observation is that the key-value ten-
deploying LLMs. Typically, GPU memory is much smaller               sors only need to be reserved in the GPU memory when their
than the host memory. For instance, NVIDIA A100 GPU has             corresponding jobs get scheduled. Based on this observation,
a maximum of 80 GB GPU memory. Besides, a large portion             FastServe extends the space of the key-value cache from GPU
of GPU memory is provisioned to store weights of LLMs.              memory to the host memory. FastServe swaps out inactive
The space to store key-value tensors for jobs is limited. As        key-value tensors of jobs to the host memory to accommodate
a result, the GPU memory capacity constrains the potential          additional pending jobs, and swaps in key-value tensors back
benefits of the skip-join MLFQ scheduler.                           to the GPU memory for upcoming jobs.

Strawman solution 1: defer newly arrived jobs. To avoid                However, the overhead of swapping is not negligible com-
out-of-memory (OOM) errors, a naive solution is to simply           pared to the token generation time. When deploying OPT
defer the execution of newly arrived jobs when the GPU mem-         175B on 16 NVIDIA A100 GPUs, the key-value tensors of a
ory is not sufficient and keep scheduling current in-memory         job can occupy 2.3 GB memory. The token generation time in
jobs until they finish. This straightforward solution is widely     the decoding phase is about 60 ms, while the time to swap the
used in existing serving systems, such as vLLM [11]. In this        key-value tensors between host memory and GPU memory
manner, although new jobs are assigned with higher priority,        with PCIe 4.0×16 full bandwidth is about 36 ms. Therefore, a
they are blocked to await the free memory space. Under ex-          simple reactive swapping mechanism that processes swapping
treme GPU memory-constrained settings (e.g., long sequence          and inference sequentially introduces a large overhead.
inference), this solution would degenerate MLFQ to FCFS,
which again suffers from head-of-line blocking.                        Instead, FastServe employs a proactive key-value cache
                                                                    swapping algorithm to mitigate the adverse effects of swap-
Strawman solution 2: kill and re-compute low-priority               ping overhead. The key insight is to overlap the LLM infer-
jobs. Another straightforward solution is to kill some low-         ence for running jobs with the data transmission for pending
priority jobs and release their key-value cache to make room        jobs so that the swapping overhead is out of the critical path
for newly arrived high-priority jobs. This solution has two         of LLM inference. Figure 9 illustrates an example. Instead
problems. First, the killed jobs lose their generation states,      of swapping key-value tensors of pending job J2 after job
necessitating to rebuild their key-value tensors. This results      J1 is preempted or finished, the proactive algorithm swaps
in the waste of valuable computational resources and time.          the key-value tensors of J2 in advance. In this way, the swap-
Second, it may cause deadlocks. When the high-priority jobs         ping overhead of J2 effectively overlaps with the GPU kernel
arrive, ongoing jobs with lower priority are killed. Due to the     execution of J1, thereby achieving high GPU utilization. As
                                                                    swapping in one job consumes expensive GPU memory, the
                                                                    job swapping order is crucial for GPU memory efficiency.

                                                                    Job swapping order. Frequently swapping in and out unnec-
                                                                    essary key-value tensors incurs additional thrashing overhead

                                                                 7
GPU   Exec(  )                  Exec(  )                 Exec(  )                      Swapping      Swapping     Intermediate
PCIe                                                                                   Operations   Instructions     Results
                  Swap(  )                Swap(  )
                                                                                               GPU
                        (a) Reactive swapping

GPU   Exec(  )       Exec(  )   Exec(  )        ...                   Key-Value Cache               GPU           GPU
PCIe    Swap(  )      Swap(  )                                           Manager

                        (b) Proactive swapping

      Figure 9: Comparison of reactive and proactive swapping.                         Host Memory  Host Memory   Host Memory

if swapping in and out one job with high priority. The swap-          Figure 10: Overlapping key-value cache offloading with intermediate
ping overhead can increase to exceed the execution time,              result transmission.
leading to a deterioration in the performance of overlapping.
To address this issue, FastServe calculates the estimated next        Handling a burst of new jobs. The proactive key-value cache
scheduled time (ENST) for each job to decide the swapping             swapping strategy is designed for the skip-join MLFQ sched-
order. The ENST is the time when the job will be scheduled            uler. In scenarios where a significant influx of new jobs (with
to execute next time. The job with the largest ENST will be           high priority) occurs, the cache management system is forced
swapped out first, and the job with the smallest ENST will            to evict jobs reactively, adversely affecting the performance
be swapped in first. Typically, a job with lower priority is          of these new jobs. To mitigate this, FastServe reserves some
scheduled for later execution. However, owing to the star-            idle key-value cache slots specifically for new jobs, ensur-
vation prevention mechanism, a job of lower priority might            ing immediate availability without the need for reactive job
be elevated to a higher priority queue. Consequently, even a          swapping. This approach guarantees the performance of new
low-priority job can sometimes be executed first.                     jobs. The number of idle slots is based on historical job arrival
                                                                      patterns. A higher frequency of job bursts necessitates a larger
   In this case, for job i, FastServe considers the time to pro-      number of reserved slots.
mote this job and the sum of execution time of all jobs with
higher priorities before executing i simultaneously. Formally,        4.3 Support for Distributed LLM Serving
let the time threshold for promoting job i be Tpromote(i). As
for the sum of execution time of all jobs with higher priorities      Previous research shows that the effectiveness of LLMs em-
before executing i, we assume those jobs do not finish earlier        pirically adheres to the scaling law concerning the quantity
before being demoted to the priority queue of job i. The exe-         of model parameters [36]. However, it is important to note
cution time of job j with a higher priority can be calculated         that the memory usage of an LLM also exhibits proportion-
as follows (i.e., job j is demoted from j.priority to i.priority      ality to the number of parameters. A prime example is OPT
one by one):                                                          175B, which, even when stored in half-precision, demands a
                                                                      staggering 350GB of GPU memory solely to accommodate
      Texecute(i, j) =                               qk               its weights. Furthermore, additional memory is required for
                                                                      handling intermediate states during runtime. Therefore, LLM
                            i.priority<k j.priority                   often needs to be split into multiple pieces and served in a
                                                                      distributed manner with multiple GPUs.
where i.priority is the priority of job i, and qk is the quantum
of the priority queue with priority k. Based on this, the sum            Tensor parallelism [16,37] and pipeline parallelism [17,38]
of execution time of all jobs with higher priorities than job i       are two most widely-used techniques for distributed LLM
is defined as:                                                        serving. FastServe supports the hybrid of these two parallel
                                                                      techniques for serving LLMs. An LLM is composed of a
                     1                     Texecute(i,   j)           series of operators over multi-dimensional tensors. Tensor
                                                                      parallelism splits each operator across multiple devices, with
                        i.priority< j.priority                        each device executing a portion of the computation in paral-
       Texecute(i)=                                                   lel. Additional communication overhead is required to split
                     B                                                the input and gather the output from different GPUs. Ten-
                                                                      sor parallelism substantially augments both computational
where B is the maximum batch size of jobs. At last, taking            and memory resources available to a single job, consequently
both the promotion for starvation prevention and the execution        reducing the time of each iteration.
of higher priority jobs into consideration, the ENST of job i
is calculated as:                                                        Pipeline parallelism splits the entire operators of an LLM
                                                                      computation graph into multiple stages and executes them on
      ENST (i) = min(Tpromote(i), Texecute(i))                        different GPUs in a pipeline fashion. During inference, each
                                                                      stage computes a part of the entire computation graph and
   This ENST definition serves as a means to estimate the             transmits the intermediate results to the next stage in parallel.
expected scheduling time for the next generation of job i.            Pipeline parallelism requires less communication overhead
Therefore, using this metric to decide the order of swapping
makes the key-value tensors of active jobs reside predomi-
nantly in GPU memory, and those of inactive jobs are more
inclined to reside in host memory.

                                                                   8
compared to tensor parallelism, while also affording LLMs             Model    Size # of Layers # of Heads Hidden Size
the ability to surpass the memory constraint of an individual
GPU. Since multiple processing batches are under processing           OPT-13B 26GB    40    40                 5120
simultaneously in different stages, FastServe needs to handle
multiple batches in the distributed engine at the same time.          OPT-66B 132GB   64    72                 9216

Job scheduling in distributed serving. In the traditional             OPT-175B 350GB  96    96                 12288
MLFQ, if no new job arrives, the scheduler schedules the job
with the highest priority and executes it until it finishes or                 Table 1: Model configurations.
is demoted. However, with pipeline parallelism, the sched-
uler schedules at the granularity of individual stage. Once a             FasterTransformer [26] vLLM [11] FastServe-FCFS FastServe
job completes its first stage and transmits the intermediate
results to the subsequent stage, a decision point arises for          IP                                       
the scheduler regarding the next job to set in motion. In this
case, the scheduler cannot follow the traditional MLFQ that           IS                                       
keeps scheduling the same job until demotion, because the
job is still in progress. To preserve the semantics of MLFQ,          PA                                       
FastServe still keeps the running job in the priority queue,
but schedules the highest priority job in the pending state.          PP                                       
Thus, the early jobs in a queue can expedite their quantum
completion.                                                           Table 2: Comparison between FastServe and baselines.
                                                                      IP=Iteration-level Preemption, IS=Iteration-level Scheduling,
Key-value cache management in distributed serving.                    PA=PagedAttention, PP=Pipeline Parallelism.
Given that the key-value cache occupies a large fraction of
GPU memory, the key-value cache of FastServe is also par-             2.9K lines of Python code. The distributed execution engine
titioned across multiple GPUs. In LLM inference, each key-            is implemented with 8.1K lines of C++/CUDA code. The
value tensor is used by the same stage of the LLM. Therefore,         frontend supports OpenAI API compatible interface where
FastServe partitions key-value tensors as tensor parallelism          clients can specify the sampling parameters like maximum
requires, and assigns each key-value tensor to the correspond-        output length and temperature. The scheduler implements the
ing GPU so that all computation on a GPU only needs local             skip-join MLFQ and proactive swapping policies. The dis-
key-value tensors on the same GPU.                                    tributed execution engine uses Ray [39] actor to implement
                                                                      GPU workers which execute the LLM inference and manage
   The proactive key-value cache swapping mechanism of                the key-value cache in a distributed manner. We implement
FastServe is also distributed. Because different stages of the        popular open-source LLMs such as OPT in C++ to achieve
LLM process different jobs at the same time, each stage may           better performance and scalability than the popular Python
offload or upload different key-value tensors independently.          implementations in Huggingface [25]. We also implement
To reduce redundant control, before processing the interme-           custom CUDA kernels to support Orca's [10] iteration-level
diate result sent from the previous stage, the current stage          scheduling and vLLM's [11] PagedAttention.
does the same offloading or uploading action as the previous
stage does. The intermediate result transmission and key-             6 Evaluation
value cache swapping occur in parallel, so the overhead of
key-value cache swapping is further reduced. As shown in              In this section, we first demonstrate the end-to-end perfor-
Figure 10, when the intermediate result is sent to the next           mance improvements of FastServe over state-of-the-art LLM
stage, the next stage receives the swapping instructions and          serving systems. Then, we evaluate the design choices Fast-
can swap the key-value cache at the same time if needed. The          Serve and show the effectiveness of each component.
key-value cache swapping mechanism only needs to decide
the offloading or uploading of the first stage. When using ten-       6.1 Methodology
sor parallelism splitting the first stage into multiple chunks, a
centralized key-value cache swapping manager instructs all            Testbed. The end-to-end experiments (§6.2) use two AWS
chunks in the first stage to offload or upload the key-value          EC2 p4d.24xlarge instances. Each instance is configured with
tensors owned by the same job.                                        eight NVIDIA A100 40GB GPUs connected over NVLink,
                                                                      1152 GB host memory, and PCIe 4.0×16. Due to the limited
5 Implementation                                                      budget, the experiments for design choices (§6.3) use one
                                                                      NVIDIA A100 40GB GPU in our own testbed to validate the
FastServe is a distributed LLM inference serving system with          effectiveness of each component.
a RESTful API frontend, a scheduler, and a distributed execu-
tion engine. The frontend and scheduler are implemented with          LLM models. We choose the representative LLM family,
                                                                      OPT [18], which is widely used in both academia and indus-
                                                                      try. We select common model sizes. Table 1 lists the model
                                                                      configurations. We use FP16 precision in all experiments.

                                                                      Workloads. Similar to prior work on LLM serving [11], we
                                                                      generate workloads based on ShareGPT [13] and Alpaca [14]
                                                                      datasets. These datasets contain real-world inputs and out-
                                                                      puts of LLM services. The ShareGPT dataset is composed of
                                                                      user-shared conversations with ChatGPT [13]. The Alpaca

                                                                   9
                               SLO                       FasterTransformer                 vLLM                      FastServe-FCFS              FastServe

Latency (s/token)  0.5                                      Latency (s/token)  0.5                                      Latency (s/token)  0.5

                   0.4                                                         0.4                                                         0.4

                   0.3                                                         0.3                                                         0.3

                   0.2                                                         0.2                                                         0.2

                   0.1                                                         0.1                                                         0.1

                   0.00  1  2  3    4                 5  6                     0.00  1  2      3  4               5  6                     0.00 1 2 3 4 5 6 7
                            Job Arrival Rate (job/s)                                    Job Arrival Rate (job/s)                                         Job Arrival Rate (job/s)

                   (a) OPT-13B, 1 GPU, ShareGPT.            (b) OPT-66B, 4 GPUs, ShareGPT.                              (c) OPT-175B, 16 GPUs, ShareGPT.

                               SLO                       FasterTransformer                 vLLM                   FastServe-FCFS                         FastServe
                                                                                                                                                 5 10 15 20 25 30
Latency (s/token)  0.5                                      Latency (s/token)  0.5                                      Latency (s/token)  0.5
                                                                                                                                                   Job Arrival Rate (job/s)
                   0.4                                                         0.4                                                         0.4

                   0.3                                                         0.3                                                         0.3

                   0.2                                                         0.2                                                         0.2

                   0.1                                                         0.1                                                         0.1

                   0.00 5 10 15 20 25 30 35 40                                 0.00  5     10 15 20 25                                     0.00
                            Job Arrival Rate (job/s)                                    Job Arrival Rate (job/s)

                   (a) OPT-13B, 1 GPU, Alpaca.                                 (b) OPT-66B, 4 GPUs, Alpaca.             (c) OPT-175B, 16 GPUs, Alpaca.

                               Figure 11: Average latency of different serving systems with OPT models on real workloads.

dataset is generated by GPT-3.5 with self-instruct [14]. Since                                    · FastServe-FCFS: It uses the same distributed execution
these datasets do not include the arrival time, we follow prior                                     engine of FastServe, but it does not use techniques proposed
work [11] to generate the arrival time for each request follow-                                     in §4. This baseline helps differentiate the speedup brought
ing a Poisson process parameterized by the arrival rate.                                            by the techniques proposed in this paper from that by the
                                                                                                    efficient implementation of FastServe.
Evaluation metrics. The user-perceived latency is a criti-
cal measurement for interactive applications like ChatGPT.                                        6.2 End-to-End Performance
Specifically, similar to prior work on LLM serving [10, 11],
average per-token latency is calculated as the mean of ev-                                        We compare the end-to-end performance of FastServe to the
ery job's end-to-end latency divided by its output length. In                                     three baseline systems under ShareGPT and Alpaca work-
addition, we also report the P95 tail latency.                                                    loads on OPT-13B, OPT-66B and OPT-175B in Table 1.

   For comparison, we set a latency SLO and compare the                                              The first row of Figure 11 shows the end-to-end perfor-
maximum throughput each system can achieve under the SLO.                                         mance of all the systems under the ShareGPT dataset. Al-
We follow prior work [40] to set the latency SLO to 10× of the                                    though FasterTransformer implements highly optimized GPU
latency of a single iteration in the decoding phase. Specifically,                                kernels for LLM inference, it does not support iteration-level
we set SLO to 0.3 seconds based on our profiling.                                                 scheduling. It cannot return early finished jobs in the ongoing
                                                                                                  batch and add new jobs into the batch to reduce latency. As a
Baselines. We compare FastServe with three baselines. For                                         result, FasterTransformer suffers from the significant head-of-
fair comparison, all baselines use the same tensor parallelism                                    line blocking even when the job arrival rate is small. FastServe
size, pipeline parallelism size, and batch size as FastServe,                                     outperforms FasterTransformer by 31.5­74.9× in terms of
except that vLLM only uses tensor parallelism to serve OPT-                                       throughput under the SLO. As the state-of-the-art serving
175B, because it does not support pipeline parallelism. Table 2                                   system, vLLM is equipped with most of the techniques to
shows the comparison between FastServe and baselines.                                             accelerate inference and reduce GPU memory consumption.
                                                                                                  However, because vLLM schedules jobs in an FCFS man-
· FasterTransformer [26]: It is a production-grade infer-                                         ner, a large portion of the end-to-end latency is the queuing
  ence engine from NVIDIA. It supports both tensor paral-                                         delay. Optimizing the execution time of the LLM inference
  lelism and pipeline parallelism. However, it adopts job-level                                   job is not enough. Equipped with the skip-join MLFQ sched-
  scheduling and short jobs are blocked by long jobs in the                                       uler, FastServe can significantly reduce the queuing delay
  same batch. We use FasterTransformer v5.3.                                                      and outperform vLLM by 2.3­18.3×. It is worth noting that
                                                                                                  FastServe-FCFS also outperforms vLLM, because it uses
· vLLM [11]: It is the state-of-the-art LLM serving system                                        more efficient C++ implementation and fuses more opera-
  that supports iteration-level scheduling [10] and Paged-                                        tions into fewer GPU kernels. But it still suffers from the
  Attention [11] to reduce memory fragmentation caused                                            head-of-line blocking problem, which makes it slower than
  by key-value cache. However, it uses a simple FCFS sched-                                       FastServe by 2­4×.
  uler with run-to-completion execution, which suffers from
  head-of-line blocking. We use vLLM v0.1.7.

                                                                                           10
                                                       SLO               FasterTransformer                                vLLM           FastServe-FCFS                                               FastServe

P95 Latency (s/token)  1.0                                                                  P95 Latency (s/token)1.0                                                                 1.0

                       0.5                                                                                                               Norm. Latency (s/token)0.5                  0.5

                       0.00     1     2                3        4        5                                                                                    P95 Latency (s/token)0.00123450.00 1 2 3 4 5 6 7
                                   Job Arrival Rate (job/s)                                               Job Arrival Rate (job/s)                                                                  Job Arrival Rate (job/s)

(a) OPT-13B, 1 GPU, ShareGPT.                                                                (b) OPT-66B, 4 GPUs, ShareGPT.                                                        (c) OPT-175B, 16 GPUs, ShareGPT.

                                                       Figure 12: Tail latency of different serving systems with OPT models on real workloads.

                                          vLLM               FastServe-FCFS            FastServe                                                                                   FCFS               Naive MLFQ
                                                                                                                                                                                   Fixed Priority     SJ-MLFQ
SLO Attainment (%)     100                             100                             100

                       75                              75                              75                                        1.0
                       50 0.63 1.77 2.59               50 0.64                         50 0.76 2.05 3.15
                                                                   1.83  3.01

                       25                              25                              25                                        0.5

                       00    1     2  3                4 00  1     2     3             4 00        2      4

                             Job Arrival Rate (job/s)        Job Arrival Rate (job/s)        Job Arrival Rate (job/s)            0.0

                             (a) 5× SLO. (b) 10× SLO. (c) 20× SLO.                                                                    0.25  1.0                                           4.0         16.0  64.0  256.0
                                Figure 13: P95 goodput under different SLOs.
                                                                                                                                                                                               Ratio

                                                                                                                                      Figure 14: Effectiveness of skip-join MLFQ.

   The second row of Figure 11 shows the experiment results                                                                      problem and does not hurt the tail latency. Even for long jobs,
under the Alapca dataset. Since the job size of Alpaca is                                                                        FastServe can still accelerate them by reducing their queuing
smaller than that of the ShareGPT dataset, all serving sys-                                                                      delay. The starvation prevention mechanism ensures that long
tems can maintain low latency even when the rate is relatively                                                                   jobs can be scheduled in a reasonable time.
higher than that under the ShareGPT dataset. However, the
performance gain of FastServe is similar. Without iteration-                                                                     Impact on goodput. We further investigate the impact on
level scheduling, FasterTransformer is the slowest system and                                                                    goodput for different systems under various SLOs when serv-
FastServe outperforms it by 9.5­15.8×. vLLM achieves better                                                                      ing OPT-13B model. Similar to previous works [35, 41, 42],
performance than FasterTransformer, but it still suffers from                                                                    we measure the P95 goodput, defined as the throughput when
the head-of-line blocking problem. As a result, FastServe                                                                        95% of jobs can be completed within the SLO of the initial-
outperforms vLLM by 3­31.4×. With our efficient implemen-                                                                        ization phase and the decoding phase. We set the SLO of two
tation, FastServe-FCFS also outperforms vLLM, but it is still                                                                    phases to 5×, 10×, and 20× of the latency in the correspond-
slower than FastServe by 1.6­2×.                                                                                                 ing phase under light load. As shown in Figure 13, FastServe
                                                                                                                                 consistently achieves the highest P95 goodput across different
Impact on tail latency. A potential concern of preemptive                                                                        SLOs. Specifically, FastServe outperforms vLLM by 4.1×
scheduling and MLFQ is that it can cause starvation for long                                                                     to 4.7× and FastServe-FCFS by 1.46× to 1.64 ×. These
jobs and hurt tail latency. FastServe incorporates a starva-                                                                     results demonstrate that FastServe effectively improves the
tion prevention mechanism in its skip-join MLFQ scheduler                                                                        system's throughput without violating the SLOs of both the
(§4.1). To demonstrate the effectiveness of the starvation                                                                       initialization phase and the decoding phase.
prevention mechanism, we measure the 95% latency of all
the systems under the ShareGPT dataset. As shown in Fig-                                                                         6.3 Design Choices
ure 12, FastServe significantly improves the throughput of
LLM inference jobs under the same SLO requirement for tail                                                                       Effectiveness of skip-join MLFQ. To show the effectiveness
latency. For example, when serving OPT-175B, compared to                                                                         of the skip-join MLFQ, we conduct a performance compari-
FastServe-FCFS, FastServe improves the throughput by up to                                                                       son against the FCFS, naive MLFQ, and Fixed Priority when
1.5×. FastServe also outperforms FastServe-FCFS by 2­2.8×                                                                        serving OPT-13B. We use the ShareGPT dataset to generate
when serving OPT-13B and OPT-66B. FastServe achieves up                                                                          jobs and alter the ratios between input and output lengths
to 17.9× and 59.8× performance improvement compared                                                                              while preserving the original length distribution. This adjust-
to vLLM and FasterTransformer, respectively. The results                                                                         ment reflects the current industry trend of expanding input
show that although FastServe is designed to reduce average                                                                       token limits in LLMs [4, 5, 42]. Figure 14 shows the results
latency, it can also significantly reduce tail latency of LLM in-                                                                when varying the ratio between input and output lengths. The
ference jobs. Prioritizing short jobs with the skip-join MLFQ                                                                    latency is normalized by the slowest system. The FCFS con-
scheduler can effectively reduce the head-of-line blocking                                                                       sistently experiences high latency due to the head-of-line
                                                                                                                                 blocking issue, regardless of the ratio. The naive MLFQ has

                                                                                                                          11
     Proactive  Reactive  Recompute Queuing                            Swapping  Execution            queuing delay, execution time, and swapping time. The swap-
                                                                                                      ping time is the time when the job is blocked by the proactive
0.4                                    100                                                            swapping mechanism. As shown in Figure 15(b), the swap-
Latency (s/token)                                                                                     ping time is less than 5% of the end-to-end latency, which is
                                               Time Breakdown (%)0.380                                negligible compared to the execution time and queuing delay.
                                                                                                      The reason confirms that the proactive swapping mechanism
                                                60                                                    can overlap most of the swapping time with the execution time
0.2                                                                                                   of other jobs. As a result, the proactive swapping mechanism
                                                                                                      nearly does not affect the end-to-end latency.
                                                40
                                                                                                      7 Related Work
0.1                                                                20
                                                                                                      Preemptive scheduling. Many solutions for job schedul-
0.00.0 0.5 1.0 1.5 2.0 2.5 3.0                                     0 0.5 1.0 1.5 2.0 2.5 3.0          ing in datacenters use preemptive scheduling. Many net-
       Job Arrival Rate (job/s)                                         Job Arrival Rate (job/s)      worked systems [15, 31, 32, 43] use preemptive flow schedul-
                                                                                                      ing to minimize flow completion time. Many schedulers for
(a) OPT-13B on ShareGPT. (b) Latency breakdown.                                                       latency-sensitive datacenter workloads, such as Shinjuku [44],
                                                                                                      Shenango [45], and Caladan [46], also use fine-grained pre-
Figure 15: Effectiveness of proactive key-value cache management.                                     emption and resource reallocation to optimize microsecond-
(a) Comparison between different key-value cache management                                           scale tail latency. As for DL workloads, Tiresias [34] uses
policy. (b) Latency breakdown of FastServe.                                                           MLFQ to optimize job completion time for distributed DL
                                                                                                      training jobs. Pipeswitch [47] and REEF [48] provide efficient
good performance under a small ratio since the difference                                             GPU preemption to run both latency-critical and best-effort
between the initialization and decoding phases is minimal.                                            DL tasks on the same GPU. Different from them, FastServe
However, as the ratio increases, the naive MLFQ struggles                                             targets a new scenario, LLM inference serving.
with the prolonged initialization phase. In contrast, the Fixed
Priority excels with larger ratios, where the initialization phase                                    Inference serving. Many traditional model serving sys-
dominates execution time, but underperforms when the ratio                                            tems [8, 9, 22, 23, 49] only focus on serving relatively small
is low, as it neglects the decoding phase when setting prior-                                         models in a cluster without awareness of characteristics of
ity. Benefiting from a semi information-agnostic scheduling                                           LLMs. Recently, several serving systems are proposed to op-
policy, the skip-join MLFQ consistently improves the perfor-                                          timize Transformer-based LLMs [10, 35, 50­52]. Orca [10]
mance compared to FCFS, naive MLFQ, and Fixed Priority                                                and vLLM [11] considers the autoregressive generation pat-
by up to 8.9×, 1.87×, and 13.9×.                                                                      tern of LLMs. However, due to their FCFS policy, they suffer
                                                                                                      from severe head-of-line blocking problem. VTC [52] focus
Effectiveness of proactive key-value cache management.                                                on the fairness of LLM serving but does not consider the
To show the effectiveness of the proactive key-value cache                                            preemption scenario. Splitwise [53] and DistServe [41] dis-
management, we evaluate the performance of FastServe with                                             aggregates the prefill and decoding phase to eliminate the
two baseline strategies Recompute and Reactive mentioned                                              interference between them and thus optimize execution la-
in §4.2 when serving OPT-13B on the ShareGPT dataset.                                                 tency. LoongServe [42] uses elastic sequence parallelism to
Figure 15(a) shows the results. At low arrival rates, the GPU                                         dynamically set degree of parallelism for different requests at
memory is sufficient to accommodate the key-value caches for                                          different phases. These systems are orthogonal to FastServe.
all jobs, making the performance of the three strategies simi-
lar. As the arrival rate increases, the GPU memory becomes                                            Memory optimization for LLMs. Due to high memory us-
insufficient so that systems have to preempt some key-value                                           age for LLMs, many techniques have been proposed to re-
caches for other jobs, leading to distinct performance.                                               duce memory overhead. Some work [54, 55] targets train-
                                                                                                      ing, which is orthogonal to the serving scenario. Quantiza-
   In such case, Recompute discards the key-value caches for                                          tion [56­59] compresses the model weights into lower pre-
low-priority jobs, increasing the re-computation overhead for                                         cision after training to reduce the memory footprint during
these jobs' KV caches. As shown in Figure 15(a), this re-                                             inference. SparTA [60] exploits model sparsity to accelerate
computation overhead makes the proactive-swapping strategy                                            computation. However, these approaches sacrifice the model
outperforms recomputation by 2.7×.                                                                    accuracy. vLLM [11] proposes PagedAttention to reduce the
                                                                                                      GPU memory fragmentation. This is orthogonal to this paper
   As for the Reactive, it swaps out low-priority jobs to host                                        and FastServe implements PagedAttention as well.
memory when GPU memory is inefficient and swaps in these
jobs if needed. The data transfer is in the critical path. Subse-
quent computation must wait for these transfers. Conversely,
proactive-swapping anticipates the memory requirements of
new incoming jobs and preempts low-priority caches in ad-
vance. Similarly, when a high-priority job's cache is detected
in host memory and GPU memory is available, it will be
proactively swapped into the GPU memory. This allows the
data transfer overlap with computation and achieves 1.7×
improvement over reactive approach.

   To further investigate the overhead of the proactive swap-
ping mechanism, we split the end-to-end latency as three parts:

                                                                                                  12
8 Conclusion                                                       [11] W. Kwon, Z. Li, S. Zhuang, Y. Sheng, L. Zheng, C. H.
                                                                         Yu, J. E. Gonzalez, H. Zhang, and I. Stoica, "Efficient
We present FastServe, a distributed inference serving system             memory management for large language model serving
for LLMs. We exploit the autoregressive pattern of LLM infer-            with pagedattention," in ACM SOSP, 2023.
ence to enable iteration-level preemption and design a novel
skip-join MLFQ scheduler to address head-of-line blocking          [12] K. Kaffes, T. Chong, J. T. Humphries, A. Belay, D. Maz-
problem. We propose a proactive key-value cache manage-                  ières, and C. Kozyrakis, "Shinjuku: Preemptive schedul-
ment mechanism to handle the memory overhead of the key-                 ing for µsecond-scale tail latency," in USENIX NSDI,
value cache and hide the data transmission latency with com-             2019.
puting. Based on these, we build a prototype of FastServe.
Experiments show that FastServe improves the throughput            [13] "Sharegpt teams." https://sharegpt.com/, 2023.
by up to 31.4× and 17.9× under the same average and tail
latency SLOs respectively, compared to vLLM.                       [14] R. Taori, I. Gulrajani, T. Zhang, Y. Dubois, X. Li,
                                                                         C. Guestrin, P. Liang, and T. B. Hashimoto, "Stanford
References                                                               alpaca: An instruction-following llama model." https:
                                                                         //github.com/tatsu-lab/stanford_alpaca, 2023.
 [1] "Introducing ChatGPT." https://openai.com/blog/
      chatgpt, 2022.                                               [15] W. Bai, L. Chen, K. Chen, D. Han, C. Tian, and H. Wang,
                                                                         "Information-agnostic flow scheduling for commodity
 [2] "ChatGPT sets record for fastest-growing user base."                data centers," in USENIX OSDI, 2015.
      https://www.reuters.com/technology/chatgpt-
      sets-record-fastest-growing-user-base-                       [16] M. Shoeybi, M. Patwary, R. Puri, P. LeGresley, J. Casper,
      analyst-note-2023-02-01/, 2023.                                    and B. Catanzaro, "Megatron-lm: Training multi-billion
                                                                         parameter language models using model parallelism,"
 [3] "Reinventing search with a new ai-powered bing                      arXiv, 2020.
      and edge, your copilot for the web." https://
      news.microsoft.com/the-new-Bing/, 2023.                      [17] Y. Huang, Y. Cheng, A. Bapna, O. Firat, M. X. Chen,
                                                                         D. Chen, H. Lee, J. Ngiam, Q. V. Le, Y. Wu, and Z. Chen,
 [4] Google, "Our next-generation model: Gemini 1.5."                    "Gpipe: Efficient training of giant neural networks using
      https://blog.google/technology/ai/google-                          pipeline parallelism," Neural Information Processing
      gemini-next-generation-model-february-                             Systems, 2019.
      2024/, 2024.
                                                                   [18] S. Zhang, S. Roller, N. Goyal, M. Artetxe, M. Chen,
 [5] Anthropic, "Introducing the next generation of Claude."             S. Chen, C. Dewan, M. Diab, X. Li, X. V. Lin, T. Mi-
      https://www.anthropic.com/news/claude-3-                           haylov, M. Ott, S. Shleifer, K. Shuster, D. Simig, P. S.
      family, 2024.                                                      Koura, A. Sridhar, T. Wang, and L. Zettlemoyer, "Opt:
                                                                         Open pre-trained transformer language models," arXiv,
 [6] "Introducing Qwen." https://qwenlm.github.io/                       2022.
      blog/qwen/, 2023.
                                                                   [19] T. B. Brown, B. Mann, N. Ryder, M. Subbiah, J. Ka-
 [7] K. He, X. Zhang, S. Ren, and J. Sun, "Deep residual                 plan, P. Dhariwal, A. Neelakantan, P. Shyam, G. Sastry,
      learning for image recognition," in IEEE Conference on             A. Askell, S. Agarwal, A. Herbert-Voss, G. Krueger,
      Computer Vision and Pattern Recognition, 2016.                     T. Henighan, R. Child, A. Ramesh, D. M. Ziegler, J. Wu,
                                                                         C. Winter, C. Hesse, M. Chen, E. Sigler, M. Litwin,
 [8] A. Gujarati, R. Karimi, S. Alzayat, W. Hao, A. Kauf-                S. Gray, B. Chess, J. Clark, C. Berner, S. McCandlish,
      mann, Y. Vigfusson, and J. Mace, "Serving DNNs like                A. Radford, I. Sutskever, and D. Amodei, "Language
      clockwork: Performance predictability from the bottom              models are few-shot learners," arXiv, 2020.
      up," in USENIX OSDI, 2020.
                                                                   [20] H. Touvron, T. Lavril, G. Izacard, X. Martinet, M.-A.
 [9] H. Zhang, Y. Tang, A. Khandelwal, and I. Stoica, "Shep-             Lachaux, T. Lacroix, B. Rozière, N. Goyal, E. Hambro,
      herd: Serving dnns in the wild," in USENIX NSDI, 2023.             F. Azhar, A. Rodriguez, A. Joulin, E. Grave, and G. Lam-
                                                                         ple, "Llama: Open and efficient foundation language
[10] G.-I. Yu, J. S. Jeong, G.-W. Kim, S. Kim, and B.-                   models," arXiv, 2023.
      G. Chun, "Orca: A distributed serving system for
      Transformer-Based generative models," in USENIX              [21] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit,
      OSDI, 2022.                                                        L. Jones, A. N. Gomez, L. Kaiser, and I. Polosukhin, "At-
                                                                         tention is all you need," Neural Information Processing
                                                                         Systems, 2017.

                                                               13
[22] C. Olston, N. Fiedel, K. Gorovoy, J. Harmsen, L. Lao,            [36] J. Kaplan, S. McCandlish, T. Henighan, T. B. Brown,
      F. Li, V. Rajashekhar, S. Ramesh, and J. Soyke,                       B. Chess, R. Child, S. Gray, A. Radford, J. Wu, and
      "Tensorflow-serving: Flexible, high-performance ml                    D. Amodei, "Scaling laws for neural language models,"
      serving," arXiv, 2017.                                                arXiv, 2020.

[23] N. Corporation, "Triton inference server: An optimized           [37] D. Narayanan, M. Shoeybi, J. Casper, P. LeGres-
      cloud and edge inferencing solution.," 2019.                          ley, M. Patwary, V. A. Korthikanti, D. Vainbrand,
                                                                            P. Kashinkunti, J. Bernauer, B. Catanzaro, A. Phan-
[24] M. Ott, S. Edunov, A. Baevski, A. Fan, S. Gross, N. Ng,                ishayee, and M. Zaharia, "Efficient large-scale language
      D. Grangier, and M. Auli, "fairseq: A fast, extensible                model training on gpu clusters using megatron-lm,"
      toolkit for sequence modeling," arXiv, 2019.                          arXiv, 2021.

[25] T. Wolf, L. Debut, V. Sanh, J. Chaumond, C. Delangue,            [38] D. Narayanan, A. Harlap, A. Phanishayee, V. Seshadri,
      A. Moi, P. Cistac, T. Rault, R. Louf, M. Funtowicz,                   N. R. Devanur, G. R. Ganger, P. B. Gibbons, and M. Za-
      J. Davison, S. Shleifer, P. von Platen, C. Ma, Y. Jer-                haria, "Pipedream: Generalized pipeline parallelism for
      nite, J. Plu, C. Xu, T. L. Scao, S. Gugger, M. Drame,                 dnn training," in ACM SOSP, 2019.
      Q. Lhoest, and A. M. Rush, "Huggingface's transform-
      ers: State-of-the-art natural language processing," arXiv,      [39] P. Moritz, R. Nishihara, S. Wang, A. Tumanov, R. Liaw,
      2020.                                                                 E. Liang, M. Elibol, Z. Yang, W. Paul, M. I. Jordan, and
                                                                            I. Stoica, "Ray: A distributed framework for emerging
[26] N. Corporation, "Fastertransformer," 2019.                             AI applications," in USENIX OSDI, 2018.

[27] L. Schrage, "A proof of the optimality of the shortest           [40] G. Prekas, M. Kogias, and E. Bugnion, "Zygos: Achiev-
      remaining processing time discipline," Operations Re-                 ing low tail latency for microsecond-scale networked
      search, 1968.                                                         tasks," in ACM SOSP, 2017.

[28] N. Shazeer, "Fast transformer decoding: One write-head           [41] Y. Zhong, S. Liu, J. Chen, J. Hu, Y. Zhu, X. Liu, X. Jin,
      is all you need," arXiv, 2019.                                        and H. Zhang, "Distserve: Disaggregating prefill and
                                                                            decoding for goodput-optimized large language model
[29] J. Ainslie, J. Lee-Thorp, M. de Jong, Y. Zemlyanskiy,                  serving," in USENIX OSDI, 2024.
      F. Lebrón, and S. Sanghai, "Gqa: Training generalized
      multi-query transformer models from multi-head check-           [42] B. Wu, S. Liu, Y. Zhong, P. Sun, X. Liu, and X. Jin,
      points," arXiv, 2023.                                                 "Loongserve: Efficiently serving long-context large lan-
                                                                            guage models with elastic sequence parallelism," arXiv,
[30] D. Li*, R. Shao*, A. Xie, Y. Sheng, L. Zheng, J. E.                    2024.
      Gonzalez, I. Stoica, X. Ma, and H. Zhang, "How long
      can open-source llms truly promise on context length?,"         [43] M. Chowdhury, Y. Zhong, and I. Stoica, "Efficient
      2023.                                                                 coflow scheduling with varys," in ACM SIGCOMM,
                                                                            2014.
[31] C.-Y. Hong, M. Caesar, and P. B. Godfrey, "Finishing
      flows quickly with preemptive scheduling," in ACM               [44] K. Kaffes, T. Chong, J. T. Humphries, A. Belay, D. Maz-
      SIGCOMM, 2012.                                                        ières, and C. Kozyrakis, "Shinjuku: Preemptive schedul-
                                                                            ing for µsecond-scale tail latency," in USENIX NSDI,
[32] M. Alizadeh, S. Yang, M. Sharif, S. Katti, N. McKeown,                 2019.
      B. Prabhakar, and S. Shenker, "pfabric: Minimal near-
      optimal datacenter transport," SIGCOMM CCR, 2013.               [45] A. Ousterhout, J. Fried, J. Behrens, A. Belay, and H. Bal-
                                                                            akrishnan, "Shenango: Achieving high cpu efficiency
[33] M. Chowdhury and I. Stoica, "Efficient coflow schedul-                 for latency-sensitive datacenter workloads.," in USENIX
      ing without prior knowledge," SIGCOMM CCR, 2015.                      NSDI, 2019.

[34] J. Gu, M. Chowdhury, K. G. Shin, Y. Zhu, M. Jeon,                [46] J. Fried, Z. Ruan, A. Ousterhout, and A. Belay, "Caladan:
      J. Qian, H. H. Liu, and C. Guo, "Tiresias: A gpu clus-                Mitigating interference at microsecond timescales," in
      ter manager for distributed deep learning.," in USENIX                USENIX OSDI, 2020.
      NSDI, 2019.
                                                                      [47] Z. Bai, Z. Zhang, Y. Zhu, and X. Jin, "Pipeswitch: Fast
[35] Z. Li, L. Zheng, Y. Zhong, V. Liu, Y. Sheng, X. Jin,                   pipelined context switching for deep learning applica-
      Y. Huang, Z. Chen, H. Zhang, J. E. Gonzalez, et al., "Al-             tions," in USENIX OSDI, 2020.
      paServe: Statistical multiplexing with model parallelism
      for deep learning serving," in USENIX OSDI, 2023.

                                                                  14
[48] M. Han, H. Zhang, R. Chen, and H. Chen, "Microsecond-
      scale preemption for concurrent GPU-accelerated DNN
      inferences," in USENIX OSDI, 2022.

[49] D. Crankshaw, X. Wang, G. Zhou, M. J. Franklin, J. E.
      Gonzalez, and I. Stoica, "Clipper: A low-latency online
      prediction serving system.," in USENIX NSDI, 2017.

[50] J. Fang, Y. Yu, C. Zhao, and J. Zhou, "Turbotransform-
      ers: an efficient gpu serving system for transformer mod-
      els," in ACM PPoPP, 2021.

[51] D. Li, R. Shao, H. Wang, H. Guo, E. P. Xing, and
      H. Zhang, "Mpcformer: fast, performant and private
      transformer inference with mpc," arXiv, 2023.

[52] Y. Sheng, S. Cao, D. Li, B. Zhu, Z. Li, D. Zhuo, J. E.
      Gonzalez, and I. Stoica, "Fairness in serving large lan-
      guage models," in USENIX OSDI, 2024.

[53] P. Patel, E. Choukse, C. Zhang, A. Shah, Íñigo Goiri,
      S. Maleki, and R. Bianchini, "Splitwise: Efficient gener-
      ative llm inference using phase splitting," in ACM/IEEE
      ISCA, 2024.

[54] Y. Bai, C. Li, Q. Zhou, J. Yi, P. Gong, F. Yan, R. Chen,
      and Y. Xu, "Gradient compression supercharged high-
      performance data parallel dnn training," in ACM SOSP,
      2021.

[55] J. Wang, B. Yuan, L. Rimanic, Y. He, T. Dao, B. Chen,
      C. Re, and C. Zhang, "Fine-tuning language models
      over slow networks using activation quantization with
      guarantees," Neural Information Processing Systems,
      2022.

[56] Z. Li, E. Wallace, S. Shen, K. Lin, K. Keutzer, D. Klein,
      and J. Gonzalez, "Train big, then compress: Rethink-
      ing model size for efficient training and inference of
      transformers," in International Conference on Machine
      Learning (ICML), 2020.

[57] G. Xiao, J. Lin, M. Seznec, J. Demouth, and S. Han,
      "Smoothquant: Accurate and efficient post-training quan-
      tization for large language models," International Con-
      ference on Machine Learning, 2022.

[58] E. Frantar, S. Ashkboos, T. Hoefler, and D. Alistarh,
      "Gptq: Accurate post-training quantization for generative
      pre-trained transformers," arXiv, 2022.

[59] T. Dettmers, M. Lewis, Y. Belkada, and L. Zettlemoyer,
      "Llm. int8 (): 8-bit matrix multiplication for transformers
      at scale," arXiv, 2022.

[60] N. Zheng, B. Lin, Q. Zhang, L. Ma, Y. Yang, F. Yang,
      Y. Wang, M. Yang, and L. Zhou, "SparTA: Deep-
      Learning model sparsity via Tensor-with-Sparsity-
      Attribute," in USENIX OSDI, 2022.

                                                                           15
